# BlindAssist 想法池

> 本文件只保留尚未决定的产品、架构和研究方向，以及已完成方向的简短可追溯标记。当前 SANPO 状态以 [docs/SANPO_CURRENT_STATUS.md](docs/SANPO_CURRENT_STATUS.md) 为准；逐轮实验结果进入日期化 `docs/` 快照或 `DEVELOPMENT_LOG.md`，不要把每次运行都追加为新 idea。

## 维护规则

- 新条目必须说明要解决的问题、尚未验证的假设和进入实施前的门槛。
- 已采纳的不可逆技术决定移入 `docs/decisions/`；已完成方向保留短摘要并标记状态。
- 以下既有研究记录完整保留，只作为历史检索，不覆盖当前状态或协议。

## 【部分完成】独立根因诊断、反事实事件与生命周期头路线

- 用户原话：**“写入idea，开始独立测试：先做确定性线性 probe，判断究竟是特征问题还是 head 优化问题。如果特征可分，做 prototype/bootstrap 初始化的五组短跑。再单独测试距离场辅助监督。同时开始采集反事实 episode。中期做‘风险轮廓 + 生命周期头’原型，让像素分割降为辅助监督。SAM/ASAM可以作为第三顺位优化实验；它可能找到更平坦的解，但不应早于根因诊断。”**
- 目标：先用独立、可否证的实验区分 backbone 特征不可分与 head 初始化/优化不稳，再决定是否改初始化、辅助监督或任务头；同时把训练和晋级单位从高度相关的单帧/像素，转为与产品行为一致的物理风险事件。同一事件只需在有效接近窗口成功提醒一次，通过或清除后不得再次提醒。通过同一 session 覆盖多种 scene、同一 scene 跨多个 session，并为每个正事件采集视觉相近但不应提醒的 matched negative，减少模型把 session 背景误学成场景或风险的可能。
- 已开始执行：新增 [反事实事件采集协议](docs/SANPO_COUNTERFACTUAL_EPISODE_COLLECTION.md) 与离线配置 `configs/sanpo_counterfactual_episode_collection_v1.json`，冻结首轮 `6 sessions × 4 scenes × 每格 2 positive + 2 matched negative`，共 96 个 episode、48 个 matched pair。当前仅完成协议和配置，尚未采集、复核、训练或运行设备门，因此标记为【部分完成】。
- 2026-07-13 根因诊断结果：确定性 raw OS8 probe 与 raw OS8+OS32 联合 probe 均做到两次闭式 ridge 系数完全一致，但分别只有 global mIoU `0.2475/0.3308`、boundary IoU `0.0205/0.0297`，最差 `step_curb` session mIoU `0.0671/0.0990`，未达到预注册 `mIoU>=0.35 + boundary IoU>=0.20` 可分门。因此按用户条件停止 prototype/bootstrap 五组短跑；当前优先修复表征、taxonomy 与 P3 session/split 分布，而不是继续搜索随机 head 初始化。完整证据见 [确定性 probe 审计](docs/SANPO_DETERMINISTIC_LINEAR_PROBE_2026-07-13.md)。
- 2026-07-13 距离场前置结果：辅助 target/权重与 blind 拒绝合同已通过独立测试，但 384 mask 中 dev/train boundary 像素占比约为 `16.976%/0.857%=19.8×`。在 P3 split 重构前直接训练距离头无法可靠归因，因此暂不接入 trainer；这属于按停止门主动暂停，不是否定距离场机制本身。
- 2026-07-17 公开视频主线追加结论：r4 加入 `gie8` 静态家具同源反事实后，冻结对象轨迹 probe 仍可分但 balanced accuracy 从 `.8167` 降到 `.75`，五组 prototype/bootstrap 优化中位数降到 `.6667` 且只有 `2/5` 达标；冻结 DINO 为 `.6667`，显式相对深度走廊轮廓仅 `.50`。这说明问题不是“再换一个通用 backbone 或加简单深度统计”即可解决，而是正例机制异质、静态通道净空缺少专门表征、每种机制的独立 matched source 太少。
- 2026-07-17 机制分层扩展：在完全独立的公开视频主线 r5 中新增 `Chcne` 第二组静态通道收窄同源 pair，形成 14 条非弃权 episode、正负各 7、4 个 pair。对象轨迹 probe 回升到 `.7857`，风险轮廓/潜生命周期 MIL 中位数从 r4 的 `.5833` 回升到 `.7857`、pair 排序中位数从 `.6667` 升到 `.75`，证明“补同机制数据”方向有效；但五组 head 仍只有 `3/5` 达标，范围 `.6429–.9286`，且 `gie8/Chcne` 两个静态 no-alert 在 source 留出时均被误报。因此新增硬门：`dynamic_agent_approach` 与 `static_corridor_narrowing` 各至少 3 个独立 matched source；当前各只有 2 个，禁止以总体 4 对宣称数据充分，也不进入 SAM/ASAM。
- 2026-07-17 r6 低置信度动态 pair 试验：从 `JtMY` 同一官方 SANPO train session 的既有 RGB 中隔离早段 226–300 开放路线与晚段 339–429 马车近距离横穿，保留原正例 `.63` 置信度，不抬分。机制门新增 `pair 内最低 episode confidence >= .65`，因此动态机制虽然表面已有 3 对，合格覆盖仍为 2 对/2 来源，静态仍为 2 对/2 来源。冻结对象轨迹 `.7321`、五组短跑中位数 `.75` 且仍仅 `3/5` 通过；风险轮廓/潜生命周期 MIL 中位数升至 `.8661`、五组都正确排序 `JtMY` pair，说明“时序风险轮廓”比单一 episode 向量更适合横穿/接近事件，但该低置信度 pair 只能作为结构诊断，不能关闭数据门。
- 2026-07-17 置信度加权 OFAT：保持 r6 的冻结检测、风险轮廓、LOSO、bootstrap、五组 seed、步数与超参数完全不变，只把每类内部 episode loss 从等权改为按银标 confidence 线性分配。加权前后五组 balanced accuracy 与 pair 排序逐项完全相同，最大单 episode 概率变化约 `.01`，因此停止继续试更复杂 confidence weighting；当前主要矛盾是机制覆盖和静态净空表征，不是轻量 loss 重加权。
- 隔离原则：另一个独立模型方向只允许作为方法风险参考，禁止复用其数据、指标、目录、权重或晋级结论；公开视频银标 r4/r5/r6 构建器和机制门均对该目录名 fail closed，避免互相破坏实验结构。
- 阶段一——确定性线性 probe：冻结同一 backbone 和同一 train/dev split，在固定特征上训练可重复的线性分类/像素 probe，按 model seed、session、scene、boundary/unknown 报告可分性。若线性 probe 在 worst-session / worst-scene 仍不可分，停止 head 优化路线，优先修复数据、taxonomy 或 backbone 表征；不得用更复杂优化器掩盖特征问题。
- 阶段二——prototype/bootstrap 初始化：仅在线性 probe 证明特征可分后，使用类 prototype 或 bootstrap 得到的 head 初始化进行预注册五组短跑，与随机初始化保持相同数据、step、sampler 和评价合同。只有 model-seed range、最差 seed 和最差 session 同时改善才保留；仅提高最佳 seed 即停止。
- 阶段三——距离场辅助监督：作为独立 OFAT 实验，为 boundary/step/curb 生成有符号或截断距离场辅助目标，检验它是否改善细边界和最差 session，而不改变事件主门。若只改善几何 boundary 指标、却降低 obstacle/unknown 或事件召回，则停止。
- 并行阶段——反事实事件采集与稀疏标注：每个正事件只标 `first_visible`、`alertable_start`、`passed_or_cleared` 三个时间点、稳定的 `risk_event_id` 和 matched pair；负例标出完整观察区间并明确 `expected_should_alert=false`。每条 session 必须覆盖四类 scene，禁止“一条 session 等于一个 scene”。固定 leave-one-session-out 六折，训练、校准和阈值选择不得访问 holdout session。
- 中期阶段——风险轮廓 + 生命周期头：用风险轮廓提供空间证据，用生命周期头直接预测 approach / alertable / passed-or-cleared 与一次提醒决策；像素分割降为辅助监督。主指标改为 event recall、critical miss、false alerts/min、实际交付提醒次数/event、清除时延和 event regeneration，逐帧 recall、mIoU 与像素边界只作诊断。
- 第三顺位——SAM/ASAM：仅在完成特征/head 根因诊断、初始化和距离场对照后，才测试是否能找到更平坦且跨 seed 更稳定的解。必须用相同五组 seed 和 worst-group 门评估；不得因训练 loss 更平滑或单 seed 更好而晋级。
- 后续主动失败闭环：在独立长序列中按模型分歧、事件重复再生和高置信误提醒生成审计队列，人工只做事件接受/拒绝、事件合并与三个时间点复核；至少 20% 候选来自固定随机审计，防止模型盲区永远不进入主动队列。
- 成功门：六个 LOSO fold 均满足 `event recall >= 0.90`、`critical miss rate <= 0.05`、`false alerts/min <= 0.50`、`delivered repeated alert rate <= 0.10`、`post-event clearance rate >= 0.90`；同时报告 worst-session / worst-scene，且 matched negative 不得靠 session 身份或静态背景被轻易区分。任何离线成功都不自动授权模型替换。
- 停止门：线性 probe 证明固定特征不可分时停止继续调 head；prototype/bootstrap 只提高最佳 seed、不改善最差 seed/session 时停止；距离场只改善像素边界、不改善最差组或造成 obstacle/unknown/事件退化时停止；SAM/ASAM 不得提前替代根因诊断。任一 holdout session 出现关键事件漏报上升、反事实负例压制真实侧向切入或中心侵入、session identity probe 显著高于随机、事件时间点不能在容差内一致复核，或主动队列持续漏掉随机审计中的系统性失败时，也必须停止扩训并回到数据或任务合同。
- 安全边界：本路线仅用于 benchmark-only 候选和受控研究，不接入或替换 App 默认模型，不用 holdout 调参，不以单个最佳 seed、平均 mIoU 或 GPU 速度宣布晋级。采集前须具备许可、隐私复核和本地原始资产 SHA256 闭环；涉及行走风险的采集必须由健视安全员控制，在静态或受控路线中完成，不要求盲人参与者暴露于未验证模型决策，且始终明确该原型不替代盲杖、导盲犬、人工判断或专业辅助设备。

## 【部分完成】SANPO Traversability v2

- `v10.4.0` 已通过当前 30 帧否定集：错误提醒率 `3.3%`、主区域命中 `86.7%`、total P95 `65.919ms`。
- 已补充许可明确的公开平行路沿、正前方台阶、中心通道障碍连续序列；扩展集危险提醒召回 `88.9%`，但错误提醒率 `25.9%` 未达门槛。
- 2026-07-13 已完成 14 条官方 session、720 帧 real-only canonical、step-budgeted 三 seed 训练和三段晋级门。最佳 384 候选把 mIoU/boundary IoU 提高到 `0.4344/0.4506`，但其余 seed 明显回落，离线门仍有三项 red，因此没有进入 INT8 和设备事件门。
- 待完成：把优化目标从“最好 seed”改成“跨 session 宏平均 + 最差场景 + 最差 seed”；优先补强 step/curb 中的 unknown 与 obstacle 互混、无 boundary 场景的 boundary 幻觉，并在离线门全绿后才进入 INT8 和同设备事件复测。

### 2026-07-13：文献驱动的研究指导（记录）

- 依据：已阅读本地论文缓存 `.downloads/papers/2026-07-assistive-navigation/` 中的 SANPO、RGB-D 助盲系统、手机障碍检测、MobileNetV3、FastDepth、Lite-Mono 与 BiSeNet V2。它们的共同结论不是立即替换模型，而是把 BlindAssist 定义为“连续可通行走廊的风险事件系统”：分割/检测输出只提供证据，连续帧确认风险，事件生命周期控制提醒次数。
- 当前优先级：先关闭风险事件误提醒门，而不是训练分割模型。现有 `RiskEventTracker` 已有 `APPROACHING -> ALERTED -> PASSED_OR_RECEDING -> CLEARED` 状态；下一步应区分“真正通过风险”和“短暂 mask 丢失”，为已提醒事件加入冷却与再入场条件。必须新增并报告 `alerts_per_event`、`duplicate_alert_events`、`post_pass_alert_frames`、`false_alert_duration_ms`；通过台阶后的重复提醒必须为零。
- 走廊语义：将“最大分割连通域”升级为“中心近场走廊是否被切断”的证据。建议输出 `walkablePathContinuity`、`corridorBlockedWidthRatio`、`boundaryCrossesCorridor`、`nearFieldOccupancy`。`boundary_step_curb` 默认仅是边界证据；只有横断中心走廊，且同时满足近场、持续或相对接近等至少两类证据时才允许提醒。平行路沿不得单独升到提醒等级。
- 时序证据：`TemporalRiskTracker` 不能仅因框面积、底边或相对深度的任一变化就认定逼近。下一候选应要求两类一致证据或单一证据持续多帧，并加入低成本的全局相机运动/帧质量 veto，抑制头动、俯仰和视差造成的假逼近。深度仅可作确认或拒绝证据，不得独立提升风险。
- 模型顺序：事件闭环与走廊规则通过后，才以 SANPO-Synthetic 预训练、SANPO-Real 微调训练全 INT8 `MobileNetV3 + Lite R-ASPP` 四类分割候选（`walkable`、`boundary_step_curb`、`obstacle`、`unknown_nonwalkable`）。`BiSeNetV2` 仅在 MobileNetV3 持续漏掉台阶/路沿边界时作为同数据、同契约的对照；FastDepth 与 Lite-Mono 保持离线相对深度消融，不进入实时提醒主链。
- 候选晋级门：在冻结的连续序列与独立盲测 session 上，危险事件提醒召回不得低于当前 `88.9%`，错误提醒率须不高于几何基线 `5.3%`，关键漏报不得增加，`post_pass_alert_frames=0`，并预先登记端到端 P95 延迟预算（当前建议不高于 `70ms`）。不得用 mIoU、GPU FPS 或单帧深度误差单独宣布模型可上线。
- 证据状态矩阵：后续报告不得再用单一 `training_authorized=true` 代表整条路线可发布。至少分开报告 `raw-source closure`、底层原始资产跨 split 隔离、训练规模/域覆盖、Torch↔TensorFlow 浮点等价、TensorFlow↔全 INT8 TFLite 量化保真、blind 风险事件门和同设备延迟/误提醒门；只有全部绿色才允许进入 App A/B。
- 数据边界：当前 `300 train/dev + 120 blind` 的 420 帧应定位为回归/门禁集，不宣称足以训练通用分割器。下一轮训练集需按独立 source session、有效独立帧数和场景域覆盖扩充；blind 不能只含程序化盲道序列，还要覆盖真实台阶/路沿、中心障碍、平行边界和动态侧向目标。
- 不确定性：把 `unknown_nonwalkable` 从被动兜底类升级为可校准的 `unknown/abstain` 证据，报告走廊未知占用率、拒判率和 selective risk；未知区域不得被强行改写成普通 obstacle，只有持续切断中心近场走廊时才产生克制的“路径不确定”事件。
- 人因验证：模型与事件门闭合后，再在受控条件下记录碰撞/触碰、完成时间、路线偏离、人工干预、每事件提醒数和主观提醒负担。遵循“安全直行时安静、风险事件一次提醒、详细语义按需获取”，避免用提醒数量增加冒充安全提升。
- 产品边界：学习 Lin 等助盲系统的“无风险少说话、输出可行动方向”原则，但不采用不可解释的端到端动作模仿替代当前风险证据链；所有路线继续明确为辅助提醒原型，不替代盲杖、导盲犬、人工判断或专业辅助设备。

本文件用于收集 BlindAssist 后续可能性、创造性想法和产品成熟化方向。它不是开发日志，也不代表所有想法都会立即实施；真正落地时仍需同步更新 `README.md`、`DEVELOPMENT_LOG.md`、版本号、测试结果和 APK 归档。

记录原则：

- 有创造性、可能性或未来路线价值的想法，优先写入本文件。
- 想法可以先粗略记录，但应尽量说明价值、风险和适合落地的阶段。
- 已经实施的想法不要删除，应把标题改成 `### 【已完成】原想法标题`，并在条目中补充实现版本、完成时间、验证证据或被放弃原因。
- 如果一个大想法只实现了一部分，先标注为 `### 【部分完成】原想法标题`，并写清楚已完成范围和剩余部分。
- 涉及助盲安全能力时保持谨慎表述，不把原型能力描述为可以替代人工判断、盲杖、导盲犬或专业辅助设备。

## 近期可实施方向

### 【已完成】推荐优先：日常使用向导与一键模式

- 想法：在现有设置项和相机页之上增加“日常使用向导”，把提醒档位、使用场景、语音风格、震动强度、关怀模式等分散选择整合成几个清晰的一键模式，并在相机会话前后给出轻量引导。
- 价值：当前 v5.3.0 已经有比较完整的偏好、无障碍和中英文能力，但新用户仍需要理解很多设置项。下一步若想大幅提高使用体验，最有价值的不是继续增加开关，而是降低决策成本，让用户一进入 App 就能选择“通用日常、室内慢行、走廊通行、密集区域、户外慢行”等实际任务，并由系统应用一组推荐配置。
- 可落地内容：
  - 在 Features 页或进入相机前增加“今天怎么使用”选择区，提供 4-5 个任务模式。
  - 每个模式映射到现有 `AssistScenario`、`AlertProfile`、`SpeechStyle`、`VibrationStrength` 和 Care Mode，不更换模型、不新增权限、不引入联网或定位。
  - 相机页显示当前模式的短解释和“一键调安静 / 一键调敏感”快捷操作，减少在设置页来回切换。
  - 结束相机会话后用现有现场测试摘要生成“本次提醒是否太频繁 / 太少 / 合适”的反馈入口，并给出下一次推荐配置。
  - 补充 JVM 测试覆盖模式到偏好组合的映射，补充 Compose 测试覆盖模式选择、相机页展示和语言切换下的文案。
- 风险：不能把推荐配置说成自动理解真实场景；它仍应明确是用户手动选择的辅助预设。会话后反馈只应用于本地偏好建议，不应暗示已经完成个性化机器学习。
- 已完成范围：`v5.8.0` 已在 Features 页新增日常使用向导，提供五个日常预设；每个预设映射到既有 `AssistScenario`、`AlertProfile`、`SpeechStyle`、`VibrationStrength` 和 Care Mode，并通过现有 SharedPreferences 偏好持久化。当前偏好组合如果不匹配任何预设，会显示为 自定义 / Custom。
- 已完成范围补充：相机页已显示当前日常模式，并把提醒档位循环改为 调安静 / 调敏感 两个快捷操作；二者保留当前场景，只调整提醒档位、语音风格和震动强度。
- 实现版本：`v5.8.0` / `versionCode=22`。
- 完成时间：2026-05-19 12:06:16 +08:00。
- 验证证据：完整 JVM 单测和 debug APK 构建已通过，105 tests / 0 failures / 0 errors / 0 skipped；debug APK 已归档到 `releases/apk/BlindAssist-v5.8.0-debug-20260519-120747.apk`，并安装到 `SM-S9280` 核对为 `versionCode=22` / `versionName=5.8.0`。本轮未运行 `connectedDebugAndroidTest`，因为设备在线但处于锁屏状态 `mDreamingLockscreen=true`。

### 【已完成】离线测试素材回放最小版

- 想法：做一个 debug-only 或演示用的“素材回放”入口，用少量授权图片稳定复现检测框、风险等级、语音/震动决策和风险解释。
- 价值：答辩、课堂展示和录屏时不完全依赖现场光线、行人、障碍物和设备网络状态，老师能更稳定地看到核心链路。
- 可落地内容：
  - 先放入 3-5 张体积较小、来源明确的本地测试图片。
  - 复用现有 `ObjectDetector`、`AssistSessionCoordinator`、`CameraGuidanceMapper` 和现场测试摘要映射，不新增网络、存储权限或模型。
  - 每张素材记录“预期可观察现象”，例如检测框、风险方向、提醒是否触发、解释文案。
  - 演示入口明确标注为测试/展示辅助，不替代真实摄像头体验。
- 风险：素材授权和仓库大小需要控制；不要把离线素材结果描述成真实场景泛化能力。
- 已完成范围：`v9.9.0` 已加入四张 CC BY 2.0 COCO debug-only 素材和 `ReplayFrameSource`，以 2 FPS 进入真实 detector→risk→feedback→overlay→session summary 链路；release 不包含 replay 资产或入口。
- 完成时间：2026-07-10。

### 【已完成】眼镜设备模拟中心

- 想法：把当前“连接眼镜设备”占位升级为模拟设备状态页，先展示连接状态、电量、摄像头来源和数据链路说明，不接入真实蓝牙。
- 价值：能支撑“多模态智能助盲系统”的产品想象，又不在没有硬件协议时贸然引入 BLE、网络或权限复杂度。
- 可落地内容：
  - 新增“模拟连接 / 已断开 / 低电量 / 使用手机摄像头”几种本地 UI 状态。
  - 在页面中说明当前版本仍以手机摄像头为真实输入，眼镜为未来扩展原型。
  - 预留 `FrameSource` 概念上的“手机摄像头 / 未来设备摄像头”来源说明，但不实现真实外部流。
- 风险：UI 文案要谨慎，不能让用户误以为已经支持真实眼镜硬件连接。
- 已完成范围：`v9.9.0` 已用全屏模拟中心替换占位入口，支持模拟连接、正常/低电量、断连、重置和 debug-only 回放；中英文与 TalkBack 均明确标注模拟，不扫描蓝牙、不联网、不连接真实眼镜。
- 完成时间：2026-07-10。

### 【部分完成】连续真实场景证据升级

- 想法：把现有 COCO 静态图片评测扩展为“连续真实步行序列 + 来源与授权 + 人工风险复核 + 统一晋级门槛”，用于验证逼近趋势、低矮/非 COCO 障碍、遮挡、运动与真实场景变化。
- 已完成范围：2026-07-11 新增 SANPO-Real 选择性下载、15→10 FPS 重采样、分割区域解析、无歧义 COCO 映射、来源哈希、许可证记录、QA 预览和 `draft -> review -> finalize` 门禁。首批本地集包含 30 帧/3 秒城市步行序列；三路独立 AI 逐帧复核已完成，并以显式 AI provenance 生成 canonical manifest，可进入工程 benchmark。
- 真机结论：首批 30 帧已在 Samsung `SM-S9280` 完成 Detector A/B benchmark。YOLO11n 与 YOLO26n 的通用障碍 approach recall 均为 0，且各有一次错误提醒；YOLO26n 更快但 FP/img 更高，默认模型不替换。该证据把下一算法重点明确指向可通行区域/通用障碍感知。
- 剩余范围：先用同一序列建立一个可通行区域或通用障碍候选基线，再补至少 6–10 个跨环境 session、自采低矮/透明/台阶/坑洞场景和人工双人复核风险标签；后续再决定是否进入 USB-C 眼镜样机。
- 风险：公开数据的相机高度、视场和地理环境与真实手机/眼镜存在域差异；任何自动分割或模型输出都不能替代人工助盲风险真值。

### 【部分完成】TalkBack 与大字体现场检查清单

- 想法：围绕现有 Compose 主界面、设置页、相机控制区和现场测试摘要，补一轮无障碍检查与小修。
- 价值：BlindAssist 本身是助盲主题，TalkBack、触控目标、状态语义和大字体适配比普通 App 更能体现项目专业性。
- 可落地内容：
  - 检查主要按钮、开关、场景选择、提醒档位选择是否有清楚的 TalkBack 状态描述。
  - 检查 48dp 触控目标、大字体下文本是否挤压或遮挡。
  - 给现场测试摘要、风险状态等关键标题补充合适的语义层级。
  - 补充 1-2 个 Compose UI 测试或静态说明记录。
- 风险：不要只为了“读出来很多”而增加冗长朗读；高风险提醒仍应保持短句。
- 已完成范围：`v5.3.0` 已补强设置页、语言选择器、提醒档位、使用场景、语音风格、震动强度、相机控制区和现场测试摘要的核心 TalkBack 语义；设置选择器改为 48dp+ 全宽控件，降低大字体下 chip 挤压风险；新增 Compose 仪器测试覆盖语言选择和关键 contentDescription。
- 剩余范围：本轮未做真实 TalkBack 人工验收，也未覆盖所有展示性静态长文案的大字体视觉走查；后续可在真机无障碍设置中补一轮手测记录。
- 实现版本：`v5.3.0` / `versionCode=21`。
- 完成时间：2026-05-19 11:37:40 +08:00。
- 验证证据：完整 JVM 单测和 debug APK 构建已通过；首次 `connectedDebugAndroidTest` 因重复 wireless ADB serial / UTP 状态超时，随后断开重复 serial、设置 `ANDROID_SERIAL` 后在 `SM-S9280 - 16` 上完成 5 个 Compose 仪器测试，0 failed。

### 【部分完成】多语言与提醒文案资源化准备

- 想法：把当前提醒短句、设置文案、TalkBack 文案逐步迁移到 Android `strings.xml` 资源，为后续中英文切换或系统语言适配做准备。
- 价值：既能提升工程完整度，也方便论文/答辩中说明“本地化、无障碍语义和反馈策略”。
- 可落地内容：
  - 先从语音提醒模板、风险解释、设置页选项这些高价值文案开始资源化。
  - 保留现有中文默认体验，增加英文资源作为后续可选项。
  - 更新测试，避免资源化后破坏已有 `FeedbackController` 文案预期。
- 风险：一次性迁移所有 UI 文案容易扩大改动范围；可以先做提醒链路和设置页。
- 已完成范围：`v5.3.0` 已新增 App 内 `中文` / `English` 语言偏好，默认中文；核心提醒链路、TTS locale、设置页控件、相机状态、风险解释、现场测试摘要和 TalkBack 语义已支持中英文；`values` 与 `values-en` 中补入核心资源项。
- 剩余范围：展示性静态长文案、完整 onboarding、Features/Profile 全量文案仍可继续迁移；本轮优先保证核心 assistive flow 可用。
- 实现版本：`v5.3.0` / `versionCode=21`。
- 完成时间：2026-05-19 11:37:40 +08:00。
- 验证证据：新增/更新 `UserPreferencesTest`、`FeedbackControllerTest`、`CameraGuidanceMapperTest`、`FieldTestSummaryMapperTest` 和 Compose 仪器测试覆盖语言偏好、TTS 文案、mapper 输出与设置页语言切换；完整 JVM 单测和 debug APK 构建已通过；单一 wireless ADB serial 下 `connectedDebugAndroidTest` 已在 `SM-S9280 - 16` 上通过 5 个测试。

### 现场测试脚本与评分表

- 想法：在仓库文档中补一个可执行的现场测试脚本，按“室内慢行、走廊通行、密集区域、户外慢行”记录检测表现、提醒频率、误报漏报和主观可用性。
- 价值：比只展示 App 截图更像真实课程设计/小毕设验证材料，也能让后续迭代有量化依据。
- 可落地内容：
  - 设计 4 个短场景，每个场景 2-3 分钟。
  - 记录版本号、手机型号、光线、场景、提醒档位、平均 FPS、触发次数和问题备注。
  - 与 `DEMO_GUIDE.md`、`PROJECT_PROGRESS_REVIEW.md` 保持一致，不伪造历史结果。
- 风险：如果没有实际测试，就只能写成模板或计划，不能写成已完成实验结论。

### 【已完成】v3.3.0 首次使用引导与权限说明

- 想法：为首次打开 App 的用户增加 2-3 页引导，说明手机摄像头检测、语音/震动提醒、原型安全边界和本地处理方式。
- 价值：让 App 从工程原型更接近真实移动产品，减少用户第一次打开时的陌生感，也便于课堂演示和答辩说明。
- 可落地内容：
  - 首次启动引导页。
  - 相机权限请求前的解释页。
  - 本地检测、不上传视频、不联网的隐私说明。
  - “辅助提醒原型，不替代盲杖、导盲犬或人工判断”的安全边界文案。
- 风险：文案要简短，不能把安全责任说得过满；引导不能阻塞老用户快速进入相机功能。
- 实现版本：`v3.3.0` / `versionCode=13`。
- 完成时间：2026-05-18 15:50:32 +08:00。
- 验证证据：已运行完整 Gradle 验证并通过；debug APK 已归档到 `releases/apk/BlindAssist-v3.3.0-debug-20260518-154943.apk`。本次真机安装未完成，原因是 ADB 返回 `no devices/emulators found`。

### 【已完成】现场测试记录与演示摘要

- 想法：在设置页或调试区域增加一次运行中的测试摘要，例如运行时长、平均 FPS、最近风险次数、提醒次数和当前提醒档位。
- 价值：让毕设展示更有证据感，证明 App 不只是界面好看，也能记录实际运行表现。
- 可落地内容：
  - 本次相机会话运行时长。
  - 最近 30 帧或最近 1 分钟的风险摘要。
  - 语音/震动提醒触发次数。
  - 平均推理耗时和 FPS。
- 风险：先保持内存统计即可，不急着写文件，避免引入存储权限和隐私风险。
- 实现版本：`v3.4.0` / `versionCode=14`。
- 完成时间：2026-05-18 19:24:08 +08:00。
- 验证证据：已运行完整 Gradle 验证并通过；debug APK 已归档到 `releases/apk/BlindAssist-v3.4.0-debug-20260518-192333.apk`，并通过 ADB over Wi-Fi 安装到 `SM_S9280`，包信息核对为 `versionCode=14`、`versionName=3.4.0`。

### 【已完成】无障碍细节升级

- 想法：系统检查 TalkBack 读屏顺序、按钮语义、状态描述、高对比和大字体适配。
- 价值：项目主题是助盲辅助，App 自身界面也应该体现无障碍意识。
- 可落地内容：
  - 为提醒档位、语音、震动、关怀模式补充更自然的状态语义。
  - 标题增加语义 heading。
  - 检查 48dp 触控目标。
  - 在大字体和高对比场景下验证主页面、设置页和相机控制面板。
- 风险：不要只堆 `contentDescription`，应优先保证读屏顺序和状态含义自然。
- 实现版本：`v3.4.0` / `versionCode=14`。
- 完成时间：2026-05-18 19:24:08 +08:00。
- 验证证据：设置页开关、提醒档位、相机页紧凑按钮和现场测试摘要已补充自然状态语义，交互目标保持 48dp 以上；完整 Gradle 验证和真机版本核验均已通过。

## 中期成熟化方向

### 【部分完成】ViewModel 与 StateFlow 状态拆分

- 想法：把主界面状态、相机状态、设置状态从 `MainActivity` 逐步拆到 ViewModel 和 StateFlow。
- 价值：随着 Compose 页面、设置项和相机状态增多，状态集中在 Activity 中会越来越难维护。轻量 ViewModel 化可以提升工程成熟度。
- 可落地内容：
  - App 壳层状态 ViewModel。
  - 设置偏好状态 ViewModel。
  - 相机会话状态模型。
  - UI 通过 `collectAsStateWithLifecycle()` 订阅状态。
- 风险：当前项目体量不大，先不急于引入 Hilt、多模块或复杂仓库层；避免为了架构而架构。
- 已完成范围：`v3.5.0` 已落地轻量首阶段。Compose 可观察的壳层状态、设置偏好状态、弹窗状态、相机激活状态、模型状态、引导状态和现场测试摘要已集中到 `BlindAssistViewModel`，通过只读 `StateFlow` 暴露，并由 Compose 使用 `collectAsStateWithLifecycle()` 订阅。
- 已完成范围补充：`v4.8.0` 在不引入 Hilt、多模块、DataStore、Room、新权限、联网或定位的前提下，继续拆出 `CameraXFrameSource`、`ObjectDetector`、`AssistSessionCoordinator`、`FpsTracker`、`CameraGuidanceMapper` 和 `FieldTestSummaryMapper`，让 `MainActivity` 从总控类变成更薄的生命周期和权限入口。
- 剩余范围：更深层的权限事件状态机、未来设备来源切换和 repository/use case 分层仍保留为后续方向。
- 实现版本：`v3.5.0` / `versionCode=15`；补充升级版本 `v4.8.0` / `versionCode=20`。
- 完成时间：2026-05-18 19:38:19 +08:00。
- 验证证据：已运行完整 Gradle 验证并通过；debug APK 已归档到 `releases/apk/BlindAssist-v3.5.0-debug-20260518-193819.apk`，并通过 ADB over Wi-Fi 安装到 `SM_S9280`，包信息核对为 `versionCode=15`、`versionName=3.5.0`。

### 【部分完成】Compose UI 测试补强

- 想法：在已有纯 Kotlin 风险规则测试之外，补充基础 Compose UI 测试。
- 价值：防止后续界面迭代破坏底部导航、设置开关、相机入口和返回路径。
- 可落地内容：
  - 底部导航页面切换测试。
  - “使用手机摄像头”入口测试。
  - 相机页返回主界面的状态测试。
  - 设置开关状态展示测试。
- 风险：相机预览和真实权限不适合一开始就纳入 UI 自动化，可先测试壳层和纯 UI 状态。
- 已完成范围：`v3.6.0` 已新增最小 Compose 仪器测试文件，覆盖设置页反馈细项和相机控制面板基础控件，并新增 debug-only `ComposeTestActivity` 作为测试宿主。
- 剩余范围：`connectedDebugAndroidTest` 的 `Process crashed` 已在 v3.6.0 后续修复中解决；后续可继续扩展到底部导航、相机入口、返回路径和更复杂的大字体场景。
- 实现版本：`v3.6.0` / `versionCode=16`。
- 完成时间：2026-05-18 21:29:31 +08:00。
- 验证证据：完整 JVM 单测、debug APK 构建和 `connectedDebugAndroidTest` 均已通过；详见 `DEVELOPMENT_LOG.md` 的 v3.6.0 仪器测试修复记录。

### 【已完成】发布与展示材料整理

- 想法：补充更清晰的版本路线、演示说明和隐私说明，让项目更像可提交、可展示、可答辩的完整作品。
- 价值：课程设计和毕设展示不仅看代码，也看过程、版本、验证和边界说明。
- 可落地内容：
  - `CHANGELOG.md` 或 README 版本路线章节。
  - 演示流程说明。
  - 隐私和安全边界说明。
  - APK 归档列表整理。
- 风险：文档不能替代真实功能验证；每个版本仍应保留构建、安装和测试证据。
- 已完成范围：`v4.1.0` 已新增 App 内“项目展示中心”，集中展示本地识别、语音/震动提醒、现场测试摘要和原型安全边界，并复用现有手机摄像头演示入口和新手引导回放入口；新增 `CHANGELOG.md` 和 `DEMO_GUIDE.md`，整理真实版本路线、演示脚本、无设备 fallback、隐私说明、安全边界和 APK 归档线索。`v4.3.0` 已按当前使用需要暂时移除 App 内“项目展示中心”，保留仓库文档和 APK 归档作为展示材料，后续答辩如需要可再设计更轻量入口。
- 剩余范围：后续如果需要正式答辩提交包，可继续把截图、课堂讲解词或 PPT 成稿纳入材料，但不应改写真实开发日志时间线。
- 实现版本：`v4.1.0` / `versionCode=17`。
- 完成时间：2026-05-18。
- 验证证据：`v4.1.0` 已完成 JVM/构建验证，debug APK 已归档到 `releases/apk/BlindAssist-v4.1.0-debug-20260518-231542.apk`，并安装到 `SM-S9280` 核对为 `versionCode=17`、`versionName=4.1.0`。重新连接并确认设备不在锁屏/Bouncer 状态后，`connectedDebugAndroidTest` 已在 `SM-S9280 - 16` 上完成 4 个 Compose 仪器测试并通过，具体命令和输出见 `DEVELOPMENT_LOG.md`。

## 长期创造性可能

### 眼镜设备连接原型

- 想法：在当前“连接眼镜设备”占位基础上，未来做一个真实或模拟的设备连接流程。
- 价值：符合“多模态智能助盲系统”的长期产品想象，也能把手机 App 从单端工具扩展为设备控制中心。
- 可落地内容：
  - 先做模拟设备状态，不申请蓝牙权限。
  - 后续再评估 BLE 扫描、配对、连接状态和设备电量。
  - 增加“眼镜摄像头 / 手机摄像头”来源切换概念。
- 风险：真实蓝牙和硬件协议会显著增加复杂度；没有硬件前不要做过度承诺。

### 【已完成】场景化提醒模式

- 想法：根据不同使用场景设置提醒策略，例如室内、走廊、楼梯口、户外慢行。
- 价值：比单纯的 Quiet/Standard/Sensitive 更贴近真实使用语境。
- 可落地内容：
  - 保留现有提醒档位，增加场景说明或场景预设。
  - 不同场景调整中风险确认帧数、震动强度和语音冷却时间。
  - 在 README 和日志中明确这些仍是规则层策略，不是绝对安全判断。
- 风险：如果没有传感器或可靠场景识别，先让用户手动选择，不要声称自动识别真实场景。
- 实现版本：`v4.2.0` / `versionCode=18`。
- 完成时间：2026-05-19 00:02:00 +08:00。
- 已完成范围：新增 `AssistScenario` 手动场景，包含通用、室内慢行、走廊通行、密集区域和户外慢行；场景与提醒档位共同调整中风险确认、提醒保持、近距冷却和震动时长。设置页、相机控制面板、现场测试摘要、风险解释、偏好持久化和 JVM/Compose 测试均已同步。
- 验证证据：已运行完整 JVM 单元测试和 debug APK 构建；debug APK 归档为 `releases/apk/BlindAssist-v4.2.0-debug-20260519-000200.apk`，并安装到 `SM-S9280` 核对为 `versionCode=18`、`versionName=4.2.0`。本轮 `connectedDebugAndroidTest` 因设备处于锁屏/Bouncer 状态未获得通过结果。本功能仍是手动规则层预设，不做自动场景识别，也不引入定位、联网或蓝牙权限。

### 离线测试素材回放

- 想法：加入一组本地测试图片或短片段回放流程，用于展示检测框、风险规则和提醒逻辑。
- 价值：演示时不完全依赖现场环境，老师可以稳定看到功能效果。
- 可落地内容：
  - 放入少量可公开使用的测试图片。
  - 增加 debug-only 回放入口。
  - 对每个素材记录期望检测和风险判断。
- 风险：视频或图片素材要注意授权和仓库大小；debug-only 能力不要误导真实用户。

### 【部分完成】提醒语音风格与多语言

- 想法：支持更自然的提醒语音风格，例如简短、详细、安静；未来可考虑中英文切换。
- 价值：助盲提醒对语气、长度和频率很敏感，成熟 App 应允许用户选择适合自己的反馈方式。
- 可落地内容：
  - 提醒短句模板集中管理。
  - 简短/详细两种提醒模式。
  - 设置页增加语音风格选择。
- 风险：提醒语不能过长，不能在高风险时分散注意力。
- 已完成范围：`v3.6.0` 已新增 `BRIEF`、`STANDARD`、`DETAILED` 三档语音风格，集中到 `SpeechStyle` 模板生成，并在设置页提供语音风格选择；标准风格保持原有短句，简短/详细风格分别减少打扰和补充目标类别。
- 剩余范围：尚未实现中英文切换、多语言资源拆分或按系统语言自动选择；后续如要多语言，需要补充 strings 资源和 TalkBack 文案验证。
- 实现版本：`v3.6.0` / `versionCode=16`。
- 完成时间：2026-05-18 21:29:31 +08:00。
- 验证证据：`FeedbackControllerTest` 已覆盖三种语音风格文案；完整 JVM 单测和 debug APK 构建已通过。

### 核心感知算法上位替代路线

- 想法：把当前“CameraX 实时帧 -> TFLite YOLO11n 目标检测 -> 检测框位置/面积/中心偏置规则 -> 风险稳定与反馈”的核心链路，逐步升级为“目标检测 + 深度/可通行区域/低频场景理解”的多层感知框架。目标不是简单换一个更大的 YOLO，而是让 App 从“识别到什么物体”进一步走向“前方是否可通行、哪些目标正在靠近、是否需要提醒”。
- 当前状态：2026-06-11 已完成“YOLO11n + 单目深度TFLite + 风险融合”的候选实验脚手架。新增 `DistanceEvidence`、`DepthEstimator`、`TfliteMonocularDepthEstimator` 和 `comparisonMode=DepthFusion` benchmark，可在 BlindAssist EvalSet 上比较纯几何距离规则与深度融合规则；该路线仍不进入默认 App runtime，不替换 `yolo11n`，也不估算真实米数。同日已下载官方 Depth Anything V2 Small 权重并通过前 20 张 EvalSet 的 PyTorch smoke；TFLite 导出仍阻塞在 `onnx2tf` 的 ViT `Reshape` 转换，尚未进入真机 A/B。
- 价值：当前风险层主要依赖检测框底部位置、面积比例和中心偏置推断相对距离，容易受物体真实尺寸、相机角度、遮挡和类别覆盖限制影响。引入深度估计、可通行区域分割或低频语义理解后，可以让助行提醒更接近真实风险判断，并为论文/答辩说明提供更强的算法演进路线。
- 可落地内容：
  - 短期可评估 `YOLOv10n`、`YOLO12n`、轻量 RT-DETR 等实时检测器，作为 `ObjectDetector` 的替代实现；重点比较 Android 端 TFLite/LiteRT 延迟、稳定性、输出解析复杂度和漏报率，而不是只看公开数据集 mAP。
  - 中期优先尝试“YOLO11n + 单目深度估计”融合，例如引入轻量 Depth Anything V2 或同类小模型，把 `RiskAnalyzer` 从框几何启发式升级为“目标类别 + 方向 + 相对深度/接近趋势”的风险评分。
  - 中期可探索可通行区域/地面分割，让系统判断正前方走廊、地面或道路区域是否被占用；它可以补充 YOLO 对未知障碍、低矮杂物和非 COCO 类别目标的盲区。
  - 长期可把 SAM 2/3、Grounding DINO、OWLv2 等开放词汇检测/分割模型作为离线标注、数据扩充和教师模型，不直接承诺端侧实时运行；通过它们生成训练样本，再蒸馏或微调更小的 Android 模型。
  - 长期可把 MobileVLM、Gemma 3n、Moondream 等小型视觉语言模型放在低频场景摘要位置，例如每几秒解释“前方是走廊/门口/人群/楼梯附近”，但不让它承担逐帧安全提醒主链路。
  - 如果目标机型支持 ARCore Depth 或稳定的视觉惯性能力，可探索高端设备增强模式，用深度图、光流或接近趋势辅助风险判断；同时保留纯相机 + TFLite 的基础模式。
- 推荐优先级：
  - 第一优先：检测 + 单目深度融合，直接解决当前“相对距离只能靠框大小猜”的核心短板。
  - 第二优先：轻量实时检测器横向评测，选择 Android 端真实延迟和漏报率更稳的模型。
  - 第三优先：可通行区域分割，用于判断正前方路径是否被占用。
  - 第四优先：SAM/开放词汇检测/VLM 作为离线标注、教师模型或低频语义解释，不直接放入实时避障闭环。
- 风险：算法升级必须保持助盲安全表述克制，不应把原型描述成可替代盲杖、导盲犬、人工判断或专业辅助设备；所有模型替换都需要重新记录模型来源、导出参数、输入输出 shape、端侧延迟、功耗、误报漏报和真机场景验证。VLM、开放词汇检测和大型分割模型存在延迟、功耗、幻觉或端侧部署成本，不适合作为当前实时安全提醒的唯一核心。
- 适合阶段：先作为研究路线和实验计划记录；真正落地时应先建立小型场景评测集和模型对比脚本，再修改 `ObjectDetector` 或 `RiskAnalyzer`，并同步更新 README、开发日志、模型检查脚本、测试结果和 APK 归档。

### 三阶段算法升级建议

- 阶段一：【部分完成】实时检测器横向升级。
  - 目标：不改变现有 CameraX/TFLite/风险稳定主架构，先把 `ObjectDetector` 做成可对比的模型入口，评估当前 YOLO11n 是否值得替换。
  - 候选：`YOLOv10n`、`YOLO12n`、轻量 RT-DETR，以及当前 YOLO11n 的不同输入尺寸、FP16/INT8 量化版本。
  - 验证重点：真机 FPS、单帧总耗时、模型大小、TFLite/LiteRT 部署难度、输出解析复杂度、误报率和漏报率。这个阶段的产物应优先是模型评测矩阵，而不是直接拍板替换模型。
  - 已完成范围：`2026-05-27` 已建立 detector lab，本地下载 COCO8 小型 smoke dataset，并把 `yolo26n`、`yolo12n`、`yolov10n` 导出为 320 FP16 TFLite；多模型 shape/dtype 检查和本机 `ai-edge-litert` benchmark 已跑通，默认 App 模型仍保持 YOLO11n。
  - 剩余范围：还需要补 BlindAssist 真实助行图片集、真机端性能采样、误报/漏报对照和默认模型替换决策；在这些完成前不应宣称候选模型已优于当前默认模型。
- 阶段二：检测 + 单目深度融合。
  - 目标：保留检测器负责“识别是什么”，新增轻量深度估计负责“离我大概多近”，把 `RiskAnalyzer` 从检测框几何启发式升级为“类别 + 方向 + 相对深度 + 接近趋势”的风险评分。
  - 候选：轻量 Depth Anything V2、小型 MiDaS/自蒸馏深度模型，或在目标机型支持时接入 ARCore Depth 作为增强模式。
  - 验证重点：前方近处障碍、远处大物体、近处小障碍、侧向经过、弱光和遮挡场景下的漏报/误报变化。该阶段最能体现 BlindAssist 从目标检测原型走向空间风险感知原型。
- 阶段三：可通行区域 + 低频场景理解。
  - 目标：在检测和深度稳定后，进一步判断“前方路径是否可走”，而不是只判断某个检测目标是否危险。
  - 组成：可通行区域/地面分割用于判断正前方走廊、道路或地面区域是否被占用；光流或接近趋势用于判断目标是否正在靠近；低频 VLM 场景摘要用于解释“前方像走廊、门口、人群、楼梯附近”等语义状态。
  - 边界：SAM、Grounding DINO、OWLv2 和 VLM 更适合作为离线标注、数据增强、教师模型或低频解释器，不应直接放入逐帧安全提醒闭环。
- 推荐顺序：先做检测器评测，降低模型替换风险；再主攻检测 + 深度融合，解决当前相对距离估计短板；最后补可通行区域和低频场景理解，把项目从目标检测 App 推向场景风险感知 App。
- 最值得优先投入的单点：`YOLO + 单目深度融合`。它对助盲避障的实际价值最大，也最容易解释为什么比当前核心算法更“上位”。

### 面向算法创新的连续帧风险感知主线

- 想法：把 BlindAssist 的算法创新重点从“替换一个更大的 YOLO 模型”转向“端侧目标检测 + 连续帧风险建模 + 可解释分级提醒”。当前默认链路已经能完成 `CameraX -> YOLO11n TFLite -> RiskAnalyzer -> 语音/震动/界面反馈`，后续更有价值的创新是让风险层理解目标是否位于行走通道、是否正在靠近、是否需要稳定提醒，而不是单纯追求公开数据集 mAP。
- 价值：该方向更贴合助盲任务，也更适合论文、答辩和专利表达。它可以把项目从“基于 YOLO 的识别 App”提升为“面向盲人出行的端侧实时风险感知与分级提醒算法”，重点体现任务定义、风险建模、评价指标和可解释性。
- 可落地内容：
  - 连续帧目标记忆：按类别、IoU、中心点距离或框位置相似度，为近几帧目标建立轻量轨迹，不追求复杂多目标跟踪，只服务于风险稳定判断。
  - 接近趋势判断：统计同一目标的框面积变化、框底部位置变化、中心通道偏移变化，判断目标是否正在进入正前方或靠近用户。
  - 可解释风险评分：在现有 `RiskAnalyzer` 阈值基础上增加 `riskScore`，由类别危险权重、方向权重、距离层级权重、中心通道权重、连续帧接近权重组成，并输出每项贡献，便于调参和论文说明。
  - 风险稳定策略升级：保留当前中风险多帧确认和高风险快速提醒逻辑，同时把“连续靠近”作为升级依据，把“单帧抖动”作为降噪依据，减少误提醒和闪烁式提醒。
  - 专用评价指标强化：继续以 `centerRiskRecall`、`alertRecall`、`alertFalsePositiveRate`、`criticalMissCount`、`distanceBandAccuracy`、`riskLevelAccuracy`、P95 延迟为主，不把速度或 COCO AP 作为唯一决策标准。
  - 数据集补强：在现有 BlindAssist EvalSet 之外，补充真实短视频抽帧或连续帧片段，覆盖正前方近距离目标、侧向经过目标、低矮障碍物、室内走廊、弱光遮挡和人群场景。
- 推荐实现顺序：
  1. 在 `core:assist` 增加轻量 `RiskTrack` / `RiskTrend` 领域模型，先用纯 Kotlin 单元测试验证连续帧趋势，不急着改 CameraX runtime。
  2. 给 `RiskAnalyzer` 输出可解释 `riskScore` 和评分分项，同时保持现有 `RiskLevel`、`RiskDirection`、`ProximityBand` 对外契约不变。
  3. 在 `AssistEngine` 或会话协调层接入连续帧风险增强，让同一目标的接近趋势影响提醒升级和稳定。
  4. 扩展 benchmark artifact，在 `per-image.csv` 或连续帧评测输出中记录风险分项、趋势判断、升级原因和抑制原因。
  5. 用同设备、同数据、同指标对比“当前几何规则 baseline”和“连续帧风险增强 candidate”，只有在 critical miss 不增加、误报不明显上升、P95 延迟可接受时才考虑推广。
- 与模型替换路线的关系：`yolo26n`、`YOLO12n/12s`、微调 `yolo11n` 仍可作为候选检测器，但它们必须服务于助盲风险指标。默认模型不应因为速度略快或普通 AP 略好就替换；候选必须在 `centerRiskRecall`、`criticalMissCount`、`alertFalsePositiveRate` 和延迟上通过同设备门槛。
- 与 DepthFusion 路线的关系：单目深度融合保留为实验支线。已有 MiDaS / FastDepth 类候选说明链路可以跑通，但误报、距离准确率和延迟仍可能退化；在没有可靠候选通过门槛前，主线应优先做连续帧几何风险建模和可解释评分。
- 风险：连续帧算法会引入状态管理、目标匹配错误和提醒延迟问题，必须保持“宁可清楚承认原型边界，也不夸大安全能力”的表述。所有结论应以 BlindAssist 专用评测集、真实设备延迟和人工复核为依据，不宣称可替代盲杖、导盲犬或人工判断。
- 适合阶段：作为下一轮算法创新主线和论文/答辩技术贡献储备；真正落地时需要同步更新 `README.md`、`DEVELOPMENT_LOG.md`、相关单元测试、benchmark 文档和本地验证证据。

### 完整双向音视频眼镜终端路线

- 想法：把最终形态定义为“手机 App 作为本地 AI 中枢，眼镜作为低延迟音视频终端”。眼镜负责佩戴式摄像头、麦克风、扬声器/骨传导耳机、按钮、IMU、电池和状态反馈；手机负责接收眼镜视频帧、本地 TFLite/YOLO 推理、风险融合、语音/震动/眼镜音频反馈策略、UI 设置、日志和测试证据。
- 路线判断：不建议把旧 ESP32-S3/STM32 模块继续硬改成完整音视频主控。旧模块适合保留为低功耗控制协处理器，处理按钮、IMU、电池、震动、状态灯、简单蜂鸣和 BLE 唤醒；视频编码、音频编解码、断连恢复和实时传输更适合交给 UVC 有线链路或小型 Linux/AIoT 模块。
- 推荐阶段：
  1. 阶段一做 USB-C 有线完整样机：用 UVC 摄像头和 USB 音频/串口/HID 接入手机，优先证明“眼镜采集 -> 手机推理 -> 眼镜反馈”的全链路，降低 Wi-Fi、配网、丢包和功耗调试成本。
  2. 阶段二做无线眼镜终端：用 Wi-Fi Direct、眼镜热点或局域网连接承载视频/音频主链路，用 BLE 承载配对、唤醒、电量、按钮和低频控制。视频优先考虑 H.264/RTSP/WebRTC，音频优先考虑 Opus/PCM；控制通道可用 WebRTC DataChannel、WebSocket 或轻量 UDP。
  3. 阶段三再追求可佩戴体验：优化电池、散热、重量、镜架结构、骨传导输出、离线降级和长时间稳定性，而不是一开始就把所有能力塞进 MCU。
- Android 架构落点：
  - 新增 `GlassesFrameSource`，与现有 `CameraXFrameSource` 并列，让运行时可以在手机摄像头、USB 眼镜摄像头和无线眼镜摄像头之间切换。
  - 新增 `GlassesConnectionRepository` / `GlassesDeviceState` / `GlassesControlChannel`，把连接状态、电量、固件版本、摄像头来源、音频链路和错误原因从 UI 中分离出来。
  - 扩展 `FeedbackGateway` 或新增眼镜反馈适配器，把现有语音/震动提醒映射为眼镜端蜂鸣、震动、骨传导播报或左右声道提示。
  - 保留无外设降级路径：眼镜断连时自动回到手机 CameraX 和手机语音/震动，不让助盲主链路依赖单一外设。
- 旧眼镜模块复用方式：保留 ESP32/STM32 的硬件经验和固件资产，但重新定位为“控制板/反馈板/实验外设”。短期可以复用 I2S 麦克风、扬声器、摄像头 MJPEG 和 TCP PCM 协议做验证；长期应逐步替换为更适合实时音视频编码和传输的硬件主链路。
- 协议边界：
  - BLE：配对、唤醒、电量、按钮、模式、震动和低频控制。
  - USB 或 Wi-Fi：视频上行、麦克风上行、TTS/提醒音频下行。
  - 控制消息：`ALERT level/direction/distance/pattern`、`SEARCH direction/distance/beep_interval`、`STOP`、`PING`、`BATTERY`、`FRAME_SOURCE`、`AUDIO_ROUTE` 等小消息优先结构化，不直接混在视频流里。
- 价值：这条路线能把第一版电脑中枢项目里的“眼镜硬件闭环”和当前 Android App 的“本地推理闭环”接起来，同时避免把算力、网络、UI 和安全策略都堆到眼镜端。答辩、论文或后续产品说明中也更容易表达为“端侧手机 AI 中枢 + 佩戴式音视频终端”的系统架构演进。
- 风险：无线完整音视频眼镜会显著增加权限、隐私、配网、延迟、功耗、散热、断线恢复和真机测试复杂度；所有阶段都必须保持助盲安全边界，不宣称可替代盲杖、导盲犬或人工判断。接入真实硬件后需要同步更新 `README.md`、`DEVELOPMENT_LOG.md`、权限说明、设备回归脚本、真机延迟指标和 APK 归档记录。
- 适合阶段：先作为中长期产品形态和硬件路线储备；真正实现时建议先落地 USB-C 有线样机，再扩展无线协议和可佩戴工程。

## 历史研究决策记录（不作为当前真源）

### 【进行中】公开视频银标 r7.2：因果事件退出原型修复当前 post-event clear，跨来源验证仍未闭合

- 隔离边界：`secondary-corridor-causal` 是另一条独立测试验证方向。本主线只参考其方法层启发，不读取其数据、标签、权重、指标或实验产物，不修改其代码，也不共享输出目录。r4–r7 构建器和相关机制/深度诊断对包含该目录名的输入输出 fail closed。
- 数据扩展：从 Wikimedia Commons 上 POPtravel 的 CC BY 3.0 第一视角曼谷步行视频中，按来源页、作者、许可、YouTubeReviewBot 许可确认时间和原视频链接绑定来源合同；下载的 240p 转码 SHA256 为 `8f0efe24eddd939e8396abc60cfa35789003e9a3b9f115b9538182d0060e6a17`。仅提取预注册时间点，不把公开视频或 GPT/VLM 判断称为人工真值。
- 新增反事实：
  - 动态：车道口近场清空 `no-alert` → 面包车在近场横穿并遮挡通路 `alert`。
  - 静态：砂堆占据中心人行通道 `alert` → 绕过砂堆后近场恢复清空 `no-alert`。
- r7 共有 19 条非弃权 episode、7 组 matched pair。置信度门下，动态机制达到 3 对/3 来源，静态机制达到 3 对/3 来源，机制覆盖门首次通过。
- 根因结论：覆盖门通过后，冻结对象轨迹 balanced accuracy 为 `.7389`，但五组 prototype/bootstrap 中位数降至 `.6833`、仅 `2/5` 达标；弱监督风险轮廓/生命周期 MIL 中位数 `.7222`，pair 排序中位数 `.7143`。因此新增数据没有证明 head 稳定，反而暴露了更真实的表示缺口。
- 表示三角验证：
  - 冻结 COCO 对象轨迹：`.7389`，总体线性门通过，但新来源的车道口清空被误报。
  - 冻结 DINO/Depth pooled feature：`.3611`，把新来源两个 alert 全判为 no-alert。
  - 显式相对深度走廊轮廓：`.5222`，仍不可分且 prototype 方向不一致。
- 具体失败：
  - 近场横穿面包车只有 8 个检测、7 条短轨迹、1 条持续轨迹，COCO proposal 没有稳定表达“正在封闭近场”。
  - 砂堆正例与绕过后的负例均为 14 个检测、6 条轨迹、5 条持续轨迹，目标轨迹特征几乎看不到非 COCO 的地表/静态占道变化。
  - 隔离车道口清空负例后，轨迹 probe balanced accuracy 从 `.7389` 升至 `.8889`，增量约 `+.15`；这只能路由到表示/语义复核，不能据此删除或翻转标签。
- 已测试的后续表示：独立实现了冻结 `free-space topology` probe，只从当前主线 SANPO 分割 logits 计算自适应路径、可走宽度、瓶颈、路径偏移和四类非可走概率，再与对象轨迹拼接。拓扑单独 balanced accuracy `.5222`；融合后只有 `.6278`，低于轨迹基线 `.7389`；pair 增量平均余弦还从 `.0349` 降到 `-.0272`。四条 Wikimedia episode 在 walkable `.50` 阈值下的最小路径宽度全部为 `0`，包括明确的绕过砂堆后清空段，说明当前分割 logits 本身没有可用的路径拓扑校准。停止该分支，不调阈值、不搜索拓扑超参数、不接入 head。
- 运动补偿占用诊断：新增 homography 注册后的下方走廊残差与可靠性通道。motion-only balanced accuracy `.5167`，与轨迹融合为 `.6833`；压缩残差与轨迹融合虽回到 `.7389`，但预测与轨迹基线完全相同。该通道揭示“面包车横穿时注册失败、静态占道 pair 的 alert 残差范围高于 no-alert”，却没有形成独立可泛化判别能力，因此停止继续调光流/注册阈值。
- 机制时间范围审计：动态和静态 pair 的 source 内风险顺序都达到 `3/3`；动态留出端点 `6/6` 正确，静态只有 `4/6`。信号存在，但静态机制不能共享一个绝对阈值，必须依赖 source-relative 的接近/离开基线和事件生命周期结构。
- head 修复：在 MIL 中加入首个可靠帧的时序基线变化和完整 pair 的 ranking loss 后，五组 balanced accuracy 为 `.8389/.7333/.7389/.6833/.8389`，中位数 `.7389`、`4/5` 过 `.70`，pair 排序中位数 `.8571`。这证明 head 组织确实是第二个问题，但最差 seed 和静态 post-event 仍未闭合。
- 公开视频继续扫描但不强行造 r8：Novi Sad 的柱体候选被行人、卡车和转向/路线变化混杂；Trubarjeva roadworks 视频主要为固定机位且有白色面包车横穿。二者都被记录为拒绝候选，没有进入训练或评测。
- 受控合成反事实仅作训练增强：从 3 个真实 no-alert 父 episode 复制完全一致的 clear 帧，再确定性叠加逐帧增大的路障或砂堆，生成 3 对、6 条 episode、18 张图，其中 9 张正例带 alpha-derived mask/bbox，9 张为原图级 clear 负例。全部 18 张经 contact sheet 视觉 QA 接受；所有包均标记 `train_only + provisional`，不计入真实评测，并在父真实 source 被留出时自动排除对应合成 pair，防止 source 泄漏。
- 当前最佳候选：`时序基线 + pairwise ranking + 仅训练合成静态反事实` 的五组真实 episode balanced accuracy 为 `.7944/.7944/.8944/.7944/.7444`，中位数 `.7944`、最差 `.7444`、`5/5` 过 `.70`；相对无合成候选，中位数从 `.7389` 提升到 `.7944`，最差从 `.6833` 提升到 `.7444`。pair 排序中位数保持 `.8571`。这支持“受控反事实能稳定 head”，但不证明合成场景代表真实世界。
- 外观统计负实验：追加 lower-corridor LAB/灰度/饱和度/边缘/纹理统计后，中位数 `.7889`、最差 `.6889`，弱于最佳候选；停止外观通道和阈值搜索。
- 残留硬问题：曼谷砂堆 alert 在 5 个 seed 全部正确，但“绕过砂堆后的 clear”仍有 `4/5` 被误报；这是障碍生命周期结束/安全基线恢复的语义问题，不是继续堆优化器能解决。当前候选不授权校准、blind 结论、保存部署权重或替换生产模型。
- 严格 terminal pooling 负实验：把 episode 监督和 pair ranking 全改为只看最后一个因果帧，五组 balanced accuracy 降到 `.6278/.6333/.7833/.6833/.6333`，中位数 `.6333`，pair 排序中位数 `.7143`。它偶尔能释放陈旧峰值，却会丢失动态接近事件，不能替代 smooth-max。
- 合成 mask teacher 负实验：用 exact clear/composite 同位置 patch 和 alpha mask 训练冻结 DINO 稠密静态障碍 teacher，并在父 source 留出时排除对应合成记录。真实 LOSO balanced accuracy 只有 `.5778`，且把真实砂堆/清空方向判反，说明合成素材的视觉域差距仍太大；不接入 MIL。
- prompt-free 静态语义：下载并冻结 YOLOE-11s prompt-free 模型，使用其内置 4585 类，不输入文本提示、银标、source mask 或合成图。模型在砂堆最接近帧以 `.5845` 检出 `sand box`，三张 post-event clear 均无 surface-material 检测。静态语义单独 balanced accuracy `.6111`；与轨迹直接拼接只有 `.5778`，所以“全局粗暴融合”失败，但 held-out Bangkok 四条 episode 恰好全部判对。
- 因果事件退出路由：只有同时满足“同 source 紧邻前段有 surface-material 检测、当前段完全消失、时间间隔不超过 5 秒、source-isolated COCO 轨迹判定当前无动态危险”时，才关闭上一静态事件并覆盖陈旧 MIL alert。路由不读取当前标签、没有可学习参数，也不包含 episode ID 特例。它只识别出砂堆后 2 秒的 clear 一条候选。
- r7.2 五组结果：balanced accuracy 从 `.7944/.7944/.8944/.7944/.7444` 提升为 `.8444/.8444/.8944/.8444/.7944`，中位数 `.8444`、最差 `.7944`；五组均不退化。post-event clear 从 `1/5` 正确变为 `5/5` 正确，no-alert recall 在四个受影响 seed 各提高 `.10`，alert recall 保持 `.8889`。把时间窗收紧为 1 秒后候选消失、结果完全退回基线，负控证明提升依赖真实的 2 秒因果连续性。
- 当前判定：已解决这一个已知 post-event clear 失败，并形成比“只看末帧”更合理的事件生命周期结构；但证据只包含 1 个合格 surface-material exit source，仍不足以宣称通用事件终止能力或进入 App/生产。
- 下一步：继续从独立公开视频来源采集至少 2–3 组 `static semantic present → absent → dynamic clear` 连续事件，预注册同一 5 秒/轨迹 guard 后做整 source 留出。只有跨来源 exit precision/recall 和事件级假提醒门同时通过，才把该路由映射到 `RiskEventTracker`；SAM/ASAM 仍不优先。`secondary-corridor-causal` 继续保持实现、数据、输出和实验编号完全隔离。

### 【进行中】公开视频银标 r7.3：跨来源退出发现失败，转向 VLM 时间窗审阅

- 新增 4 条许可明确的 Commons 连续步行视频：Greenwich、上海陕西南路、哈尔滨中央大街和 Worms，总计约 416 MiB、112 分钟。5 秒采样得到 1,346 帧；320/0.01 的 prompt-free YOLOE 共提出 92 条退出候选，其中 6 条 surface、86 条 barrier。
- GPT 多帧 contact-sheet 复核拒绝全部 6 条 surface 候选；最高分 barrier 也都是建筑、围栏、固定入口、船体、普通路口车辆、楼梯或人群，没有干净的 `static risk present → absent → dynamic clear`。上海 990–1050 秒的停放电动车段可作为“狭窄但仍可通过”的 hard negative 候选，但没有同源正例，暂不训练。
- 为检测闪烁增加连续缺失持久化：5 秒采样下要求 10 秒连续缺失，候选从 92 降至 77；要求 20 秒后降至 62，但 6 条 surface 错检完全不变。结论是持久化能去掉 barrier 闪烁，却不能修复 surface 语义错检。
- 用更接近正式语义 probe 的 640 输入、`.05` 置信度和 10 秒连续缺失重扫同 1,346 帧，候选降至 51（3 surface、48 barrier）。剩余 3 条 surface 和每来源最高分 2 条 barrier 仍全部经 GPT 视觉复核拒绝。继续扩大 YOLOE 扫描量的边际收益已不足，停止该分支。
- 近场走廊 box 几何门作为负实验保留：它把真实施工边界检出一起过滤，只留下低置信噪声，不能用于路由。发现扫描必须记录 `image_size`、Ultralytics、Torch 和 OpenCV 版本；仅冻结权重不足以描述完整推理合同。
- 新增一条许可明确的 Hof 公交后窗施工视频（CC BY 4.0），将原始 10–40 秒实拍帧确定性时间反转，得到 301 帧退出反事实。原始相邻缺失产生 3 条候选；要求 10 个连续一秒缺失后只剩 `18s → 19s`，与 GPT 判断的约 19–21 秒视觉清除窗口一致。该素材仅作为 exit-persistence 回归，不进入真实评测分母或训练，因为视角不是行人第一视角且时间方向为合成。
- YouTube Creative Commons 搜索已确认可发现更贴题的公开视频，但当前出口被 YouTube 人机登录拦截；没有读取浏览器 Cookie，也没有绕过认证。Piped/Invidious 公开实例同样不可用，因此本轮只使用 Commons 直链和许可页。
- 决策：r7.2 的单来源 router 指标保持不变，不修改 Android `RiskEventTracker`。下一步改成“大模型直接审阅许可视频的粗时间窗/overview，再对少量边界做冻结模型复核”，而不是让 YOLOE 独自搜索全部视频；反事实 Hof 挑战作为时序持久化回归，公开视频/GPT 结果继续保持 provisional、非校准、非 blind、非生产。

### 【进行中】公开视频银标 r7.4：真实 Hof 退出支持“风险轮廓 + 生命周期”

- VLM 先审阅找到新的 Hof 公交车窗施工短片（作者自发布 CC BY 4.0）。原始时序中 `0–6s` 可见裸土、碎石、机械和围栏，`7s` 起施工区离开画面，`8–10s` 稳定为普通铺装街道和开放广场。视频 SHA256 `1d9dff54e8ff89c4f40b66f29818eddca5948c564ef64c2f92a02d09efcc9e4c`。
- 冻结 YOLOE-11s prompt-free 在 `640/.05/1s` 下，`6s` 最后检出 `construction site` `.5029`，`7–10s` 连续 4 个样本缺失；GPT 多帧审阅以 `.92` 置信度接受 `6s → 7s` 为 discovery-only 退出边界。
- 该来源暴露了单 `surface_material` 规则的结构性风险：`1s` 短暂检出 `sand bar`，`2s` 表面类消失，但施工环境仍持续到 `6s`。因此“表面类消失”不等于“风险结束”。
- 隔离的外部 challenge 要求至少 3 个连续缺失样本：仅 surface 在 `1s → 2s` 过早退出而失败；仅 barrier 和 `surface + barrier` 风险轮廓并集都在 `6s → 7s` 通过。
- 新增主线隔离 `risk_profile_union` 回放，不修改 r7.2 v1 router。19 条真实 episode 上仍只产生曼谷砂堆后 clear 一条候选；五组 balanced accuracy 保持 `.8444/.8444/.8944/.8444/.7944`，中位数 `.8444`、最差 `.7944`。1 秒 gap 负控仍无候选并精确回退基线。
- 结论：“风险轮廓并集 + 连续缺失 + 动态轨迹清空”比单 surface 规则更稳健，且不伤害 r7 结果。但 Hof 是侧向公交车窗视角，只支持机制原型，不进入训练、校准、blind 或生产。Android `RiskEventTracker` 仍不修改；下一个关键证据是行人第一视角的第二/第三个真实退出来源。

### 【进行中】公开视频银标 r7.5：许可作用域与连续性门成为当前主瓶颈

- 新审阅两条 Commons Ljubljana 工地视频和一条 Vimeo Gympie 工地巡场视频。前两条是固定/摇摄工地声景，后一条混入航拍、人物、机械、图纸和片尾的硬剪辑蒙太奇；三条都不能建立“行人连续离开风险区”的因果时间线，已写入哈希绑定拒绝清单，未进入扫描、评测或训练。
- 把公开视频发现 registry 扩展为平台无关 v2，同时保留 Commons v1 向后兼容；新增 Vimeo 官方 CC-BY 单页候选台账工具。每次在线运行只请求一页、不分页、不登录，候选默认 `training_eligible=false`，且搜索页 CC-BY 过滤仍必须再做视频条目级许可确认。
- Vimeo 严格搜索只得到一条占星/出行延误短片。其条目元数据虽报告 CC BY，但 `walking`、`roadwork` 只出现在描述举例中，属于关键词碰撞，下载前拒绝。
- 登录态 YouTube 找到两条高语义匹配候选：Addis Ababa 施工走廊 22 分 50 秒连续步行片段因视频许可未闭合而 `hold_no_download`；另一条 1 分 30 秒工地旁步行片段展开描述后确认视频本身声明版权，CC BY 3.0 只覆盖背景音乐，因此按“许可作用域假阳性”拒绝。
- 当前根因进一步收敛：不是 head 优化器，也不是缺少更多无约束视频，而是缺少同时满足“视频级可复用许可 + 连续行人视角 + 风险存在到稳定清空”的独立来源。新增合格行人退出来源仍为 `0/2`，训练入口、Android runtime 和 `RiskEventTracker` 修改继续关闭；SAM/ASAM 不前移。
- 后续恢复网络采集时，先用 `CC BY/CC0 video footage` 做条目级许可筛选，再做 overview/VLM 连续性审阅，最后才运行冻结风险轮廓、持久化和轨迹清空挑战。音乐 CC、搜索页 CC、普通 YouTube 可观看性都不得替代视频许可。

### 【负实验】公开视频银标 r7.6：Hof 冻结 DINO 方向不能替代新来源采集

- 以 Hof 原始时序中的 `0/3/6s` 风险帧和 `8/9/10s` 清空帧构造零训练参数的冻结 DINO-S 方向；按 5 秒间隔扫描 Greenwich、上海、哈尔滨、Worms 四条连续视频，共 1,346 帧。原始投影跨来源不校准，仅使用来源内 robust-z 的“前高后低”持续下降做候选排序。
- 对 12 个最高分窗口按 2 秒间隔生成多帧接触表并逐一复核，接受 `0/12`。误报机制集中在室内外切换、近景商店/雕塑/动物、砖石与岩壁纹理、固定门洞/花园入口和镜头转向；没有一个窗口形成连续的“施工或障碍风险 -> 稳定净空”因果时间线。
- 因通过复核的来源为 `0/2`，没有运行后续风险轮廓、持续性或轨迹清空挑战，也没有产生训练样本或模型权重。审阅报告 SHA256 `05b5fe9637598144fce86a7fb5fb839baccf8714e4f8520ff1d33f6908d05112`。
- 结论：冻结 Hof 方向学到的是大量场景域、物体距离和材质相似性，不是可跨来源复用的施工退出语义。不要把 DINO 与 YOLOE 失败候选做事后权重调参或并集“救分”；当前主瓶颈仍是具备条目级许可和因果连续性的真实行人来源，而不是 head 优化。训练入口、Android runtime、`RiskEventTracker` 与 SAM/ASAM 顺位均保持不变。

### 【负实验】公开视频银标 r7.7：Fremantle 揭示“未检出不等于已净空”

- 本地已有的 Fremantle CC BY 2.5 AU 原时序视频提供约四分钟连续车载道路施工记录。GPT 密集多帧复核把施工锥桶通道的视觉退出定在 `178s → 179s`，`179–195s` 为稳定普通道路；它是第二个独立外部机制来源，但仍不是行人第一视角或 should-alert 真值。
- 冻结 YOLOE-11s 在 1 秒、640、`.05` 下扫描 262 帧，barrier-only 与风险轮廓并集都产生 10 个退出候选：相对视觉边界 8 个过早、2 个过晚、0 个精确命中。surface-only 没有候选。
- 风险存在窗口 `150–178s` 共 29 帧，barrier 只在 2 帧激活，覆盖率 `.0690`，最长连续漏检 20 秒；稳定净空窗口 `179–195s` 又有 1 帧假激活。三帧连续缺失、十帧连续缺失甚至简单超时都不能区分“风险仍在但语义闪烁”和“真正净空”。挑战报告 SHA256 `8e37b59d75ca2c2985c06e4cd826491bebb59eaa0d4fc05415744953b277a7a4`。
- 设计结论：生命周期头不能再把检测缺失当作清空证据。下一版应显式维护 `risk_present / uncertain / clear` 状态，入口由正风险证据触发，退出必须由独立的净空/可通行证据支持；只有检测空窗时保持 uncertain 或衰减置信度。没有这种双向证据，延长 persistence 只会把 `182s` 的净空段假阳性继续拖成陈旧风险。

### 【探索性通过】公开视频银标 r7.8：施工标志词表 + 三态生命周期

- 冻结 YOLOE 内置词表实际包含 `barricade / cone / construction worker / traffic cone`，旧的预注册 barrier 子集未使用这些直接施工标志。新增默认关闭的探索开关，仅把这四类加到 barrier，不改既有 r7/r7.4 基线，也不加入文本 prompt 或训练参数。
- Fremantle 风险段覆盖从 `2/29=.0690` 提升到 `23/29=.7931`，最长漏检从 20 秒降到 2 秒；原始退出候选从 10 个降到 3 个。它仍未精确命中视觉边界，并在 `182s/239s` 保留两个单帧晚假阳性，所以单纯扩词表还不能直接作为退出规则。
- 新增零学习参数三态状态机：最近 3 帧至少 2 帧有风险才从 clear 进入 present；缺失先进入 uncertain；连续 3 帧缺失才确认 clear；clear 后单帧命中不能重开事件。Fremantle 只形成一个事件：`9s` 进入、`177s` 最后风险证据、`178s` 首次缺失、`180s` 确认净空，不确定区间覆盖 GPT `178→179s` 参考，且两个单帧晚假阳性未重开。报告 SHA256 `302152ec2cb3ab2287bec387fdbc064a447f0fe5987a3d14f3694b2d56ce5744`。
- 不调参回放原 Hof baseline scan，同样只形成一个事件：`0s` 进入、`6s` 最后风险、`7s` 首次缺失、`9s` 确认净空，覆盖既有 `6→7s` 参考。报告 SHA256 `3f73e28d97278d68533ed434ec077939b3b8dc2a405c21c234e66e3a2a9de37e`。
- 这为“风险轮廓 + 生命周期头”提供了首个跨两个真实车辆视角来源的机制原型，但 marker 词表是在 Fremantle 诊断后探索加入，两个来源也都不是行人第一视角。因此它不能关闭 `0/2` 行人门禁、不能进入训练/校准/Android；下一条新来源必须在打开画面前冻结该词表与 `2-of-3 / 3-clear` 参数，作为真正的前瞻验证。

### 【前瞻失败】公开视频银标 r7.9：通用 work-zone marker 会开启普通场景假事件

- 在打开新控制窗口前冻结完整 marker 集合、1 秒/640/`.05`、`2-of-3 / 3-clear` 和覆盖率门槛，合同 SHA256 `48a8319f61bb58f9e319460e5dbb655340f8729de06871718cb258550cce5fa1`。
- 四个已知 nuisance 窗口中 Harbin/Worms 通过，但 Greenwich 固定门栏与 Shanghai 普通街景分别被 `traffic cone / barricade / construction site` 连续误激活，只有 `2/4` 通过。失败报告 SHA256 `b4ce3f45ec2791924492df35c0402dd5325d888a08ed733cd816fc4e8b97a038`。
- 单类 leave-one-out 没有任何变体同时保住 Fremantle 并通过四个控制；近场走廊几何把 Fremantle 风险覆盖压到 `.241`，仍未消除两条假事件。因此不能靠删一个词或加通用 box 位置门修复，r7.9 保持失败。

### 【窄专家】公开视频银标 r7.10：多锥桶走廊可用，但不能覆盖稀疏施工桩

- 逐帧计数显示 Fremantle 风险段常有 `2–20` 个 `traffic cone`，而 r7.9 四个普通场景每帧最多 `1` 个。固定测试阈值 `1/2/3/4` 后，只有 `>=2` 同时通过 Fremantle 与四个旧控制；审计 SHA256 `517b17d00432c35b3f742f7aa752202f9e5adcdb27f01e5dc390fe0d32467ad7`。
- 冻结 r7.10 多锥桶合同 SHA256 `109521ccc692d5f711ebcabf2ed0f26b4694f6c42773f72e13921235fd29c35d` 后，另四个未参与阈值选择的 nuisance 窗口 `4/4` 通过；报告 SHA256 `d81541eeefc43997ce26def42761364e66455f01e3387fec9182db19cd0ec348`。
- 但另一条 Hof 原时序视频在稀疏红白施工桩风险段只有 `2/18=.111` 帧满足多锥桶条件，状态始终 clear，报告 SHA256 `3335a4e466c3e9b679c9406f64dc54aad9cc20a7408eacd09fd7926dea5a9c79`。所以它只能是 dense multi-cone 专家，不能被包装成通用施工风险模型。

### 【前瞻负控通过】公开视频银标 r7.11：彩色施工标志专家

- 对真实/误检 detection box 提取颜色与几何：Fremantle/Hof 真实标志的高饱和像素中位数约 `.133/.140`、暗像素 `.070/.048`；Greenwich 黑色固定门栏误检为 `.042/.259`。面积、位置和长宽比不能稳定分离，使用无绝对阈值的物理规则 `high_saturation_fraction > dark_fraction`。
- 同时读取冻结内置类 `traffic cone + barricade` 后，`clear_absent=5` 是 `3/4/5/6` 中最小的充分值：Fremantle 形成唯一退出区间 `last_active=177s / first_absent=178s / confirmed_clear=182s`；Greenwich/Shanghai 固定设施不开户；Hof 稀疏施工段 `22s` 开启，终态 uncertain 但 open event 未解除。审计 SHA256 `1ace0742e75c63df14e34c60aef66e253c4c38d07c6a53d807b763f3d61f6065`。
- 冻结 r7.11 合同 SHA256 `3e6e6f410ce03053f4e8d6e38475156f69cf6a41352d8619541a7c8ae526bca7` 后，在第四批未参与颜色/持久性选择的 4 个 nuisance 窗口上扫描 190 帧。虽然原始模型产生 35 个目标检测，彩色标志 + 三态生命周期仍 `4/4` 零事件、终态 clear；报告 SHA256 `c0c7dde0cdf0545a45d9a36fce2a971afe79ad02c0b7a931b87371e133bb746f`。
- 当前最可信的结构不是“一个大 barrier 类”，而是机制路由：dense multi-cone 与 chromatic sparse-marker 专家分别产生正风险证据，生命周期只在证据短空窗时进入 uncertain，绝不把 absent 当 clear。r7.11 仍缺少冻结合同后的独立真实正退出来源，且现有正控制均为车辆视角，所以不进入训练、Android `RiskEventTracker`、校准或生产。

### 【来源门禁】公开视频银标 r7.12：按视频 SHA 去重后仍无独立正退出来源

- 对本地 7 份来源注册表做全集核对并按视频 SHA256 去重，共得到 14 个真实不同的视频文件；注册表缺项、同文件多别名、视频哈希漂移或 r7.8/r7.10/r7.11 派生污染都会 fail closed。库存合同 SHA256 `2110df3b8973a68efa7f466cb2ffe2d7f2f8a4c60c281339b11054fa62af7090`。
- 大模型复核全部时间概览和既有候选：Moira/Ljubljana/Anasskoko 为固定或平移机位，Duisburg/Vimeo 有硬剪辑，四条长步行视频没有施工离场，Fremantle/Hof 正例已参与既有合同派生，Hof 长片只提供持续风险进入而非独立退出。
- 严格门禁结果为 `14` 个唯一来源、冻结后独立正退出 `0`、行人第一视角正退出 `0`；审计 SHA256 `9070787e41d701129ec56597a09cd78c0dce834e48ce9efeefaaab7fc99bb370`。因此当前问题是来源级正证据缺失，不是数据文件数量、别名或 head 优化；训练和 Android 继续关闭。

### 【自动验收】公开视频银标 r7.13：新正来源必须一次通过冻结合同

- 新增 `evaluate_public_video_chromatic_marker_prospective_positive.py`，同时绑定 r7.11 合同、冻结 feature report、r7.12 来源审计和大模型多帧复核的 sidecar SHA；任何合同漂移、替换特征报告、视频 SHA 不一致、来源参与过派生或硬剪辑都会 fail closed。
- 正退出必须同时满足：来源谱系合格、大模型接受原时序连续退出、恰好一个事件区间覆盖视觉边界、风险窗口激活率 `>=.4`、稳定净空假激活率 `<=.1`、终态 clear 且无 open event。行人视角单独记录，机制通过不能冒充行人门通过。
- 固定复核模板 SHA256 `07d872826ff68231e327c231dd52de2002b34b16256fc6bf45255673a0e355bd`；8 个离线测试覆盖合格退出、派生污染、边界错位、净空假激活、合同错配、报告替换和硬剪辑。模板和测试不创造正样本，当前真实门仍为 `0/1`。

### 【离线可行性】公开视频银标 r7.14：pair-relative 生命周期变化 probe

- 新增 `run_public_silver_pair_relative_lifecycle_probe.py`，只读取既有机制覆盖报告、冻结 temporal-range 报告和 SHA 绑定 episode 帧，不重新训练 head，也不使用全局绝对阈值。时间先后只取 source manifest 的整数 `frame_index`；每组必须恰好两条、同 source、标签一正一负且时间不重叠。
- 预测合同固定为 `sign(later_score - earlier_score)`：正值开事件、负值关事件、零值弃权。动态使用冻结对象走廊占用范围，静态使用运动补偿下方走廊残差范围；可训练参数为 `0`。
- 六组合格 matched pair 全部方向正确：`5` 次 `clear→risk` 开事件、`1` 次 `risk→clear` 关事件，transition accuracy `6/6`。已知的 Bangkok 砂堆后净空从 `0.0301014` 降至 `0.0288612`，正确触发关事件。
- 但唯一关事件的绝对差只有 `.0012401`，归一化 margin 约 `.0412`；在事后仅作诊断的 `>5%` margin 压力线上会弃权，剩余 `5/5` 方向仍正确。因此 r7.14 证明“相对变化 + 生命周期状态”比统一绝对分类更符合现有 pair，却还没有证明关事件抗扰动、任意帧冷启动或实时流式泛化。
- 最终报告 SHA256 `421e2d5be2c7ca81991ebe36ccadc0033c4503515acb828345fbb0c63a51ec68`。它要求近期可信参考状态，只是 GPT/VLM 暂定同源 pair 的离线可行性，不是人工真值、校准、blind、Android 集成或生产授权。独立正行人退出来源仍为 `0`，`RiskEventTracker` 不修改；下一步应优先补至少一个新的 risk→clear pair 或前瞻正来源，验证关事件 margin 和连续多窗确认，再考虑生命周期 head 原型。

### 【补充压力样本】公开视频银标 r7.15：SK1 动态风险后的未用净空帧

- 在不改既有训练包和标签的前提下，检查六个 matched source 的未用 source-manifest 帧。只有 SK1 在风险 episode 之后保留了连续 frame `8–9`；大模型先按原时序审阅并冻结判断：frame `5–7` 的近场中央行人风险在 frame `8–9` 已离开，中央近场恢复净空。review SHA256 `d620eb14494751ca61e9e97f591857e36e21a01c9cf6e33b5adbe3372dc15452`。
- 使用与 r7 时间范围报告相同的冻结 YOLO12n（SHA256 `419ff3dca37d69bacc93a50fa0c186a1c6f9fe62fae0f108b0872829689e9ca6`）复算。风险窗口分数精确复现 `.0349100`，后段净空为 `.0123757`；差值 `-.0225343`，归一化 margin `.6455`，正确形成第二个 `risk→clear`，并补上动态机制的关事件压力样本。
- 结果 SHA256 `8e1d212893a3e77729b911b4d93d0d8588cef1586cbe68478ecea6c43a0bf6bb`。review 中手填 `created_at_utc` 比实际执行时间晚几分钟，已保留原文件并另写勘误 SHA256 `4458000e09c595e1a7b66cc456da55ab826330873ea5b3c204ea92023f099497`；文件写入顺序显示 review/sidecar 早于推理结果，但该元数据只作补充证据。
- r7.15 仍是同一既有派生来源上的 retrospective 大模型银标，不是新来源、前瞻样本或人工真值，不能把 r7.12 的独立正来源 `0` 改成 `1`。它只把技术判断从“关事件方向只在静态机制出现过”推进到“静态和动态各有一个可工作的关事件”；跨来源稳定性、冷启动和连续窗口仍未闭合。

### 【事后融合原型】公开视频银标 r7.16：强相对变化 + 弱变化双证据生命周期

- 新增 `run_public_silver_dual_evidence_lifecycle_fusion.py`，把 r7.14 相对变化、r7.15 动态关事件和既有 risk-profile 语义退出路由组合为 fail-closed 状态机。它不使用绝对场景分类阈值：可信 clear 基线后的强上升开事件；可信 risk 基线后的强下降关事件；弱下降只有与同一 source、同一前后 episode 边界的因果语义退出候选一致时才关事件；缺少可信参考、零变化或证据冲突均保持 `uncertain`。
- `5%` 强变化边界来自 r7.14 已发布的事后 margin stress grid，因此报告明确把它标为 diagnostic，不是校准或前瞻合同。现有 7 个转移全部正确：5 个强上升开事件、SK1 `.6455` 强下降关事件、Bangkok `-.0412` 弱下降 + 语义退出关事件。
- 四个 fail-closed 控制全部通过：把 Bangkok 语义 gap 收紧到 1 秒后弱下降不能关闭；只有语义缺失而分数不降不能关闭；语义退出与风险上升冲突保持不确定；没有可信参考时即使强下降也不关闭。
- 报告 SHA256 `b0f0a4c46bd2a0de6df6ba9a859811280c594898017e47aa98f2d21172acddc7`。retrospective acceptance 为 true，但 prospective acceptance 被代码固定为 false，因为融合规则是在 r7.14/r7.15 之后组装，尚无新独立来源挑战。它给出了下一版生命周期 head 的明确结构，却仍不授权训练、校准、blind、Android `RiskEventTracker` 或生产替换。

### 【合同已冻结】公开视频银标 r7.17：双证据生命周期前瞻验收

- 冻结 `public_video_dual_evidence_lifecycle_contract_r717.json`，SHA256 `e7439ab3beac677ac913a0bb51155378ce2b2898c61dc4c38399c31235cd6175`。合同公开承认 r7.16 是 post-hoc，并在打开任何新来源画面前固定 `.05` strong margin、三态状态、强/弱关事件规则、1 秒完整视频采样、模型哈希、输入尺寸、语义组、5 秒互证 gap 和所有授权 false；post-clear 动态安全 guard 由验收器直接比较冻结占用峰值，feature report 不能提交主观 hazard 布尔值。
- 新增 `evaluate_public_video_dual_evidence_prospective_lifecycle.py`。执行顺序强制为：先对完整视频生成并冻结 feature report，再允许大模型看原时序并只选择 `pre-risk clear / risk-present / stable post-clear` 三个窗口；review 不能编辑 feature value，也不能直接提交开关分数。
- 新增 `extract_public_video_dual_evidence_features.py`，按合同对完整视频做半开区间的 1 秒时间表采样，并以有界 batch 提取 YOLO12n corridor occupancy、相邻帧注册 residual 和 prompt-free semantic risk count。它不接收 review window，不输出 alert/clear verdict，也不接受外部 hazard 布尔值；视频、两份模型、合同和输出均 SHA 绑定。
- 验收器从冻结特征独立计算机制分数：必须强上升开事件；强下降或弱下降 + 精确同边界语义退出关事件；post-clear 后半窗不能强重开。冷启动、语义缺失单独出现、语义退出与上升冲突三个控制必须保持 `uncertain`。来源谱系、视频 SHA、合同、feature report、inventory audit 和 review 均通过 sidecar/hash 绑定。
- 大模型 review 模板 SHA256 `03923a78b3dd48bee49581d36f2fbe2ac8d2110a1e8f49c2fd3ee82742b48ffa`。提取器 5 个测试、合同 7 个测试、验收器 13 个测试全部通过，包括真实临时 sidecar 的 `run()` 路径和输出哈希复验；当前 r7.12 库存没有新独立正来源，因此没有伪造真实 r7.17 result。Android、训练、校准、blind 和生产门继续关闭。
- 真实工程 smoke 使用已参与 r7.10/r7.11 的 40.117 秒 Hof 公交视频，完整跑过 41 个一秒样本、多个 batch、YOLO12n、YOLOE 和运动补偿，feature report SHA256 `95f2bb6f8bc60f68622b4ecee57669b2850cde128bb39d49da8bc320b7b2a031`。随后故意提交旧 review，验收器在生命周期打分前以结构化错误拒绝，且未生成 result/sidecar。smoke audit SHA256 `9e0897599d2057411f1c606982213422b118878b9dc9f748bfc5190db9a9f59e`；独立正来源计数未改变。

### 【前瞻失败已定位】公开视频银标 r7.18：首个独立行人正例暴露静态特征问题

- 网络恢复后按“先登记许可/来源、再下载、先冻结完整特征、最后看原时序”的顺序检查三条新候选。Commons Trubarjeva 虽有 CC BY 3.0 和许可复核记录，但画面近似固定且只有货车遮挡；Pexels 3874684 是连续 POV，却始终保留施工围挡，路面车辆也不构成行人路径风险。两条均在验收前拒绝，未伪造 positive。
- YouTube `TVCX9tpaty8` 由 Creative Commons 搜索发现，item metadata 明确为 `Creative Commons Attribution license (reuse allowed)`；456 秒、公开、曾直播保存。360p 下载 SHA256 `551f8483e112b38afd0a91840fb56c8539cfcbe3a6475465677a342c695e63ec`。完整 457 个逐秒样本在视觉复核前冻结，feature report SHA256 `9ff5ecbe0abba930463cc24c4679a2419a53364f0df472008df706c7743504e4`。
- 全片 5 秒原时序概览未见硬切；300–405 秒逐秒复核选择 `340–345s clear / 365–371s concrete-pipe risk / 378–389s stable clear`。来源库存门得到独立正例 `1/1`、行人正例 `1/1`，inventory audit SHA256 `7847f19e6494dda6af56c4a4ee4f8b101664009d16044437b151090d57d3bb50`。
- r7.17 真实前瞻结果没有通过：视觉清晰参考窗的静态 residual range 为 `.3058160`，巨型混凝土管近场风险窗反而只有 `.1055915`，open normalized change `-.6547222`，所以冻结 head 正确输出 `uncertain`；风险到净空的强下降 close 通过，post-clear 也未重开。结果 SHA256 `633591eafd019e2e5d6ae91c63268bde666de4564465798d7364884dcd3aae96`。
- 这次失败把根因从“没有独立正来源”推进为“静态通道把相机转动/压缩模糊的残差波动当成比真实近场侵入更强的变化”。下一步不做 SAM/ASAM、head 调参或回改 `.05`，而是冻结一个能分离相机运动不稳定与因果近场侵入的新静态特征，再找下一条独立来源前瞻挑战。r7.17 失败结果永久保留；训练、校准、blind、Android `RiskEventTracker` 和生产门继续关闭。汇总审计 SHA256 `3cd003c196de87b94d4b5afe209f247a54b3bf64f12a572ba8e27f31816520fe`。

### 【表征诊断继续失败】公开视频银标 r7.19：局部变化与单帧自由空间都不是通用静态风险

- 新增零参数 background-normalized probe：ORB/RANSAC 注册后，用固定近场中心残差减同深度两侧残差，只取窗口 median/q75。5 个纯测试通过；Rice 风险段对前后净空排序正确，但三组旧静态 pair 无候选全过。报告 SHA256 `7a15d0e0443b359c6db0b23a4c6dd1c2dda0e0b1c3a5f63af8436e848bc695c9`。它只能表达局部变化，不能表达已静止占道物。
- 网络恢复后下载冻结 `nvidia/segformer-b2-finetuned-ade-512-512` 教师，只把 `floor/road/sidewalk/path` 作为软可走支持，不拟合阈值。固定中心版本在 `Chcne` 和砂堆方向正确，但 `gie8` 反向，Rice 工地前净空的绝对非可走概率还高于管道风险；报告 SHA256 `26ef0ea50707d94a7be5e2ec6faeb6a35f571d0103c4bea32c8362b51fda4f8f`。
- 按明确几何缺陷追加一次无阈值自适应连续路径，不改类别或教师。4 个纯测试通过，但完整五项排序仍无候选通过；报告 SHA256 `642ad6b6ce6e266f76d0367d68896092b2bf463e584820b0a1f8a120abc24be7`。
- 结论：当前静态标签混合了固定中心遮挡、可绕行夹道、非标准工土地面和已静止占道等不同机制，不能期待一个残差或单帧自由空间标量统一排序。停止 mask、类别、阈值与光流变体搜索；恢复独立 matched episode 采集，后续风险轮廓保留多通道机制路由。训练、校准、blind、Android 和生产门保持关闭。
- 恢复采集后，YouTube 逐条许可展开的 18 条新候选均无明确 CC 字段，全部未下载；Commons 上两条 CC BY 3.0 Ljubljana 工地声景先登记后下载 360p 转码，但 5 秒概览确认分别为近固定机位和固定街对面横移，施工从头持续到尾，无行人前进或 clear-risk-clear，故在特征提取前拒绝。triage SHA256 `d6ab3842d651ed3576fa55ae341c0754319e93edef08d4aaa03433377aee90e1`。
- 新增 16 维显式风险轮廓诊断，把注册后局部变化、绝对净空、自适应路径占用、绕行偏移与既有 163 维对象轨迹拼接；4 个合同测试通过。19 episode 的 source-isolated 结果为：trajectory-only `.7389`、profile-only `.4222`、fusion `.7389`，融合没有严格改善基线；profile/fusion matched-pair 平均方向余弦分别为 `-.0626/-.0264`，Rice profile 投影的 open/close 也均失败。报告 SHA256 `d370192ca5ca81d1693eb26274e43daa859210251afd2983d1ead561be005db1`。因此按预注册条件不运行五组 prototype/bootstrap：当前主要是跨机制特征方向/标签合同问题，不是 head 初始化问题。
- 网络恢复后又逐项核验 30 个 YouTube 结果，只发现 `SI7uinNg7jk` 明确为 Creative Commons。先登记再下载 360p，10 秒概览确认它是多机位街景/骑行/滑板/步行蒙太奇，存在连续切镜，不能构造同一行人视角的 clear-risk-clear；已在特征提取前拒绝。继续采集时应优先搜索未剪辑的长 POV，而不是放宽连续性门。

### 【根因与训练入口】公开视频银标 r7.20：机制专家仍失败，合成反事实只支持训练表示层

- 对 6 组、12 个高置信 matched episode 做嵌套 source-isolated 机制专家诊断：observable router 和 oracle mechanism 都不能让统一特征稳定工作。trajectory 的 unified/router/oracle/routed balanced accuracy 分别为 `.5833/.5833/.5000/.6667`；risk-profile 为 `.5833/.3333/.5833/.5000`；fusion 为 `.4167/.4167/.5000/.5833`。报告 SHA256 `8c29ea04130b2af9a65a407c58d9a318586166b13a8d0ff8320eaf5c531f5f7a`。这排除了“只加一个机制路由器或分 head”作为根治方案，冲突已经发生在机制内部的表示与暂定标签之间。
- 建立照片级静态反事实 train-only 数据集：6 个独立真实 clear 父来源、3 个障碍族各 2 个来源、6 对/12 张 1280×720 图。生成图只提供 pair-ranking/classification 暂定监督，不伪造 bbox、mask 或像素真值；父来源被留出时全部后代一起排除。大模型复核与来源/图像 SHA 全绑定，review SHA256 `522c6a98b90370b42f21081f95740b2d68cac193d7ef0332bf4b3182eba953de`。
- 合成响应审计 v2 的来源、哈希、train-only、无像素标注、每族双来源和后代隔离门均通过，报告 SHA256 `2e7dcbcdb30278098aee2d33669a11b7b0d3b38a912d243d846560b168cd4b8d`。`vcz_temporary_barrier` 虽视觉合格，却让冻结四通道 teacher 的 path-lower nonwalkable 与 path offset 下降；它被保留为 hard counterexample，不能因 teacher 不响应而删掉。
- 父来源隔离的四通道 pair-delta 增广将旧真实静态 pair 排序从 `1/3` 提到 `2/3`，但 Rice open 仍失败、close 通过；报告 SHA256 `eb2e574bc4c86d2d7aaa454c990c240dbc9346dc9062511f2610bd159d9578bd`。完整 450 维 ADE 语义 adapter 则只过旧 pair `1/3`，但 Rice open/close 都通过；报告 SHA256 `1d5dc9f2700c8cdf179e4a5bdd11574d6ffc02f215acbdab8f0490ea93a0d42b`。
- 两条冻结方向互补是事后发现，禁止用 OR/fusion 回写成成功。下一步不再堆 frozen head：扩充 teacher-hard、多距离/遮挡/绕行反事实后，训练小型表示 adapter，并继续按 parent-source 隔离；只有真实旧 pair 与一个新冻结前瞻来源同时通过，才进入五组 bootstrap 和三态生命周期 head。SAM/ASAM 仍排在根因闭合之后，Android/runtime 不改。
- 已按上述门先运行一次固定 `seed=0`、4 个 softplus positive-evidence unit、300 step 的非线性语义表示 adapter；readout 是不可训练的等权平均，SegFormer 冻结，不做超参搜索或保存权重。source-isolated 真实 pair 仍仅 `1/3`，Rice open 失败、close 通过，单跑门失败；报告 SHA256 `b8630d4c08823b3bce026f9db77203e4a9d61c17c1e06b564e32f41ef1ffeb7c`。因此五组 bootstrap 明确跳过；下一轮不能继续加 head 容量，必须增加跨父来源 hard counterexample 或改变教师表征。
- 网络恢复后启动逆向反事实：从许可明确的 Wikimedia 真实障碍照片保留真实 risk 端，再由大模型只移除障碍生成 clear 端。首批 2 对分别为 CC0 的人行道废弃沙发、CC BY-SA 4.0 的窄人行道电线杆；4 张图的哈希、作者、来源页、许可和 source-descendant 留出规则已绑定。manifest SHA256 `fbf3a2ab6b7df429823a3f23fb68703847cdd23276672000957287808826639b`，GPT/VLM review SHA256 `dbf60fd344fa67da98f1e1c495d7446245a0bb516bb63ab286645b7e3c656292`。它仍只允许 train-only pair ranking/classification，不产生 bbox、mask、distance field 或真实评测信用。

### 【特征门通过、独立前瞻暴露专家分工】公开视频银标 r7.21–r7.23

- 两对“真实 risk + GPT 只移除障碍生成 clear”的逆向反事实通过独立来源、许可、图像哈希、无 bbox/mask、train-only 和父来源隔离审计，审计 SHA256 `784adac3c5b3d707c2159ec29e13ef174307235ab56dbd495aec2f494f4fc943`。它们只参与表示诊断，不计作真实评测。
- 固定 `facebook/dinov2-small` 最后一层的 5 个预定义区域统计，不选层、不选区域、不搜阈值，形成 1920 维 pair direction。三组旧真实静态 pair 与 Rice open/close 全过，报告 SHA256 `37f90be695d16cf3937a7c019cc8e2e8c04a5cb482489ee89aecb3b3fbcdfd5f`；随后五个固定 seed、source-bootstrap、80-step pair-ranking 短跑 `5/5` 全过，SHA256 `69223aae8d1ff4d6273cf0acf4f37d7ab01b49dc6182054d72265f1e38fa7852`。这把根因推进为：通用 teacher 的区域 patch 表征可分，先前主要是特征合同不足，不是 SAM/ASAM 或普通 head 优化不足。
- 在看新视频前冻结 r7.22 DINO pair 合同，SHA256 `6b5924ab35b9866a4ec23994518e5316de1b7ccc1e127e2e79a5535392389845`。Japan rural riverside 连续行人视频先冻结全片逐秒特征，再复核固定 `2–7s clear / 10–15s cone risk / 17–22s clear`；DINO open projection `-.0048568` 失败、close `.0432368` 通过，结果 SHA256 `cde840bb604766d5dcd0c3f471e32d9db64c5e4b171c271113622d16c97a4995`。窗口和方向永久不回改。
- 同一 Japan 视频恰好落在更早冻结的 r7.11 彩色施工标志专家合同内。原合同不改阈值即可得到风险窗激活率 `.8`、稳定净空 `.0`、终态 clear、孤立 27 秒假检不重开；正式结果 SHA256 `ad71ad76a2859611401e13fad7ebc1de80c1927d1dc466d60593fd5bfb190191`。因此不是单一“大模型特征”覆盖所有风险，而是机制特定正证据专家互补。
- r7.23 “风险轮廓 + 生命周期”融合原型以 OR 打开独立正证据，但要求所有曾打开事件的通道各自确认关闭；冲突只能保持 `present/uncertain`，不能用缺失直接清空。Japan 后验原型 SHA256 `ed147893f8ee49f4b9cc3648aa7ec0d6227d8671e94ad6e711ba3327c0218f63`；随后冻结的真正前瞻合同 SHA256 `4918fe3e6053a3dd7b13d200ea219dfbdfee2a6e20dbc7be67fefc5b18317071`。合同 UTC 字段在任何新来源登记前由误标的香港本地时间纠正为真实 UTC，Japan 只能用于派生，下一条新来源才可计入融合前瞻门。
- 结论：风险轮廓/生命周期升级为主监督架构，像素分割和距离场只作辅助；SAM/ASAM 仍是第三顺位。r7.23 尚无冻结后的新来源，训练、校准、blind、Android runtime、`RiskEventTracker` 和生产替换继续关闭。

### 【前瞻负控失败】公开视频银标 r7.24：专家 OR 缺少走廊相关性

- r7.23 合同冻结后、任何画面审阅前登记并下载 Commons `Walking in Matoaka, West Virginia`，CC BY 3.0、连续行人视角、17:52，480p 视频 SHA256 `017f860e002c75d093206772800bd68cb1c19f226b74e4d5a933798916347821`。先冻结 1073 个逐秒 DINO 特征（SHA256 `1fb898825505deedb8f611e6de62406a628e2b055a7e14a797646f90c6318a9a`）和 1073 个原 r7.11 彩色专家特征（SHA256 `1e526a7a2e155856559766008fab3ad8c4f75315cbb441db15ed2db5fa1e008f`），之后才生成全片/密集 contact sheet。
- 原时序视觉复核先拒绝 720–840 秒的工程机械/木料候选：它们隔着排水沟，不侵入步行路线。随后固定 165–175 秒净空、178–188 秒马路对面交通锥负控、190–200 秒净空；review SHA256 `0b0484c810c530076119f523c235e8342f63b5013767d36b2a3d03ab2a969aa2`。
- r7.23 前瞻负控失败：DINO 在视觉净空中段 open projection `+.0375678`，close `-.0551055`；彩色专家从 174 秒开事件、187 秒末次激活、192 秒才清除。两个通道都会让 OR 融合假开事件，结果 SHA256 `938c03fa3ac03a341ecb3b0885946496ad5ebe67268586f2c7b55ccf7f0ac0ef`。
- 根因更新：专家识别到“场景变化/交通锥存在”不等于它与行人走廊相交；DINO 还会把转向和视点变化投影成风险方向。下一版开事件必须是 `机制正证据 × 路径相关性 × 接近/侵入时序`，不能再用无条件 OR。现成 fixed nearfield flag 在 Matoaka 全为 false，但 Japan 真正小锥桶也全为 false，故不能直接拿固定中心几何门补丁回救；要做自适应路径/轨迹相关性并在新来源上前瞻验证。
- r7.23 失败永久保留，禁止改窗口、放宽/收紧颜色阈值或把 DINO 改成辅助后追溯宣称通过。训练、SAM/ASAM、Android runtime 和生产门继续关闭。

### 【诊断冻结 + 新负控通过】公开视频银标 r7.25–r7.26：径向接近可过滤旁路锥桶

- r7.25 仅在 Japan 正例、Matoaka 负例和历史车辆来源上做回顾性诊断：每个时间点取冻结 r7.11 颜色规则接受且 `bottom_y` 最大的标志框，要求至少 5 个样本、首末各 3 个中位数、`bottom_y` 前进至少 `.05` 且大于水平扫动，并要求框面积对数增长为正。Japan 小锥桶通过（纵向 `.2839`、横向 `.1148`、log-area `2.9604`），Matoaka 5 个假事件和历史车辆事件均拒绝；诊断报告 SHA256 `8881048ddd5f3fab4f10fc56eba435d0bc572bf3b039d65b6f988cebae70efcc`。它只适用于行人接近机制，不能回改 r7.23/r7.24。
- 在新来源登记前冻结 r7.25 合同 SHA256 `6746957ce89d4f133c21802632ba4c5c972b01d0d61e34895b11b12392e9a8ce`：DINO 降为 support-only，只有原 r7.11 彩色事件与固定径向接近门同时成立才可开事件；五帧缺失才关闭，冲突维持 `present/uncertain`。训练、校准、blind、Android 与生产授权全部为 false。
- r7.26 在下载和看画面前登记两条新 Commons 连续行人视频。Bramwell 视频 SHA256 `9c582c178a67b12b70df7c5a9542b18f5f00583a69c2e61e2cbcf9dd64d29b45`，Stegna SHA256 `ae0385d336785b0179e8655ab0a3feaef507c7e9885fddb5d457ebd2fd8fc3d8`。全片 2163 个逐秒颜色特征先冻结，SHA256 `efd3127df05aca4a280dacbdb92d2e47b083989f1845918bc941cb63b827e160`；随后冻结候选为 0 个，SHA256 `576fe7da8cdd213e2d4ee71ef6c4dff7fb56f9cdd8907bba563aed7bdc222c9a`。
- 后验密集复核把 Bramwell `375–385s` 固定为“路肩锥桶近距离掠过”负例：锥桶只在草地/路肩，铺装通道未被占用或缩窄；380/381 秒的识别跳变来自桥牌附近转身/横扫，未形成持续径向接近。该负控通过，结果 SHA256 `9c3910fc5467a0890831c721eeba3eda9371164ca121c2d11658f9ca58ec2752`。Stegna 为拥挤旅游步行场景但没有审阅到施工标志生命周期，只记 context-only，不计门禁分母。
- 当前含义：`机制证据 × 径向接近` 比无条件 OR 更能抑制“看到但不挡路”的假提醒，但只有 1 个冻结后负例通过，尚无冻结后独立正例；不能宣称召回闭合，也不能训练、改 `RiskEventTracker` 或替换端侧模型。下一步继续寻找至少一条有明确路线侵入并随后净空的许可连续行人正例；像素分割/距离场保持辅助，SAM/ASAM 仍为第三顺位。

### 【来源拒绝】公开视频银标 r7.27：施工画面不等于行人接近事件

- 网络恢复后检索到 Commons `Roadwork with no dust control 02`，条目级 CC0、9.4 秒。按 r7.25 合同先登记再下载，480p 视频 SHA256 `4e6b47fc218a7e96cfa601309315dd73462957516fb1a07d15343d0baf0ba252`；视觉复核前先冻结 10 个逐秒特征，SHA256 `da2202521c783a37d9a3babc6ca70709e3a37050651b8aa7196b016c5bafc833`，冻结候选仍为 0，SHA256 `1d87766d92bc69b30bb6098f5ac7b5aaf6198e42b359ff160839eb34a00fc0f1`。
- 后验 500ms 接触表显示：0–4.5 秒主要站在原地横向拍摄施工围挡，之后快速甩镜进入建筑；没有连续行人接近、路线侵入、离开风险再稳定净空的因果时间线。该来源在评分前拒绝，既不算正例也不算负例，不能用“0 候选”给 specificity 加分。
- 这再次说明当前瓶颈是合格的独立连续正来源，而不是网络下载能力或更多施工关键词命中。继续保持 r7.25 合同不变，避免用固定机位/横扫施工画面伪造召回证据。

### 【前瞻正例失败 + 生命周期根因】公开视频银标 r7.28–r7.29

- r7.28 在看画面前登记 YouTube CC BY `TVCX9tpaty8` Rice Street 连续 POV；视频 SHA256 `551f8483e112b38afd0a91840fb56c8539cfcbe3a6475465677a342c695e63ec`，冻结颜色特征 SHA256 `1baa692d2cdd779cbd0728d192651090906c46bbe931e6cf8f015ac9afedde3d`，候选报告 SHA256 `4acf7972c45d44686764ba6a83ca17da5b02334077ce7a42f534341380810ae4`。后验复核只见邻接施工和可选绕行，375–390 秒仍有宽阔净路，419–425 秒为路线选择歧义；不把含混窗口强写成正例或负例。
- r7.29 在看画面前登记两条 YouTube CC BY 来源。Edmonton 视频 SHA256 `b63cdc78fe7d97cff3536c88bb3807a032a5d0fa413bc9836af6a03e99a00468`，Kampala `a62531418d6137168423907717ffecb10bcfd36c7f523768d96963a071e63047`；2451 个逐秒颜色特征先冻结，SHA256 `ea351ffe25201fb0e2ef6fa99d6e5cb6261784d5e429cc0fde673f519bd86ade`，再冻结唯一候选，SHA256 `0c350a4659485c9df490e3937fbb586c303d6a6da5aee46209aa7d123394c78c`。
- Edmonton 在 671–692 秒的锥桶径向接近通过入口门；大模型逐秒银标复核显示同一狭窄施工步行通道至少持续到 735 秒，782–810 秒才作为保守稳定净空。r7.25 却在 697 秒因五个缺失样本清除，提前 38 秒；第二段颜色证据 701–773 秒因不再满足径向入口而不能恢复同一事件。正式失败报告 SHA256 `d6230f066e0684c1f65a88b2c6564071e08376011263d776fc71ed498bc7ca42`。Kampala 是横向工地巡看，只记 context-only。
- 根因从“入口能否检测”推进为“入口与退出证据不对称”：径向接近适合作为开事件条件，却不能作为持续/退出条件；短期检测缺失应保持 `uncertain`，已有颜色证据恢复只能续同一事件、不得第二次提醒，真正 clear 需要更强的持续缺失或正净空证据。

### 【回顾性修复冻结 + 新负控】公开视频银标 r7.30–r7.32

- r7.30 在打开 Edmonton 失败后回顾性扫 `5–15` 个连续缺失样本。只有 `9–15` 同时满足风险段不假清、稳定净空内清除和同一事件只提醒一次；最小充分值为 9。诊断 SHA256 `a1070be1326b455c2ae83789c2254ada97c2465eedc4523c4633888d6e1528ca`。这不能回救 r7.29，只授权冻结下一份前瞻合同。
- 在检索新来源前冻结 r7.30 非对称生命周期合同，SHA256 `b692f72758d7f34021a4dd02dd65371fa24a9ddc7faa48b821fb6003dd158169`：冻结 r7.25 径向候选才可开事件；开后原 r7.11 颜色证据可跨 gap 续命但不再提醒；连续 9 秒缺失才清除；清除后仍须新的径向候选才能重开。训练、校准、blind、Android 和生产授权保持 false。
- r7.31 按 CC BY 元数据和上传者文字时间轴，先登记 Dallas `23:00–28:20` 连续步行片段，再下载/截取。视频 SHA256 `99639863eb21aa752b2a9ae39f7cd78d850690d49fdbfd8e5f199239973dc0bd`；321 个特征 SHA256 `e6777a75890a3549d9f97fa4105baf8b3e43b7e9e1383349a2e3974c7b9ad899`，0 候选 SHA256 `6c530633e6636e5c2f716ce921fde5709acbc11fce6ab5cd17056840283b6ac9`。后验复核显示橙色绕行牌在草地、孤立小锥桶在道路边缘，步道始终可通行；新负控通过，结果 SHA256 `0186c526aef0b728d08f1aedaa6e46f9c5e4e73cbba52a9109688383d0a5fa5a`。
- r7.32 仅凭 CC BY 许可和标题先登记 `More cones, barriers and lights.`；视频 SHA256 `0d784667775729099b133a2b4e8953e42f78e70bbc885ebbbdff14145e08ced7`，592 个冻结特征 SHA256 `1ecb96740015e6d0cd534253053fe9f8cd639c2bd60e5ba3e94cfd63caf45d80`，0 候选 SHA256 `1d174523e1495ba35562856068a381e472c646959e5cb6db9cd10a8430ce7a4a`。看图后确认是车库内锥桶/护栏/警示灯演示，不是连续行人走廊；拒绝报告 SHA256 `264d0d6f6f9628a78d11a96a2eb446046281a568ac8b96600193f4f519c9f08f`，不计正负门禁信用。
- r7.30 仍未闭合：r7.31 没有真实径向入口，无法检验 9 秒退出是否拖得过长。下一条最有价值的来源不是更多“施工画面”，而是“径向入口确实通过、随后 2–10 秒内视觉明确净空”的连续行人负压力；在此之前不改 Android `RiskEventTracker`，也不把大模型银标冒充人工真值。
- r7.33 用 CC BY Cape Town 长步行的文字章节先登记 `13:00–16:40` 连续窗口，再缓存公开 360p 视频并本地截取。片段 SHA256 `978fd134a69bd0b262a104f6d5b2571dbbba130c2723539687ff5567d7f68a15`；220 个冻结特征 SHA256 `968082ec21733802e6bff04221204e8497ce884f327495ebc7d7457e6654a15a`，0 候选 SHA256 `38c550e90c7921f3e83bd24b106015764d742c27c2f86c109979871cbf42b8fe`。逐秒复核显示 126–134 秒为固定混凝土柱，173–176 秒橙色锥桶位于宽阔广场且无需绕行；第二个新负控通过，结果 SHA256 `eafef19730cf4e62774e35063f1677300f5a6913ebd5405f241085c11aee8556`，仍不产生 r7.30 退出信用。

### 【高密度负控 + 局部接近假设证伪】公开视频银标 r7.34

- YouTube CC BY Jakarta 连续步行按文字章节“08:14 管理人员移动锥桶、道路恢复”先登记 `08:00–09:20`，再下载和本地精确截取；片段 SHA256 `f2a0d3ec4f075894d0e27fee8fc07b5446c477dc88aabc2cd6a0a378bd684fdb`。80 个冻结采样中 YOLOE 检出 221 个 traffic-cone，特征 SHA256 `ab5b9cba6ff27695c6f54fb4c92cabf77c76950609c44c30395a1ffba8234b4b`，但 r7.25 仍为 0 候选，SHA256 `988b87ea2628ab3ada75020fb660634ac022e35820ad2ae908f8ffdd68684735`。
- 后验逐秒复核显示锥桶和水马沿宽阔车道边界排列，拍摄者沿开放弯道通过，没有路径侵入、瓶颈或被迫绕行；高密度负控通过，结果 SHA256 `3ef05cb89c6df9cc939fa6370f673b96c23c330b484bdfc5518d214f8ff2ee3b`。
- 事件首尾底边只前进 `.0028`、水平扫动 `.1094`，但面积增长显著。为检查长事件是否吞掉局部接近，固定扫描 5/7/9/12 个接受样本的所有短窗，窗内完全复用 r7.25 门。Japan 正例在 5/7/9 保留，但 Jakarta 四种窗宽全误开，Bramwell/Stegna 与 Cape Town 也出现误开；诊断 SHA256 `a410ef8eeebd78bf16098d1beccda519c6f0e977484bac9378cbded50d4e9fb9`。因此“改短窗”不能冻结为新合同。
- 根因进一步收敛为：框的接近几何不能区分“沿边界掠过”和“侵入实际行进路径”。下一实验应显式估计自适应可通行走廊/未来轨迹占用，再与机制证据和接近时序相乘；不再继续搜径向阈值。训练、校准、blind、Android runtime、`RiskEventTracker` 和生产替换仍关闭。

### 【路径关系反事实 + 表示门失败】公开视频银标 r7.35

- 直接整图生成式编辑虽把锥桶放入路线，却重绘并放大背景，违反单变量因果合同，未进入任何 manifest。随后只用大模型生成一个无阴影透明锥桶资产，再以确定性 alpha 合成构造 3 组 Jakarta 配对；每组 clear/risk 都有相同 4 个锥桶、相同尺度/纵深/间距，只做横向平移，父图干预掩码外变化像素均为 0。等数量 v2 生成报告 SHA256 `7a1297a6d0c6f7cf5dbf9513204a904dc89a15458f02925d9df642311a270671`，manifest SHA256 `74443a9e9d1906a42a6f6c23790836de94426d8d4722ed498c07978862a6f6e4`；全部 train-only、父来源留出时整族排除，不计真实评测。
- 冻结 DINOv2-S 区域向量不训练 head：三组留一投影均约 `+.24`，水平镜像均 `+.25–.28`，pair 增量余弦 `.71–.75`；Japan 正例 `+.03166`、Edmonton 左侧走廊正例 `+.02591`、Cape Town 负例 `-.02907`。但 Jakarta 高密度开放道路仍为 `+.00882`，严格门失败，报告 SHA256 `a4494f81b9cccca082bb4c65ac5b34ff3d8a45d50453f51058848745e85b7d33`。
- 含义：冻结 teacher 能表达“同类标志从侧边进入路线区域”，且不是单纯左右位置偏差；但尚不能表达进入后的剩余通行宽度/绕行余量。二元路径侵入不能冻结为新合同，下一风险轮廓要显式包含 `侵入跨度 / 可通行宽度 / 绕行余量`，像素分割继续只作辅助。

### 【前瞻来源拒绝】公开视频银标 r7.36

- Commons `Walking down the Trubarjeva street.webm` 为 CC BY 3.0，文字描述称从仍在施工路段步行至已完工路段。整段 `0–13s` 在下载/看帧前绑定 r7.30；本地固定视频 SHA256 `cee1cf1488cfee7d1b099a67aabaaa4c40846b1cb6395b520380710e8bb6738c`，14 帧特征 SHA256 `aad2aec4988fd5f307385beae85ed0019e5bddfee899ba0279cb5fbf5cb160d8`，0 径向候选 SHA256 `cd67d219113038e50d128790a338532af094ce338bce0113c4c3a62679cac2d9`。
- 后验逐秒复核显示施工机械/车辆始终在开放街道侧边；7–9 秒白车只是遮挡镜头，之后回到同一施工场景，没有真实径向入口或稳定净空。来源在事件评分前拒绝，SHA256 `70730ac7a3cd55b837ecac323dcac0b22739c73e8c45f8bc7a5d61f71adcedfc`，不计正负门信用。

### 【元数据命中但视角不合格】公开视频银标 r7.37

- Commons `Tram Worksite in Hämeenkatu, Tampere.webm` 为 CC BY 3.0，文字明确称施工使行人通道变窄；整段 `0–137s` 在下载和看帧前绑定 r7.30。固定转码视频为 137.28 秒、640×360，SHA256 `7f930a1399165aad8822ac27b54b6924b9d8abd40a77ac0f664d66b47f47f850`。
- 视觉复核前先冻结 138 个一秒采样、192 个目标检测，特征 SHA256 `18d8539972c4c219dfa3aae1e0ad224f73334bfa7a7e6bec47adb167fe630b2b`；r7.25 径向候选为 0，SHA256 `e6918d21c581836ca68506b73d24250cb3259780bc5c3b872dd6e2fe64536d99`。
- 后验概览确认这是路边固定观察位横向拍摄公交、汽车、围栏和积雪工地，不是行人向前穿越。没有 ego 走廊入口、剩余净宽挤压或随后稳定净空；评分前拒绝，SHA256 `6ce7687fec64670fafb3ec178b08724bc24e12fe3d6afc57f51925695f1fe74a`，不计正负门禁信用。检索规则因此收紧为元数据同时要求第一视角/步行与施工/封闭人行道。

### 【二维净宽/距离场主分数证伪】公开视频银标 r7.38–r7.41

- 新增零训练参数的障碍感知路线宽度 probe：冻结 ADE20K SegFormer 只给可走支持，冻结 r7.11 彩色施工标志给障碍；障碍按检测物高度扩张后，在 128×128 图上计算下方视野到中景的最大瓶颈路径。纯几何 8 个测试覆盖开放、横断、侧边/中央障碍、软容量和自适应中心线，全部通过。
- r7.38 使用 argmax 可走类别时，真实 clear 窗本身大多不连通，四组 real risk-clear delta 全为 0；报告 SHA256 `7e5a72b10fef7a7b945dc09fc49f1c5e75bb1f35e59f84c031e4af8d65ad15d5`。r7.39 改为唯一一次软可走概率×净距 OFAT 后，Japan/Edmonton 风险反而更“宽”，Cape Town 假收窄；报告 SHA256 `c4e6ca6dd70744ad0209ffb970ef5f4a182c6e324a80107a09cc710ff6ee5861`。
- r7.40 把软语义降为只跟踪自适应中心线，主量改为中心线到障碍的距离场 q10：Japan/Edmonton 方向正确，但 Jakarta/Cape Town 同样被判收窄，且三组合成 pair 全并列；报告 SHA256 `cba812f73ae10747c8915f1a46d21059f899140417d5b89c1000f19dddfce73d`。检查发现两侧各扩一整个物高把总安全宽度写成两倍，故只补一次物理修正 r7.41（两侧各半高）；Japan 又反向、Edmonton 保留、两个负例仍失败，报告 SHA256 `7c9064019233afae56dc117fc293e5e5da9be54be5cbd68420436e25baccba03`。
- 结论：距离场能表达障碍邻近，却不能自行确定“用户本来要走的路线”；自适应路径还会绕开障碍，场景语义又会受道路外观影响。停止膨胀尺度、阈值和路径宽度变体；距离场只保留为未来表示 adapter 的辅助监督，不能作为主风险分数。下一步回到 DINO 正负 prototype 线性 probe，让 Jakarta/Cape Town 真实边界负例进入训练折，并以 source 留出判断特征是否可分。

### 【确定性正负 prototype 仍不可分】公开视频银标 r7.42

- 新增 source-isolated DINO 正负 prototype：每个 fold 内把 unit 正 prototype 减 unit 负 prototype，以 0 为固定阈值，不训练参数、不搜 ridge。三组合成后代与真实 Jakarta 共享父 source；留出 Jakarta 时四条一起删除。真实正例为 Japan/Edmonton，真实负例为 Jakarta/Cape Town/Bramwell 及 Dallas 的草地绕行牌、路缘小锥桶；全部绑定既有 feature/review sidecar。
- 结果为正例召回 `5/5`、负例召回 `2/5`、balanced accuracy `.70`。Cape Town 与 Dallas 草地绕行牌正确为负；Jakarta 高密度边界、Bramwell 转弯路肩锥桶、Dallas 路缘锥桶仍为假正。三组合成水平镜像在 Jakarta 父 source 留出方向下均保持正。报告 SHA256 `4fd55dfcd57f84295a56c2335b203a62fb85f3dd2bc71b302ddf58f7446d249f`。
- 结论：DINO 区域 delta 确实稳定表达“标志进入/接近画面路线区域”，但把转向、相邻场景变化和实际 ego-path 侵入混合；正样本不缺、head 原型也不是瓶颈，负类表征仍不可分。因此不运行五组 bootstrap，不做 SAM/ASAM。后续应把短时相机运动/转向作为 nuisance 单独估计，再让风险关系使用 motion-normalized residual；r7.25 长窗径向合同仍是当前可靠入口门，DINO 不能独立开事件。

### 【相机漂移只解释部分假正】公开视频银标 r7.43

- 保持 r7.42 的样本、source 留出、prototype 和 0 阈值不变，只从每个 clear 窗自身前后半段估计 DINO 漂移方向，并把 marker-minus-clear delta 在该方向上的分量正交投影掉。没有对合成 pair 投影，也没有搜索缩放、窗口或阈值。
- 正例仍 `5/5`，负例由 `2/5` 提升到 `3/5`，balanced accuracy 从 `.70` 升至 `.80`；Bramwell 转弯路肩锥桶由假正变真负，Cape Town 与 Dallas 草地绕行牌保持真负。Jakarta 高密度边界和 Dallas 路缘锥桶仍是假正；三组父 source 留出镜像继续为正。报告 SHA256 `738d0ce65b1e80ecc67a1556f318c1ff6dd8954f33e804702bfdf83c1c435ca0`。
- 含义：短时相机/场景漂移是表征混淆的一部分，但不是剩余路径关系的充分解释。停止 nuisance 投影变体；下一步扩充跨父来源的等数量路径关系反事实，优先从 Bramwell/Dallas 的已审阅 clear 父帧分别生成相同锥桶“边界 vs 中央”pair，父 source 留出时整族排除。只有新背景下 source-isolated 负召回闭合才允许五组 bootstrap。

### 【跨来源反事实揭示 DINO 方向冲突】公开视频银标 r7.44–r7.46

- r7.44 从 Bramwell `700s` 与 Dallas `128s` 的连续前向 clear 帧各构造一组等数量路径关系反事实。父帧无人居中、无转向；同一个锥桶只作横向平移，数量、尺度、纵深不变，掩码外父像素逐像素不变。首次 Bramwell 风险位置仍在路缘，视觉拒绝并保留；预注册位置只修正一次后，两组均通过 GPT/VLM 暂定审阅。接受版 generation report SHA256 `ac294ee6224dfd5bc2313d0d3e4be7ae0250d199722778af9928d0bba3f59e96`，review SHA256 `b9963c763513fb4ccebf8ce916759e77164e875104a2f4a59186004e9b7bd08a`。它们严格 train-only，不是人工或事件真值。
- prototype 脚本升级为每个合成后代继承自己的真实父 source；留一来源时，该来源的真实样本和所有合成后代一起删除。r7.45 保持 clear-drift 投影、正负 prototype 和零阈值不变，加入两组新反事实后 balanced accuracy 从 `.80` 降至 `.3714`：正例只命中 `1/7`，五个合成正例中四个镜像也失败。报告 SHA256 `9be9736241976bb3ce780b526738b6a4ff5f263440f5850486ffda504e0bb422`。
- 为检验“区域均值抹掉横向位置”，r7.46 只把冻结 DINO 最后一层改为固定 `4×4` patch-token 网格并展平，不加 CLS、不搜层/网格/阈值；balanced accuracy 仍只有 `.3429`，报告 SHA256 `7864be4266be053bba6053b19e028e1e4951d8de3ea30e522da82fd49f6aac0b`。
- 结论：问题不是 prototype/head 初始化，也不只是区域 pooling；冻结 DINO 表观 delta 在不同背景中没有一致的“障碍进入未来路径”方向。停止 DINO pooling、nuisance 和阈值变体，五组 bootstrap 与 SAM/ASAM继续关闭。下一表征必须显式建模“障碍位置 × 预测 ego 路线”，距离场只作辅助，不能继续用场景外观替代路线几何。

### 【单帧路线关系不足，事件级组合可行但证据未闭合】公开视频银标 r7.47–r7.48

- r7.47 新增零参数显式关系：先只在扩张后的 marker 掩码内恢复 SegFormer 可走支持，避免障碍把 adaptive path 推走；再沿恢复后的 ego route 计算到障碍的 q10 距离。4 个纯几何测试覆盖无障碍、中央/侧向障碍、障碍造成的语义缺口和非法概率，全部通过。
- 真实窗口中 Japan/Edmonton 正例 `2/2` 全过，但负例仅 `2/5`，balanced accuracy `.70`；Jakarta 密集边界、Cape Town 宽前场和 Bramwell 转向路肩仍被误判。五组合成原图只过 `3/5`，镜像 `5/5`。报告 SHA256 `6a72165c166ac64b46e71a54149b928c3ea5da3b662a18419bd6c6ad36d0f87e`。因此单帧 segmentation route 仍不能替代真实运动意图，停止 inpaint 半径、路线宽度和阈值变体。
- r7.48 将主任务改为事件级风险轮廓：`冻结 r7.25 径向入口 AND 显式路线关系相对可信 clear 上升` 才开事件；开后沿用 r7.30 颜色证据续期、短缺失进入 uncertain、连续 9 个一秒缺失才 clear，同一事件只提醒一次。像素分割/距离场只提供辅助路线关系，不能独立开事件。
- 在当前 2 个真实正事件和 5 个真实负场景上，事件混淆矩阵为 `TN=5, FP=0, FN=0, TP=2`，balanced accuracy `1.0`，Edmonton gap-bridge 生命周期也通过；报告 SHA256 `11f75c2697d45b329301667d89ca8dcae3355d483e4557f8725aea8dc2a8ecb1`。
- 但完整 closure 仍为 false：五个负场景的冻结 r7.25 径向入口都为 0，只证明入口隔离，尚未证明路线关系能否否决“真实径向接近但始终安全侧向”的负事件；Japan 也尚未按 r7.30 做完整因果生命周期重放。网络恢复后的 Commons/YouTube/Vimeo/Archive 搜索没有找到许可和连续性同时合格的新负控，检索审计 SHA256 `e8e244517e7bdd1080137d261d3050f39225db00baea76c624c66bafc2a3382d`。五组 bootstrap、训练、SAM/ASAM、Android 和生产门继续关闭。

### 【因果时序揭示提前提醒】公开视频银标 r7.49

- 新增 Japan 因果 prefix 重放：只用每个时刻之前的样本重算 r7.25，不允许完整事件未来信息；路线关系基线固定为视觉预先冻结的 `2–7s` pre-risk clear 中位数，开事件必须同时满足滚动径向通过和当前关系高于基线，退出完全复用 r7.30 的 9 秒缺失与单次提醒。
- 原始 chromatic event 从 `4s` 开始，但因至少需要 5 个接受样本，滚动径向最早在 `8s` 通过；路线关系也已高于基线，故因果组合在 `8s` 开事件。冻结 GPT/VLM 风险窗口为 `10–15s`，因此提前 `2s`，开窗检查失败。事件随后持续覆盖风险，并在 `22s`（连续 9 个缺失）进入 clear，恰好落在冻结 `17–22s` 稳定净空窗，同一事件只提醒一次。
- 报告 SHA256 `95ce8201dea8979634e07c07cbefe5edaa73f74c428dc1172676c1f644601d56`。这把 r7.48 的“当前事件分类 1.0”纠正为“事件身份可分，但时序合同未闭合”；不能事后把提前提醒改称正确，也不能通过调 relation 阈值或扩大视觉风险窗回救。下一步必须在新来源前冻结可接受提前量/风险 onset 定义，或用真实 time-to-contact/剩余通行宽度目标替代二元窗口。

### 【新来源专用时序合同】公开视频银标 r7.50

- 在继续打开新候选前冻结 `public_video_event_timing_contract_r750.json`，SHA256 `9b3c9fb4ed42e9ad8723592e31c2ac506584d12cf54036c5202d21df52041067`。提醒验收带固定为 `material_risk_onset - 3000ms` 至 `latest_useful_reminder`；3 秒上限明确是在看过 Japan 提前 2 秒失败后提出的 post-hoc 假设。
- Japan 仍是 r7.49 时序失败，只能参与派生，永远不能获得 r7.50 前瞻信用。新来源必须先登记并冻结全片特征/候选，再由大模型原时序复核 `material_risk_onset`、最晚有效提醒和稳定净空窗；复核仍只是 provisional silver，不是人工真值。
- full closure 同时要求两个不同真实来源：一个独立正事件在固定提醒带内仅提醒一次并正确 clear；一个独立真实负控必须确实产生冻结 r7.25 径向入口，但障碍始终安全侧向，并被冻结 r7.47 路线关系否决。合成或 GPT-only 样本不能获得门禁信用。
- 复核空模板 SHA256 `2e823daa9fe379e61e158a5c071792a9295f9b87ec5fba43511e0a1a4ed95797`。训练、五组 bootstrap、SAM/ASAM、Android runtime 和生产替换继续关闭。

### 【新来源筛查 + 独立正例通过】公开视频银标 r7.51

- 网络恢复后先筛 Pexels 3874684，但下载后的平台 item-ID 谱系核对发现它早在 r7.17 已冻结并视觉拒绝；不同分辨率 SHA 不能冒充新来源，r7.50 未提特征、未给门信用。第二条 Pexels 5234995 经本地 item-ID 去重后下载，视频 SHA256 `6d65adc4...e8e54d`；25 个逐秒冻结特征只有 1 个 traffic-cone 和 2 个目标检出，r7.25 候选为 0，报告 SHA256 `c547fa66...1a714`，故在视觉复核前拒绝 true-radial 负控角色。
- Commons London POPtravel 55 分钟第一视角步行以 CC BY 3.0 登记，240p 视频 SHA256 `ee68ca32...82836`。全片 3301 个逐秒特征先冻结，含 534 个 traffic-cone、923 个目标检出，特征 SHA256 `81e5317e...83680`；随后冻结唯一 r7.25 候选 `2678–2687s`，报告 SHA256 `088f37bb...5102b`。
- 只在候选冻结后打开 `2674–2699s` 原时序。大模型银标复核认为真实红色锥/隔离桩进入下方中心路线，不能诚实充当安全侧向负控；将其固定为前瞻正角色：material onset `2681s`、最晚有效提醒 `2684s`、风险结束 `2687s`、稳定净空 `2688–2699s`。
- 冻结显式路线关系增量为 `+0.953596`；事件在 `2678s` 提醒，恰好处于 onset 前 3 秒合同下边界，持续至 `2687s`，连续 9 秒缺失后 `2696s` clear，且只提醒一次。全部检查通过，报告 SHA256 `7043eeaf60427f688e18aa75e458d8536a1ee4ffa1eb91a6644bd5d14361eb8c`。
- r7.50 总 closure 仍为 false：正例已补齐，但仍缺另一个真实来源的 true-radial safe-lateral 负控被冻结路线关系正确 veto。五组 bootstrap、训练、SAM/ASAM、Android 与生产授权继续关闭。

### 【真实径向侧向负控证伪路线表征】公开视频银标 r7.52

- 新来源搜索先按许可审计拒绝仍处于 Commons license-review-needed 的 Kassel、Singapore、Brno 和 Tehran；YouTube 搜索结果没有可验证的条目级开放许可，未下载。CC BY 3.0、许可复核已关闭的 Maribor 在注册后冻结 2100 个逐秒样本，虽然有 215 个目标检测，但 r7.25 候选为 0，故在视觉角色复核前拒绝。
- CC BY 3.0 POPtravel Ulm 在下载和视觉复核前完成注册；240p 连续第一视角视频 SHA256 `67efb35b...caa48`。2177 个逐秒样本、282 个目标检测先冻结，特征 SHA256 `7053981f...18cf`；r7.25 随后冻结 3 个径向事件 `7–18s`、`1504–1510s`、`2136–2164s`，候选报告 SHA256 `bf1c1fc6...a3b9`。
- 候选窗口和 18 个关键帧时间在看图前固定，计划 SHA256 `81ca66c6...e6c1`。大模型原时序复核拒绝候选 1/3：施工围挡与实际转向重叠；候选 2 的红白路桩从道路两侧径向放大，但中央缺口持续开放、拍摄者直接穿过，无需避让，故只以 provisional silver 赋予 true-radial safe-lateral 角色。
- 新增 `evaluate_public_video_true_radial_route_veto.py` 和 3 个纯逻辑测试。冻结 r7.47 路线关系在候选 2 上没有 veto：clear 中位侵入 `0`，marker 中位侵入 `.928303`，增量 `+.928303`，把两侧边界路桩误判成路线侵入；正式失败报告 SHA256 `cb02a1c0...d9ea1`。
- 结论：r7.48 的历史完美事件指标来自负控没有真实径向入口，Ulm 首个严格压力样本将该捷径证伪。当前缺口仍是“障碍位置 × 未来 ego 路线”的可泛化表示，不是 head 初始化；距离场主分数已在 r7.38–r7.41 单独证伪，只能作辅助。因此五组 bootstrap、SAM/ASAM、训练、Android runtime 和生产替换继续关闭，不允许为回救 Ulm 调 r7.47 阈值、窗口、inpaint 或安全边距。

### 【未来帧 ego-trace 辅助教师初测】公开视频银标 r7.53

- 在 Ulm r7.52 失败后才提出并冻结回顾性诊断合同，故 Ulm 只准派生、不能回救 r7.50。固定使用未来 `1/2/3s`、ORB、ratio `.75`、RANSAC `3px`、未来画面 `(0.5, 0.9)` 锚点反投影以及半个物高扩张；禁止调参或把 mean 替代预设 median。合同 SHA256 由 sidecar 固定。
- 三个纯几何测试覆盖恒等、平移反投影和中心/侧向障碍命中。London 与 Ulm 每个事件帧都得到有效 trace；Ulm mean/median 均为 `0`，London mean 为 `.266667`，但 London median 仍为 `0`，固定的 `positive median > negative median` 门失败。报告 SHA256 `fc9f4b0a...362f3`。
- 未来帧反推路线能提供稀疏的正方向，说明“相机实际走过哪里”比单帧 SegFormer adaptive route 更接近所需变量；但它尚不足以稳定覆盖正事件，且使用未来信息只能做离线辅助教师。停止在本合同内换聚合器或调 ORB/RANSAC；后续若继续，应把它生成的连续 ego-trace/可通行中央缺口作为辅助目标，主门仍需新的前瞻来源验证。

### 【跨来源未来路线教师成立】公开视频银标 r7.54–r7.55

- r7.54 在读取其余来源输出前冻结 3 正/6 负事件和 mean 聚合，要求最弱正例严格高于最强负例。稀疏 ORB 教师的事件分数已完全分离，但 Japan 仅 `1/4` 帧得到有效匹配，覆盖门失败；报告 SHA256 `31ede4fc...f464`，不允许降低 12 matches/8 inliers 回救。
- r7.55 只把离线教师换成固定 OpenCV DIS medium 稠密 future-to-current flow，窗口、1/2/3 秒 horizon、下方中心锚点、半物高障碍扩张、mean 和完全分离门不变。9 个事件有效率全部 `1.0`；正例 Japan/Edmonton/London 分数 `.0833/.1875/.1667`，负例最高 Ulm `.0476`，完整门通过，报告 SHA256 `5b097322...3eea6`。
- 这是首个在真实 true-radial safe-lateral Ulm 压力下仍跨来源完全分离的路线变量，但依赖未来帧，只能生成训练期辅助标签，不能直接端侧推理，也不能回救 r7.50。

### 【因果外推与全局蒸馏失败】公开视频银标 r7.56–r7.58

- r7.56 固定用当前到过去 `1/2/3s` 的 DIS flow 在恒速假设下外推路线，覆盖率全为 `1.0`，但 Japan 为 `0`、London `.0333`，低于 Cape Town `.0370`；因果几何本身不能预测即将发生的路线选择，报告 SHA256 `42d3f616...e91a0`。
- r7.57 用当前检测几何、过去 flow 锚点及其 marker 距离做整来源留一、固定 ridge 的未来教师蒸馏。帧级 teacher-active AUROC `.8427` 过门，但事件级 Japan/London 为负预测、Bramwell 假高，报告 SHA256 `923f4583...50af`。
- r7.58 只增加冻结 DINOv2-S regional current-frame 特征，仍为整来源留一且不搜层/区域/alpha。帧级 AUROC `.8116` 过门，事件级仍被 Bramwell `.55`、Ulm `.21` 破坏，报告 SHA256 `744e27ba...78f84`。因此停止全局向量和 head 变体。

### 【空间距离场可学但事件 readout 未闭合】公开视频银标 r7.59–r7.60

- r7.59 将 r7.55 三个未来点合为 `16×16` 路线距离场，以 DINO patch token 的固定 96 维随机投影、patch 坐标和过去锚点做 source-isolated spatial ridge。route-band pixel AUROC `.9311`，证明空间辅助 target 可跨来源学习；但宽路线 mass 与 marker 重叠使 Ulm/Jakarta 假高并稀释 London，事件门失败，报告 SHA256 `547f087c...30471`。
- r7.60 在打开结果前冻结为 1/2/3 秒三张独立 heatmap，以各 horizon argmax 路点复用 r7.55 离散命中语义。pixel AUROC `.9161` 仍强，但 Japan 为 `0`、Ulm `.0476` 高于 London `.0333`，事件门仍失败，报告 SHA256 `c4571611...f401c`。
- 根因收敛：自动未来路线教师是有效监督，空间路线场也可学；失败来自当前仅 126 帧、静态 DINO patch + 过去锚点不足以预测跨来源未来路线选择，而不是线性 head、阈值或距离场 target 本身。下一步应扩大无需人工标注的连续视频路线辅助数据，并训练真正的短时序路线 head；像素分割继续辅助，SAM/ASAM 继续第三顺位，Android/生产仍关闭。

### 【自动时序路线辅助数据】公开视频银标 r7.61

- 在训练前冻结 `public_video_temporal_route_auxiliary_dataset_contract_r761.json`：从已绑定许可与完整特征哈希的连续视频中，只选当前有至少一个冻结 marker 检测、同时具备过去/未来 3 秒的时间点；每来源最多 128 条，来源内按排序后的 eligible timestamp 均匀确定性抽样。事件角色、风险标签、GPT/VLM 和人工标注均不参与。
- 固定 DIS medium future-to-current teacher 为每条样本生成 1/2/3 秒三个归一化路线锚点；训练时再确定性转为距离场。最终 manifest 共 `753` 条、`10` 个 source，SHA256 `05424d63fdb84cd384a75078c7df842329358e714bae3c41afe70bb97c938428`；item ID 全唯一、753 条 event_label 全为 null、753 条均有三个有效锚点。数据审计报告 SHA256 `86a186eb...d6e6`。
- 来源数量为 10 而非原事件门的 8，是因为 Edmonton/Kampala 与 Bramwell/Stegna 的已绑定联合特征报告各自包含两个独立 source；扩展只增加无事件标签路线监督，不让 context-only 来源获得事件门信用。London/Ulm/Edmonton 均按 128 上限截平，避免长视频支配。
- r7.61 只授权 source-isolated 的 temporal route auxiliary train-only prototype；不授权风险事件训练、校准、blind、Android 或生产替换。下一步输入应包含完整空间 past-flow field/短时序视觉，而不是 r7.56 的单个底部锚点。

### 【专用时序路线 head 仍未闭合事件定位】公开视频银标 r7.62–r7.64

- r7.62 固定输入为当前 RGB 3 通道、DINO patch 固定随机投影 32 通道、过去 1/2/3 秒完整 DIS flow 6 通道和坐标 2 通道，共 `43×16×16`；仅训练 `61,955` 参数卷积路线 head，10 个完整来源留出。753 条 marker-only 样本 seed 0 的 route-field AUROC `.91058`，但 localization `.1222`，Japan 事件为 `0`、Ulm `.143`，事件分离失败；报告 SHA256 `98ec998a...902cc`。
- r7.63 只做一次数据覆盖 OFAT：去掉“当前必须有 marker”限制，每来源最多 256，仍不读取事件标签。得到 2,102 条连续因果样本、10 个 source，manifest SHA256 `59923fbc...e0e`，全部 item 唯一、event label 为 null、三未来锚点完整。
- r7.64 保持与 r7.62 完全相同的模型/优化器/seed/留出，仅换用 2,102 条全连续帧。AUROC 提升到 `.91974`、MAE `.0420`，但 localization 仍只有 `.11787`，Japan `0`、London `.10`、Ulm `.14`；报告 SHA256 `eaa3c8ef...aa0c`。更多自动帧改善像素监督，仍未让因果表征恢复可靠事件路径位置，因此五组 seed 不启动。

### 【相对峰值风险轮廓冻结，等待真正新来源】公开视频银标 r7.65–r7.66

- r7.65 对 r7.64 冻结输出做只读不确定性审计。argmax、top-k 和 softmax mass 均未闭合；唯一在旧 3 正/6 负事件上完全分离的是 `exp(marker 扩张区最大路线 logit - 全局最大路线 logit)` 的三 horizon 均值：旧正例约 `.69–.76`，旧负例最高约 `.67`。这是明确的事后发现，报告 SHA256 `f4f535db...a9cf`，不能回写为历史成绩。
- r7.66 在任何新来源评分前冻结阈值 `.68`（旧最弱正与最强负的披露中点）、marker 扩张半个物高、r7.25 径向入口和 r7.30 九帧清除/单次提醒。冻结 seed-0 权重 SHA256 `18690367...7901`，合同 SHA256 `73076ff9...ecf3`；所有 r7.54–r7.65 source ID 与视频哈希只准派生，禁止获得前瞻信用。
- r7.66 仍是 diagnostic-only：必须用两个不同的新真实来源分别验证正事件 `>=.68 + 固定时序/生命周期` 与 true-radial safe-lateral 负例 `<.68`。未闭合前风险事件训练、五组短跑、校准、blind、Android runtime、生产替换和 SAM/ASAM 全部保持关闭。

### 【r7.66 第一轮新来源前瞻筛选】2026-07-19

- Commons CC0 Spiegelgasse 在下载和看帧前完成登记；121 个逐秒样本仅 4 个目标检出、0 个 r7.25 候选，故按预登记在视觉审查前拒绝。视频 SHA256 `2d68ba23...ae852`，特征 `52037f6c...915d`，候选报告 `35f26fbe...1a68`。
- Commons CC BY 3.0 Alicante 的完整 240p 视频 SHA256 `fb325c92...5445a`。先冻前 20 分钟，1,200 帧中 35 个 traffic cone、163 个目标检出但 0 候选；随后在未看帧的前提下披露顺序搜索并冻结剩余 2,581 帧，得到唯一候选 `2408–2424s`。
- 候选后视觉复核确认镜头连续，但模型把右侧餐厅露台、楼梯、栏杆和明亮装饰板误识别为逐渐放大的 barricade；现场没有真实施工标志，故在 r7.66 分数计算前拒绝，既不算正例，也不冒充 safe-lateral 负例。候选报告 SHA256 `aa61c079...4f56`。
- 新检索的 Pexels 2980886 在下载前按 item ID 与许可登记，但实际仅 `3.47s`；固定 1 秒采样最多 4 帧，结构上达不到 r7.25 至少 5 个接受样本，故在特征提取和看帧前拒绝。当前 r7.66 仍无合格新正/负事件，门禁状态不变。

### 【r7.66 第二轮：Bristol 全片无径向事件】2026-07-19

- 从 Commons 已由 YouTubeReviewBot 复核的 CC BY 3.0 POPtravel 城市长视频中，按公开路线文字选择 Bristol；在下载或看帧前固定前 20 分钟、r7.11 特征合同、r7.25 候选规则和 r7.66 禁止调参条款。240p 视频 SHA256 `1d2d74ad...dc76`。
- 前 20 分钟 1,200 个逐秒样本含 66 个 traffic-cone、188 个总目标检测，合同绑定特征 SHA256 `cfd6dbf0...78ae`，冻结 r7.25 候选为 0（`f8aac721...6cfb`）。随后只把仍未看过的 `20:00–end` 登记为自适应 proposal search，不给 blind/calibration 信用；2,161 个样本含 87 个 cone、344 个目标检测，特征 SHA256 `91b2f6cb...40a4`，候选仍为 0（`828b8186...7e0e`）。
- 整条 56 分钟视频共 3,361 个采样点、153 个 cone、532 个目标框，但没有一个满足固定径向持续接近合同的事件。按预注册在视觉复核和 r7.66 风险分数前拒绝，拒绝报告 SHA256 `92a0ccf7...ff88`。
- 新证据强化了当前瓶颈：不是缺少城市步行像素或 detector proposal，而是符合“真实标志 + 连续 ego walk + 径向接近 + 可判路径关系”的事件极稀疏。Bristol 不计正/负门信用；五组 bootstrap、SAM/ASAM、风险训练、Android 与生产仍关闭。

### 【Bangkok 真实同源压力对证伪相对峰值】公开视频银标 r7.66

- Commons Bangkok Modern Center 在下载和看帧前完成来源与前 20 分钟扫描登记；240p 视频 SHA256 `2d271158...fb572`。1,200 个逐秒样本含 185 个 traffic cone、353 个总目标，冻结出 `300–311s` 与 `328–339s` 两个 r7.25 事件。
- 候选后原时序复核把前者固定为真实径向但路线始终可从左侧安全通过的 provisional 负例，后者固定为进入触觉路线、需从左侧绕行的 provisional 正例。r7.66 对二者分别打分 `.80168/.87608`，两者都越过冻结 `.68`；安全侧向负例正式失败，禁止调阈值回救。正例按事件局部生命周期重放在 `328s` 单次提醒、`348s` clear 并通过，但同一视频不能独立闭合双来源门。
- 同一窗口的离线 r7.55 future-route teacher 为 `.11111/.33333`，方向和约 3 倍间隔正确；错误审计 SHA256 `c231b9ad...e5ec`。因此当前主要缺口是 marker 与因果路线的直接关系表示/readout，而不是继续堆普通城市步行视频。修正了 evaluator 将其他候选 reminder 混入所选事件的生命周期隔离错误；不改变 r7.66 负例失败结论。

### 【marker-conditioned 线性可分，但 bootstrap 优化不稳】公开视频银标 r7.67a–r7.68a

- r7.67 首次执行在 324 个 marker 帧中的 4 个亚 patch 小框上 fail closed；未产生分数。r7.67a 在结果前冻结逐 detection 的最近 patch 回退，不删帧、不改 target/split/alpha。132 维向量直接汇总 `43×16×16` 因果特征在 marker 区域的 mean/max/相对全局均值与几何。
- 10 个完整来源留一的 teacher-active pooled OOF AUROC 为 `.883995`，Bangkok 旁路正负均值 `.42093/.27090`、margin `.15003`；报告 SHA256 `e4ae6777...bc12`。这证明 marker-conditioned 表征含可用线性信号，但 pooled AUROC 仍有折间标尺风险，不能把结论夸大为“只有旧 readout 有问题”。
- 独立预跑审查后，r7.68a 改为 `(source,class)` 块 bootstrap、两类各 `.5` 权重、每次 bootstrap 后重算标准化和 prototype，并以 source-macro 指标授权；Bangkok 完全退出 pass/fail。五组 optimized balanced accuracy 中位数 `.7245`，低于 prototype-only `.8534`，`0/5` 通过；正类 recall 仅 `.39–.56`，负类 `.91–.94`。连续 `1/3、2/3、1` soft target 与固定 `target>0` 二元门不一致，80 步优化把正例压到 `.5` 以下；不得靠新阈值校准回救。报告 SHA256 `3e17d71a...f246`。

### 【距离场辅助监督单独 A/B 不保留】公开视频银标 r7.69

- 在五个相同 seed、相同 source-class bootstrap、相同 `Linear(132,8)-tanh` 初始化上，baseline 只用 fixed binary active BCE；treatment 唯一增加三时距 marker 掩码内 future-route Gaussian 最大值的 MSE，权重 `.2`。Bangkok 和事件标签均不参与。
- 辅助 head 的距离 MAE 五组均优于训练折常数（约 `.20–.27` 对 `.39–.40`），说明距离 target 可学；但 source-macro balanced 增量仅 `+.00046/+.00019/+.00172/-.00085/-.00595`，中位 `+.00019`，远低于冻结 `+.01` 门。报告 SHA256 `8edd27a4...d59a`，不保留辅助项、不搜索 loss 权重。
- 当前决策：prototype 方向值得保留为表示诊断，但 bootstrap 优化、距离辅助、SAM/ASAM、风险事件训练、Android runtime 和生产替换均未获授权。下一步把 Bangkok 两个真实窗口作为同 parent-source 反事实训练候选登记，并要求新的独立来源前瞻验证；中期风险轮廓/生命周期 head 只能先做接口/原型，不能晋级。

### 【真实同源 matched-contrast episode 已采集】公开视频银标 r7.70

- 冻结合同从 Bangkok 原视频精确提取 safe-lateral `300–311s` 与 route-intruding `328–339s`，各 12 个逐秒 PNG，共 24 帧且 24 个 SHA256 全唯一。manifest SHA256 `90287f20...2612`，报告 SHA256 `a3cef052...4c52`。
- 这是一组真实同 parent-source 的 matched contrast，不是合成干预或因果真值；监督仍为 hash-bound GPT/VLM provisional silver。所有派生帧必须随 `wikimedia_commons_poptravel_bangkok_modern_center_2019` 整体进入 train 或 holdout，禁止拆分泄漏。
- 当前只完成反事实 episode 采集并登记为 representation-training candidate；训练执行、独立评测、prospective、风险事件、校准、blind、Android 和生产授权仍全为 false。

### 【成对诊断修正了 target，但没有修复因果生命周期】公开视频银标 r7.71–r7.77

- r7.71 把每个 `teacher_hit_fraction > 0` 帧与同来源最近时刻的零命中帧配对，只学习差分排序。七个混合来源的中位 source AUROC `.85`、最弱 `.67385`、pair ordering `.792857`，说明同源局部对照能削弱跨来源标尺漂移；报告 SHA256 `caa37275...c6d`。
- r7.72 的五组 source-bootstrap 优化头虽然各自达到约 `.843–.882` 的中位 source AUROC，但总体仍失败：零训练 prototype 的中位 `.90833`，优化后仅 `.84889`，退化 `.05944`，超过允许 `.02`。因此不应再把算力投向 optimizer、SAM/ASAM 或阈值搜索；报告 SHA256 `be949071...cf7`。
- r7.73 将五个零训练 prototype 直接做因果风险轮廓和生命周期重放。Bangkok safe-lateral 在 `304s` 错开，route-intruding 直到 `337s` 才开，晚于 `336s` 最晚有效提醒。失败表明过去/当前输入把普通径向放大当风险，只在实际转向已经发生后才识别正例；报告 SHA256 `1615d161...b25`。
- r7.74 的几何匹配把中位时间间隔拉到 `108s`，中位 AUROC 降至 `.7733`；r7.75 在训练折内投影掉几何 nuisance 后中位 AUROC 再降至 `.675`、最弱 `.07843`。这两条捷径停止，不再继续匹配距离或残差化变体（报告 `48914904...6f7`、`7c03f19d...a2f`）。
- target 审计发现旧 `>0` 定义把任意一个未来 horizon 偶发命中也当正例：324 帧中 `0/3、1/3、2/3、3/3` 分别为 `268/15/10/31`。r7.76 固定改为至少 `2/3` horizon 命中，七来源中位 AUROC `.96774`、最弱 `.71959`、pair ordering `.94231`，证明离线特征对“持续未来路线命中”是可分的；报告 SHA256 `7b2f5d89...9d2`。
- 但 r7.77 只换多数 horizon target 后，Bangkok 因果重放仍与 r7.73 完全相同：safe `304s` 假开、positive `337s` 迟开；报告 SHA256 `c18cff4e...db1`。最终根因不再是线性可分性或 head 优化，而是当前 RGB/DINO/过去 flow 缺少可前瞻的路线选择/转向意图。后续优先增加独立真实 matched-radial episode 和可因果获得的路线意图输入；像素/距离场只作辅助，SAM/ASAM、Android 与默认模型继续关闭。

### 【独立 Düsseldorf 负例再次证伪因果 readout】公开视频银标 r7.78–r7.78a

- 在下载和看帧前登记 Commons Düsseldorf POPtravel 的 CC BY 3.0 条目、视频身份和前 20 分钟扫描范围；240p 视频 SHA256 `c9bedb93...fc5f`。冻结 1,200 个逐秒样本（85 cone、223 总目标）后得到 `117–127s` 与 `900–910s` 两个 r7.25 候选，候选报告 `529f29b5...fbd4`。
- 候选后原时序大模型复核：第一段是真围挡但与过街后右转同时发生，按 route-turn confound 拒绝；第二段是真红白围挡沿左侧路缘径向放大，拍摄者一直从右侧开放人行通道直行，冻结为独立 true-radial safe-lateral provisional 负例。
- 冻结 r7.66 在第二段得分 `.771165`，高于安全上限 `.68`，外部负例失败（`19ba400d...19d4`）。首次 r7.78 多数 prototype 执行因 902–903 秒 marker gap 暴露空 mask 隐含假设，在得分前 fail closed。
- r7.78a 在得分前单独冻结 gap-aware 合同：沿用 r7.30 同事件桥接，只让 9 个实际接受 marker 样本参与，且数量必须等于候选记录。多数 target prototype 仍在 `906s` 假开，峰值相对分数 `2.6933`，报告 `ab880466...54af`。
- 这将结论从 Bangkok 同源现象推进为独立来源复现：多数 horizon target 修正了离线 label noise，却没有让当前因果输入学会“围挡相对未来路线的位置”。下一模型实验必须显式增加路线意图/转向预测或成对 route-relative 监督；继续调 threshold、optimizer、SAM/ASAM 没有依据。

### 【因果 readout、分层告警与事件角色诊断】公开视频银标 r7.79–r7.81c

- r7.79 将同一冻结 `43×16×16` 因果缓存改为固定 `4×4` block pooling、全局 std 和多输出 ridge，直接预测未来 `1/2/3s` 六个 waypoint 坐标。平均定位误差 `.11583`，只比 r7.64 的 `.11787` 改善 `.00204`，且事件严格分离失败（Japan 为 `0`）；报告 `cc54e30...81e6`。因此主因不是 dense heatmap readout 或卷积 head 优化。
- r7.80 把 r7.25 径向入口降为无方向的环境注意，再仅用 r7.56 past-only ego trace 连续两秒升级为路线阻挡。positive 在 `336s` 恰好升级，Düsseldorf safe 不升级，但 Bangkok safe 在 `307s` 假升级，策略门仍失败。r7.80a 修正 lifecycle clear 字段读取后，三事件分别在 `320/348/919s` 清除，生命周期门恢复通过；总门仍因真实 safe false-upgrade 而为 false（`6e466af8...95e9`）。
- r7.81c 用 8 个有 marker 的事件、106 帧做整来源留一事件角色 ridge；事件 AUROC `0`、balanced accuracy `.2`、positive recall `0`。当前小样本学习的是来源相关反向捷径，不能靠翻转分数或阈值修补；报告 `3f1a69ff...beb3`。r7.81 系列仅为失败诊断，不主张干净的 prospective 信用。

### 【Cologne/Cardiff 扩源与高召回事件复核】公开视频银标 r7.82–r7.84

- Cologne CC BY 3.0 全视频先登记后下载；前 40 分钟共 2,400 个逐秒样本、40 个 cone、190 个目标检测，固定 r7.25 候选为 0，按预视觉规则停止。
- Cardiff CC BY 3.0 全片 3,241 个逐秒样本包含 270 个 cone、875 个目标检测，但固定 r7.25 候选仍为 0。诊断型五样本局部 gate 找到 3 个复核窗口；宽度 `7/9/12` 均为 0，说明它们不能倒算成 canonical r7.25 信用。
- 哈希绑定的 GPT/VLM 原时序复核保留两个真实黄色地面警示牌为 provisional route-intrusion/right-side-pass 正候选，排除路口锥桶右转窗口。复核报告 `088bad8f...17a9`；两个保留项仍来自同一 Cardiff parent source，只进入隔离的事件角色候选池，不增加独立来源数，也不授权训练、校准、blind、Android 或生产替换。
- 当前最有效的新路线不是继续优化同一 head，而是积累多个独立来源、同机制的 matched-radial 事件，并把任务拆成“通用早期环境注意 + 有因果证据才方向升级”。SAM/ASAM 仍为第三顺位。

### 【把最终安全结果从早期告警 target 中剥离】公开视频银标 r7.85

- r7.85 冻结 no-change 因果可行动性状态：径向入口为 `context_attention`；past-only trace 连续两秒达到 `1/3` 为 `intervention_needed`；干预后连续两秒低于 `1/3` 为 `route_clear`。它不使用未来帧预测当前标签，最终是否安全通过只保留为 response/lifecycle 属性。
- 三事件重放得到三种不同状态：Bangkok `300–311s` 在 `307s` 进入干预、`311s` 路线清空，属于 `intervention_then_route_clear`；Bangkok 正例在 `336s` 进入且到 marker 结束仍持续，属于 `persistent_intervention`；Düsseldorf 只属于 `context_only`。冻结预期全部通过，报告 `0120009d...07e5`。
- 这证明当前至少一个“安全负例假阳性”是合同矛盾：用户后来成功绕开，不能反推出绕开前不应提醒。相应地，银标 v3 强制 `silver_actionability` 与 `past_or_current_only`，禁止把 `safe_pass/route_changed` 反写为 `candidate_no_alert`。
- 这解决的是 target 语义和产品状态机，不是独立模型验证。训练仍需跨来源事件，方向指令仍需单独的可通行侧证据；Android 与默认模型保持不变。

### 【根因闭合到显式路线意图，而非继续调 head】公开视频银标 r7.86–r7.95

- r7.86–r7.89 将 16 个事件统一为 current/past-only actionability：4 个干预事件来自 Bangkok/Edmonton/Ulm 三个独立来源，12 个 context 事件来自 9 个来源；旧 route-role 标签有 `25%` 与因果状态不一致。Cardiff 两个视觉正候选均被 past-only trace 判为 context，Ulm c01 则补足第三个真实干预来源。
- r7.90 的事件均值 probe AUROC `.0795`、干预召回 `0`；r7.91 在完全相同输入上改为帧级风险轮廓 + 固定生命周期后 AUROC `.3409`、balanced `.5227`，方向更合理但仍未跨来源可分。停止 prototype/bootstrap、距离场权重与 optimizer 变体。
- r7.92–r7.93 引入独立、CC BY-NC 4.0 的 ADVIO Office 03 sequence 15，仅作非商用路线意图辅助诊断。官方 MD5 匹配；共享同步时长 `51.43s`，gyro/pose 约 `100Hz`、视频时间戳约 `60Hz`，完整性审计全通过。
- r7.94 用过去 `.5s` gyro/accelerometer 预测未来 `.25–1.0s` 路径转向，连续块 OOF AUROC `.4770`；r7.95 只换旋转不变物理特征仍为 `.4746`。类别为 `201/199`，失败不是不平衡，也不是 signed-axis head 表示。
- r7.96 又把 target 改为截至当前时刻已经发生的路径转向，保持旋转不变 IMU、ridge、连续块切分和 guard；AUROC `.3465`、balanced `.3878`，仍失败。手机自身旋转不等于世界坐标中的路线转向，未做 device-to-world/route 对齐的 IMU 连确认器都不能直接使用。
- 新结论：中期“风险轮廓 + 生命周期头”应优先增加显式 route-intent token（导航规划/用户选择）；无意图时只输出无方向 context。IMU/VIO 只有在姿态对齐先独立过门后才可辅助，当前不新增 Android sensor 接口。详见 `docs/PUBLIC_VISUAL_INERTIAL_ROUTE_INTENT_2026-07-19.md`。Android 默认、生产训练、校准与 blind 继续关闭；像素/距离场辅助、SAM/ASAM 顺位不变。

### 【显式 route intent 解除核心不可辨识性】公开视频银标 r7.97a

- 冻结 r7.55 future-route teacher 只作为“外部导航/用户已给路线”的 oracle 代理，在 r7.89 的 16 个事件、11 个来源上直接计算路线—marker 交叠；不训练 head，不允许未来帧进入 eval/runtime，连续两秒至少 `1/3` 交叠才升级。
- 结果 intervention recall `4/4=1.0`、context recall `10/12=.8333`、balanced `.9167`，全部有效帧覆盖 `1.0`。r7.97a 勘误版重算 12 份 feature report 与 13 个本地视频 SHA，指标不变，报告 `ae019fa9...1e85`。相对纯视觉生命周期 `.5227` 的巨大提升直接证明缺失变量是显式路线意图。
- Edmonton/Ulm c01 与因果 open 同时，Bangkok 两段提前 `1s/4s`。London 与 Ulm c03 的两个 disagreement 是加入计划信息后出现的连续未来交叠，不能反写 current/past-only 旧标签，也不能冒充生产假阳性。
- 解法固定为“视觉风险轮廓 + 外部 route field + 确定性交叠/lifecycle”；actionability 不再是脱离路线的全局分类 head。route unknown 时只允许 context。接口和晋级门见 `docs/EXPLICIT_ROUTE_INTENT_MODEL_CONTRACT_2026-07-19.md`。
- r7.98 已把确定性交叠生命周期移植为 benchmark-only Kotlin，并在 SM-S9280/API 36 上 `3/3` instrumentation 通过；这证明端侧实现可行，但没有接入 App，也不替代真实 route provider 的独立验证。

### 【三态显式选择不是 future-flow x；转向与障碍必须因果对齐】公开视频银标 r7.99–r8.07

- r7.99 用 future-route mean-x 代理三态并换成固定模板，既有 16 事件 balanced `1.0`；r7.99a 发现四个 intervention 全是 `STRAIGHT`，LEFT/RIGHT intervention 均为 0，禁止宣称完整三态 provider。
- r8.00 从 753 个 label-free r7.61 行找到 5 个左右候选；r8.02 连续复核全部拒绝，并确认 future lower-center flow correspondence 不是 turn class，会被运动物体、平移和 detector 假框污染。
- r8.03 改用上半画面背景 yaw，找到 13 LEFT/17 RIGHT；r8.05 只保留 3 个连续模板交叠；r8.07 仍全部拒绝：Kampala 是真实施工平行边界，Bramwell 是墙体误检。现有 LEFT/RIGHT intervention 覆盖仍为 `0/0`。
- 新创意收敛为两条独立输入：外部/用户 route choice 决定方向，视觉风险轮廓只决定该方向是否被阻断。公开视频检索必须寻找“真实转向 + 同一障碍进入所选分支 + 停步/改道”三者同时出现；合成反事实只能作为 train-only augmentation，不能填真实 provider/eval 门。

### 【许可通过也不等于事件通过】公开视频定向发现 r8.08–r8.09

- 获取前冻结 Commons/Vimeo 各三条查询和因果准入条件；Python TLS 未产生响应后，只用 Windows TLS 原样重放 Commons 查询一次。
- Commons 唯一候选是警方执法/dashcam 误项，未下载；Vimeo Burwell 条目级许可为 CC BY 3.0，但全片复核显示为标题卡、地图、效果图、采访和硬切组成的工程新闻片，不是连续行人 POV。
- 新增事件为 0，LEFT/RIGHT intervention 仍为 `0/0`。下一步可换独立公开来源，或构造严格 train-only 合成反事实改善风险轮廓；后者不能填真实 provider/eval 覆盖。

- r8.10–r8.11 进一步证明普通档案全文搜索会把长描述中的分散关键词拼成高分假候选；Internet Archive 三项均为历史影片并在下载前拒绝。后续检索应优先页面级精确短语、连续 walking-tour 来源及条目级许可，而不是继续扩大通用全文词表。

### 【不是分类 head，而是路线上的连续风险场】r8.12–r8.18

- 同一张合成障碍图分别绑定 LEFT/STRAIGHT/RIGHT；exact route-overlap 的线性 head 为 `1.0`，冻结 DINO 二值 risk map 只有 `.6249`，因此 head 可解、表示不可解。
- 把两个障碍族铺到三个父来源后二值 teacher 仍只有 `.6332`；单纯加外观样本没有消除来源偏置。
- 唯一改成 bbox 距离场辅助监督，路线条件 BA 升到 `.9156`，固定两帧 open 生命周期 `.9429`；全局 readout 仍接近随机。这是目前最强的结构证据：主干输出局部连续风险场，外部 route field 做条件化，生命周期只解码 open/clear。
- 五组短跑方差很低但预注册门失败，说明优化稳定性不是当前主矛盾。保留 deterministic ridge 诊断，不做 SAM/ASAM；下一步把距离场风险表示带到真实 provisional 事件，而不是继续在合成集上刷 head。

### 【距离场真实迁移只部分成立，根因落到真实风险表征】r8.19–r8.23

- r8.19 把 r8.16 的合成 bbox 距离场 teacher 原样迁到 r7.89 的 16 个真实 provisional 事件，并用 r7.97a 路线锚点作离线 oracle proxy、完整 parent source 留一。路线 readout balanced `.7083`，高于全局 `.5833` 和旧 current/past-only `.3182`，干预召回 `.75`；但 context recall 仅 `.6667`，预注册门失败（`331a2f5d...fad60`）。这证明连续距离场与路线条件化有真实迁移信号，但还不能稳定区分真实路线障碍与 context。
- r8.20 只拼接七维全场背景统计，balanced 降到 `.4583`，context recall 不变；线性“路线减背景”不是解法（`ffe88d21...bf570`）。
- r8.21 用大模型生成锥桶资产；无 alpha 的棋盘格首稿和有洋红边的首个抠图均拒绝。通过确定性颜色键、完整视觉和几何 QA 的 v2 扩成 108 图/324 路线样本，只授权 train-only auxiliary。
- r8.22 只给 teacher 增加跨三父场景的 traffic-cone 族，真实路线 balanced 反降到 `.5833`、干预召回 `.5`（`3f16e70b...691f1`）。简单堆单个合成类别会改变 teacher 方向，不能当作真实语义覆盖修复。
- r8.23 回到最佳 r8.19 teacher，用 218 个真实 provisional 帧状态、来源/类别/事件/帧分层等权 ridge 和固定两帧 open/clear。事件干预召回仍 `.75`，但 context recall `.3333`、balanced `.5417`，帧级 balanced `.4808`（`6c098d1f...815b0`）。事件均值和生命周期不是主瓶颈；风险字段在大量真实 context 上持续误激活。
- 当前最可信路线：停止 head、背景拼接、单资产扩类和 SAM/ASAM；下一项 OFAT 应直接提升真实局部风险表示，例如使用训练折内真实 provisional bbox/marker 的距离场辅助或更强局部语义 teacher，并继续完整 parent-source 隔离。显式 route provider 仍是必要输入；上述 future anchors 不能进入 runtime/eval 声明。

### 【真实 marker 距离场也不够，目标必须显式 route-relative】r8.24

- r8.24 在每个 LOSO 训练折内，使用真实 provisional marker bbox 构造连续距离场；near/far、来源、帧分层等权，held-out 来源 bbox 完全不参与 teacher。共 218 帧，其中 28 帧为无检测全零场。
- 真实 marker teacher 的全局/路线 balanced 为 `.6250/.5833`，路线 context recall 仅 `.4167`，低于 r8.19 合成 teacher 的 `.7083`；所有冻结提升门失败（`7750e172...d0013`）。
- 结论不是“需要更多 object box”：更贴近真实域地学习“哪里有 marker”仍无法决定它是否阻断所选路线。下一模型原型必须在表示层显式输入 route-distance/route field，并以 route-relative risk 为训练目标；先做训练折内确定性线性/双线性交互 probe，再决定是否训练小型 interaction head。SAM/ASAM、阈值和更多孤立资产族继续停止。

### 【route-field × raw patch 仍不可分，主线回到 detector 几何】r8.25

- r8.25 删除 object teacher，直接把三点 route polyline 转成宽度 `1.5 patch` 的连续场，与冻结 DINO patch 的固定 32 维投影做路线内均值、路线外均值和 contrast；均匀 `.5` 场为同维负控，16 事件/11 来源完整 LOSO。
- 均匀/路线交互 balanced 为 `.4583/.5000`，路线 gain 仅 `.0417`；context recall `.75`，但 intervention recall `.25`，总门失败（`ec1b3684...93b60`）。禁止搜索投影维度、sigma、ridge 或追加非线性 head 回救。
- 结合 r7.97a 的 detector bbox + 显式路线几何 `.9167`，当前工程结论收敛：核心 actionability 不应依赖冻结 DINO 自行恢复障碍语义；优先实现/验证外部 route provider、现有 detector boxes 的确定性交叠与 lifecycle。像素分割/风险场只作辅助召回或边界，不再作为主决策表示；SAM/ASAM 继续关闭。

### 【端侧几何已闭环，但真实 route provider 仍是主瓶颈】r8.26–r8.26a

- r8.26 将三点显式路线、归一化 detector bbox、`1/3` 命中阈值和既有两帧 open/clear 生命周期串成 benchmark-only Kotlin；首轮真机 9 项通过，但 218 帧全量 conformance 发现 `59/654` anchor hit 与 `30/218` frame score 不一致，因此主动拒绝首轮结论。
- 根因是非正方形画面的单位换算：像素高度扩张必须分别除以 frame width/height，不能把 normalized height 同时加到 x/y。r8.26a 加入 frame pixel dimensions 和非正方形回归，修复后全量为 `0/654`、`0/218` mismatch，SM-S9280/API 36 为 `OK (10 tests)`。
- 这条路线现在不是“一个漂亮的离线分数”，而是已有 hash-bound Python oracle、全帧数学一致性和真机实现三层证据；所以继续调 DINO/head/SAM/ASAM 已无优先级。下一步唯一主线是隔离的真实非未来 route provider，以及真实 LEFT/RIGHT intervention 覆盖；在此之前不接 App/default runtime。

### 【外部非未来路线已经有 Android fail-closed 边界】r8.27–r8.27a

- 新增 benchmark-only Android payload provider：必须带 provider/projection receipt、1/2/3 秒 waypoint、置信度与一秒内时效；risk-model 自推、future-video、未来签发、过期、低置信和畸形 payload 全部拒绝。
- r8.27 的唯一失败是测试夹具 `removeExtra()` 返回类型误用；r8.27a 不改 policy/provider，仅修夹具后离线 build 成功，真机 provider+geometry+lifecycle `16/16` 通过。
- 当前剩余问题进一步缩小：接口和端侧确定性链已通，下一缺口是一个真实导航/用户输入源产生准确 camera-space route 的外部验证，以及真实 LEFT/RIGHT obstruction episode；这不是继续训练视觉 head 能解决的。

### 【世界路线投影数学已补齐】r8.28–r8.28a

- 新增 ENU 世界路线、外部 world-to-camera 姿态回执和针孔内参到相机归一化 waypoint 的确定性投影；过期/未来/低置信姿态、非正交矩阵、相机后方或画面外点均 fail-closed。
- r8.28 的有效夹具少写一个矩阵元素而被正确拒绝；r8.28a 只修 3×3 identity fixture，真机投影+provider+geometry+lifecycle `22/22` 通过。
- 当前已没有必要用视觉模型猜路线：剩余决定性工作是接入并测量真实 pose/calibration/navigation source 的误差，以及获得真实 LEFT/RIGHT obstruction episode；在这些证据前不接默认 App。

### 【真机姿态、精确内参与坐标变换元数据均可用】r8.29–r8.30

- SM-S9280 有两个后置 camera 和约 48Hz 的真实 rotation-vector 流；119 个样本最大四元数范数误差 `4.16e-8`，说明姿态数据通道本身可用。
- r8.29d 在无 `CAMERA` 权限的 instrumentation context 中读到 exact intrinsic/distortion 为 null；Android 官方合同明确这些键需要该权限，因此该 null 不是设备缺失证据。r8.30 改用已授权 target context 后，两颗后摄均公开精确 intrinsic、distortion、lens pose rotation/translation；主摄 `fx=2766.12px, fy=2771.18px`，推翻了“只能使用推导内参”的旧判断。
- 两颗相机的 timestamp source 均为 `REALTIME`，可与 rotation-vector 的系统时间基准配对；lens pose rotation 又直接给出 Android sensor 坐标到 camera-aligned 坐标的旋转。下一确定性实现采用 `R_camera_from_world = R_lens_pose * transpose(R_device_to_world)`，不先要求独立外参标定。
- r8.31 已把该公式实现为 benchmark-only composer，并在 SM-S9280/API 36 通过 `OK (5 tests)`：identity、device-to-world 取逆顺序、r8.30 主摄四元数、现有 world projector 衔接和非法输入 fail-closed 均通过。输出刻意停在 raw camera-aligned sensor 坐标，不假装已经完成 CameraX 帧映射。
- r8.32 真机确认生产匹配 CameraX 流稳定为 camera 0、`640×480`、完整 crop、`rotationDegrees=90`，无隐藏裁剪；时间戳严格递增，约 15FPS，回调相对 capture timestamp 中位延迟 `77.43ms`。
- r8.33 直接读取 CameraX 官方 `sensorToBufferTransformMatrix`：4080×3060 active array 到 640×480 buffer 是无平移/剪切的 `0.15686275` 均匀缩放；精确主点最终映射到 480×640 display 的 `(238.99, 320.21)`。
- r8.34 将 sensor intrinsics、sensor-to-buffer、90°像素/相机轴旋转和 r8.31 pose 组合接回 world projector，SM-S9280/API 36 `OK (4 tests)`；确定性坐标公式缺口已关闭。
- r8.35 同时采集生产匹配 CameraX 帧与 rotation vector：30/30 帧均有前后姿态样本，最近样本中位差 `4.81ms`、最大 `9.67ms`，bracket 最大跨度 `19.756ms`，`OK (1 test)`；按 capture timestamp 做 quaternion SLERP 的数据条件成立。
- r8.36 自动保存真实帧并以重力消失点对比 Hough 场景线；原冻结高阈值在昏暗天花板画面得到 0 candidate，只能判不具信息量。r8.36a 对同一冻结帧做公开的 CLAHE 低照复筛，得到 89 candidate/10 aligned，最小角误差 `0.42°`，但 aligned length `8.78% < 10%`，按冻结门正式失败，不事后改阈值。
- 失败诊断不是坐标公式已被推翻，而是“所有场景长线应由世界竖直族主导”的验证假设不成立：该帧多数是水平天花板边界。下一 OFAT 改为自动短振动下的 IMU 旋转单应矩阵 vs LK 光流，不再依赖人工或场景语义。
- 在真实旋转重投影误差及真实 LEFT/RIGHT 事件通过前，不能接入 App 或授权生产。

### 【提案】路线条件化无类别风险场 RC-OARF v1

- 完整方案已保存为 [路线条件化无类别风险场方案](docs/ROUTE_CONDITIONED_OBJECT_AGNOSTIC_RISK_FIELD_PLAN_2026-07-20.md)。状态为 `proposal_only / not_started / do_not_replace_default_model`。
- 核心重定义：给定显式、非未来的用户路线场，直接预测局部连续风险场与因果生命周期；物体类别只用于解释，不再拥有安全决策权。允许未来完全移除 YOLO 安全主链，也允许保留低频语义 Adapter，但两种模式的事件结果必须一致。
- 与现有路线隔离：公共银标主线继续验证 detector bbox + explicit route geometry；Corridor-Causal Student 继续验证 YOLO/空间特征 + 运动补偿 + causal TCN；本方案独立验证无需 box 类别前提的 dense route-relative risk，不复用另外两线的数据、权重、指标和晋级结论。
- 前置事实：现有 frozen DINO route-field × patch、真实 marker 距离场和多种全局 head 已经失败或只部分迁移，因此本方案不得直接启动学生训练。首个硬门是非 blind、真实连续事件上的教师上界，并必须包含 uniform/shuffled route 负控。
- 工程前置：先让生产与 benchmark 共用同一 Assist Decision Module，再建立由至少 YOLO 与 RC-OARF 两个 Adapter 证明的模型无关感知证据 seam，避免密集风险结果被压成 `Detection` 框，也避免 benchmark 与 App 行为漂移。
- 停止原则：教师上界不优于 detector geometry、显式路线不可获得、增益只来自同源或合成、事件误提醒/清除不改善、INT8 或同机 P95 失败，任一出现即保存负向报告并停止，不转向大规模 backbone、阈值、optimizer 或 SAM/ASAM 搜索。

### 【进行中】RCLE Phase B 真实 positive-approach 数据角色准入与并行隔离

- 正确顺序固定为：`approach-role admission → implementation/performance qualification → formal execution decision`。数据角色准入只允许 metadata、pose、depth 或其他非 RGB-algorithm 几何真值；在 claim、来源哈希和门槛冻结前不得访问候选 payload，也不得读取 RGB algorithm outcome、调参或因失败换源/换窗。
- 并行工作只在证据角色和任务身份互不重叠时提高效率。同一协议版本不得并行创建两份 claim、选择两个候选或让两个进程写同一路径；若另一进程已经占用 `RCLE_PHASE_B_REAL_POSITIVE_APPROACH_ROLE_ADMISSION_R1`，本进程只能做只读独立审查或等待明确交接。
- 候选必须建立 content identity、ancestry、independence group、access/reuse policy，并证明不与当前协议明确烧掉的 sequence/capture 及未来 confirmation partition 重叠。跨项目访问按实际读取的 metadata、geometry、RGB、其他算法 outcome、claim-relevant outcome 和 selection/tuning influence 分级；不能因换研究名称恢复为 pristine/unseen，但可以在披露后转为 discovery、regression、source characterization 或 `DISCLOSED_CROSS_PROGRAM_APPROACH_CANARY`。文件曾存在于本地或 USTRF 看过不再自动判整个数据族死亡。
- 当前 R1 已预访问冻结唯一候选 ETH3D `sofa_3`；若候选访问、格式或几何门失败，保持冻结的 HOLD/INVALID 终态，不转向 `sofa_4`、OpenLORIS、镜像或其他窗口。只有 role admission 为 `REAL_POSITIVE_APPROACH_ROLE_ADMITTED / VALID`，才允许另立 performance qualification 任务。
