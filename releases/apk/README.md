# BlindAssist APK 收据目录

原始 APK 不再进入 Git。经 SHA256 核验的交付 APK 保存在：

```text
E:\linnan\blind-assist-apk-archive\apks
```

历史 Git 里程碑的文件名、大小和 SHA256 位于 [docs/APK_ARCHIVE.md](../../docs/APK_ARCHIVE.md)。未来 `scripts/archive_apk.ps1 -Milestone` 只在本目录生成小型 JSON 收据，记录外部归档路径与 SHA256。
