# 2026-08 开发记录

## 2026-08-09

### DepthART task-preserving deployment R2 冻结合同

- 修复算法 current 中仍指向 `TWO_STAGE_NUMERICAL_REPAIR_SUCCESSOR` 的残留冲突；strict G4-D 保持不可变负终态，不再继续 custom 化标准 Conv/Norm/activation。
- 新立 `DEPTHART_TASK_PRESERVING_DEPLOYMENT_R2`：以 canonical FP32 reference 对比一个 outcome 前冻结的 HTP-friendly candidate，同时要求 clearance、false-clear、false-block、temporal、transition 与 UNKNOWN coverage 的绝对门和非劣门。
- R2 当前为 `PROTOCOL_FROZEN / EXECUTION_NOT_ACTIVATED / NO_OUTCOME_ACCESSED`。新增 pre-outcome validator，只校验新独立 parent/session cohort、候选/reference 身份和门槛未漂移；不读取输出、不签署质量，也不授权执行。
- 任务质量 FAIL 即停止；只有质量 PASS 才可评价该候选自己的 partition、latency、RAM 和 thermal。DA2 replacement、默认 App、生产与 safety 继续 `NOT_AUTHORIZED`。
- 根据 task-preserving 目标补立 Development-only D0 precision screen：FP16、W8A16、INT8 三臂共用 G4-C source/control、calibration roster 与任务后处理，按技术前门→任务质量→仅合格臂 performance 的固定顺序只选一个 R2 候选。既有 G4-C 图不再被误写成已选候选。
- 从 Apple 官方 5,047-row split 元数据按固定哈希规则锁定 8 个 Validation visit/session；相对冻结 Git snapshot 中 81 个既有 ARKit identity 零重叠。32 个 RGB/depth/intrinsics/confidence asset HEAD 全为 200，总计 `2,725,771,890` bytes，payload 零读取。
- 新 roster 尚未取得显式 ARKitScenes license scope extension，因此保持 metadata-only；未下载媒体、未访问 model outcome，也未生成 activation manifest。它被明确排除于 D0 calibration 与候选选择。
- D0 FP16 按冻结 recipe 成功生成 DLC、SM8650/v75 context 并 saved-context 执行，但 DLC 将 5×SelectiveScan 与 23×LayerNorm custom tensors 全部改为 Float16，而冻结 kernel contract 为 Float32；故技术前门 fail closed，不进入任务质量/性能，也不修改 kernel 救臂。W8A16/INT8 继续共享一个新冻结的 Development calibration roster。
- W8A16/INT8 共用 calibration roster 已在访问模型 outcome 前冻结：从两个本地 TUM sequence 各按 SHA-256 顺序取 8 帧，并排除既有 consumed R0 120 rows，重叠为 0；只授权 quantizer calibration，不提供任务质量或 R2 authority。
- D0 W8A16/INT8 均在 QAIRT Windows host quantizer 构图时因 `Invalid OpPackage SelectiveScan / Invalid QnnModel constructed` 关闭；当前缺少 compatible host CPU custom-op runtime。连同 FP16 dtype mismatch，三臂均未进入 task outcome/performance，D0 终态为 `NO_ELIGIBLE_ARM`。task-preserving 总方向未关闭；唯一 successor 改为另立 fixed-mixed D1，并先冻结产品几何/task postprocess 与新 Development roster。
- D1 前置检查确认既有 G4-C 是固定 `448×448` numerical-canary 图，不能直接承载矩形 CameraX/TUM left-center-right FOV authority；D1 必须先冻结产品纵横比、resize/crop、intrinsics/truth 对齐与任务后处理，再重建图。

### DepthART 两段式数值定位与 G4-D 负终态

- 冻结同一程序化 RGB canary（SHA-256 `4500A709...63ED`）和 `rtol=3e-5 / atol=3e-6`。关闭 CUDA/CuDNN TF32 后，PyTorch↔canonical ONNX 最终 depth 为 `max_abs=1.40667e-5`，PASS。
- SM8650/HTP v75 侧依次新增 correctness-first float32 PatchConv、BatchNorm 与 exact GELU custom families。局部前缀均恢复 PASS；将 `1×PatchConv + 123×BatchNorm + 27×GELU` 放回整图后，ONNX↔HTP 降至 `max_abs=0.0272727`，但仍 FAIL。
- 修复前缀的下一首因定位到第二个标准 Conv（`max_abs=0.00568485`）。结合 QAIRT 2.47 官方 HTP floating path 的 16-bit underlying math 边界，当前标准 float 路线记为 `CURRENT_QAIRT_2_47_SM8650_HTP_STANDARD_FLOAT_PATH_STRICT_G4D_NOT_SUPPORTED`；不外推到近完整 custom-float32 engine。
- 当前图重新生成 saved context；direct DLC↔context 的 200,704 个 float32 输出 bit-exact。三项门为 `PASS / FAIL / PASS`，所以 G4-D 总门 FAIL；G4-E partition purity、G4-F performance 保持 `NOT_EVALUATED`，DA2 replacement 保持 `NOT_AUTHORIZED`。
- 新增冻结 evaluator、节点族 rewriters、HTP reference kernels 和定向测试；设备二进制、DLC、context、raw outputs 与 receipt 只保留在 `artifacts.local/`。未修改 Android runtime、默认 App 或生产模型资产。
- Goal Copilot 新建 source-disjoint T-LESS/BOP19 hard-error unary probe：DINOv2-S baseline=`27/30`，冻结 6 hard/6 control 后，唯一 PDM PerMIR arm 为 `1 rescue / 4 collateral / control retention=4/6`，未过预注册门并以 `PDM_UNARY_MIXED_RESCUE_WITH_COLLATERAL_DEVELOPMENT` 收口；8 个 absence 均因无 NONE threshold 保持 `NOT_EVALUABLE`，未授权 P1 或 App。
