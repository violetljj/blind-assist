# Project state

Updated: 2026-08-31

BlindAssist is a runnable Android showcase research prototype. The default
policy is effect-first: demonstrate a genuine, visible effect in a controlled
setup, state the evidence source and metric honestly, and keep only checks that
protect interpretation or change the next decision.

This page is the compact committed authority surface. Uncommitted local work is
work in progress and does not change the project state.

## Load order

1. Read this page for routing and the project-wide boundary.
2. Read [CURRENT_DECISION.md](CURRENT_DECISION.md) for the active cross-route
   decisions and stop conditions.
3. Open exactly one route current:
   - [L10-R0 current](../research/active/l10-r0/CURRENT.md)
   - [DTR-R2 current](../research/active/dtr-r0/CURRENT.md)
4. Open a route `README.md`, result, protocol, code, or test only when the task
   needs that detail.

Closed prose is not current authority. Use [history-index.md](history-index.md),
the route ledger, result files, and Git history for audit or reproduction.

## Active routes

### L10-R0 goal completion

Status: `L10_R0_ACTIVE`

The controller, active-observation contract, and causal handoff guard remain
implemented. PanoLab established a `4/4` entrance-ray recovery mechanism, but
not a pixel portal. The SEVN portal-private PP-OCRv6 medium witness passed every
frozen gate on 40 further fresh PAN episodes: visible-number exact OCR improved
`22/37 -> 29/37`, binding improved `16/0/24 -> 21/0/19`
correct/wrong/`UNKNOWN`, and all five new bindings were correct. The panel has
zero overlap against all 205 prior addresses and 220 prior frames, but remains
same-source SEVN. The latest 3RScan spatial-mask successor reached `0.5403`
complete IoU but remained below its `60%` ceiling-retention gate and confused an
overlapping doorframe.

The registered-geometry line now has both a positive generalization and a
clear sensor boundary. On a scan-family-disjoint 3RScan cohort, unchanged
positive planar-extent support improved rank-only `34/2/0` to `34/0/0`, F1
`1.0`, with exact unmatched sets in `15/15` scenarios. Provider-disjoint
SceneNN then supplied four doors and synchronized RGB-D. Single frames were
not evaluable for all targets, while exact three-frame registration made all
eight observations evaluable. On those partial observed surfaces, rank-only
remained strong at `34/3/0`, F1 `0.957746`, but the complete-surface extent
rule regressed to `25/2/9`, F1 `0.819672`. Threshold-free Hue consensus and
frozen DINOv2-S foreground-feature consensus also lost most true matches
(`2/0/32` and `7/1/27`). These consumed-scene appearance arms and partial-view
convex-hull veto are closed. A separate four-scene fresh test of EfficientLoFTR
local correspondence plus hard NONE improved same-crop DINOv2 F1
`0.1875 -> 0.349206`, but produced `11/18/23`, supported only `2/4` true
diagonals, and recovered `0/15` exact scenarios. That fixed homography-inlier
gate is also closed; the rank-only SceneNN result remains Development component
evidence, not phone-side identity or unknown rejection. The next admitted
challenger is calibrated dense certainty with spatially balanced matches on
fresh targets, not threshold tuning on these consumed doors.

The PanoLab candidate-routing line separates geometry, lexical evidence,
appearance, and abstention. After a fully selected-pixel-unseen three-producer
panel exposed appearance `1/0/2` and exact lexical `0/0/3`, one conservative
distinctive-token successor recovered the consumed panel to `3/0/0` with
`0/30` wrong-target matches. Its separately frozen confirmation used two more
target-way/item-disjoint, pixel- and OCR-unseen targets: lexical evidence
recovered Maison de Quartier, conditional appearance recovered Monoprix, and
the combined 13-target router reached `2/0/0`, with `0/24` wrong-target lexical
matches, `0/12` appearance wrong-goal candidates, and zero ownership bindings.
Four exact-roster-absent controls across three cities stayed `UNKNOWN` `4/4`
with `0/52` fuzzy-token matches. This is fresh same-provider Development
routing evidence, not open-world confirmation. A posthoc progressive replay
retained `2/2` while reducing observed frames `6 -> 2`, but has no fresh latency
authority. The first provider/city-disjoint OCR transfer on four previously
human-reviewed Mapillary panoramas then produced `0/0/4/0`
correct/wrong/`UNKNOWN`/ambiguous and `0/64` wrong-target matches. This freezes
a cross-provider text-observation reachability gap; the MR images cannot be
used to loosen the edit rule or tune OCR preprocessing.

The KartaView/Jakarta materialization was also frozen as source-not-evaluable:
`0/6` query frames visibly contained the selected target. Review of the official
public MCP implementation did not repair that reachability gap because
`nearby_photos` is geometric only and `object_search` requires a user-owned
token. A separate RoadTextVQA driving-sequence line then upgraded lexical
observation. Two fresh 30-video multiclass panels increased correct rows but
showed that co-visible business names make global roster exclusivity the wrong
contract. The fixed goal-conditioned, train-background-IDF verifier passed a
third 30-video OCR-unseen panel: correct/`UNKNOWN` improved
`6/24 -> 19/11` (`+43.3 pp`), generated negatives stayed `0/30`, and no wrong
goal, identity, or portal binding was emitted. This is Development evidence for
known-goal text verification only; independent target-absent pixel truth remains
missing. A fixed same-line OCR-box phrase branch added `2/30` only posthoc and
`1/30` on a fourth fresh multi-token panel, moving that fresh combined result to
`21/30` (`70.0%`) with `0/30` generated phrase-negative accepts. Because the
fresh gain missed the preregistered minimum of two, the layout branch is frozen
as a small effect rather than promoted.

The missing independent target-absence authority was then supplied by official
HierText test pixels and complete word polygons. Two disjoint 30-image panels
first exposed merged-line carrier geometry, duplicate-text instance ambiguity,
and truncated truth fragments. The fixed third-panel successor combines exact
query-span projection inside merged OCR boxes with complete-truth unique-
referent selection and runtime spatial-ambiguity abstention. On 30 further
pixel-unseen test images it improved correct/wrong/`UNKNOWN`
`11/0/19 -> 22/0/8` (`36.7% -> 73.3%`, `+36.7 pp`) with `0/30` accepts for
queries proven absent by complete truth. This passes the frozen Development
gate, but remains static text-carrier evidence—not open-world instance identity,
facade or portal ownership, action utility, arrival, or handoff.

That representation now also has a source-distinct multi-view street-sign
result. Official FSNS testdata first showed `26/30 -> 30/30` when later views
were accumulated. The unchanged formal replay used 40 validation signs whose
image hashes, labels, and distinctive goal tokens were disjoint from all 50
testdata examples. Single-first-view exact recognition was `34/40`; four-view
goal-conditioned evidence reached `40/40` (`85% -> 100%`, `+15 pp`) with zero
accepts on 40 label-disjoint challenges and 40 synthetic negatives. All six
recoveries were exact and appeared at view 2 or 3. This passes the FSNS frozen
Development gate and establishes cross-source multi-view canonical sign reading,
not pose/action causality, sign-to-facade association, or portal truth.

An Exa-discovered controlled-action source then tested the missing intervention
edge. On 30 official CATALIST validation videos balanced across five tripod
camera transformations, 12 complete labels and 12 conditioned target tokens
were disjoint from all training labels/tokens. First-frame exact evidence was
already `27/30`; fixed action-checkpoint accumulation reached `29/30`
(`90.0% -> 96.7%`) with two pan recoveries and zero accepts on 30 label-disjoint
challenges or 30 synthetic negatives. It missed the frozen +5 gain gate, and a
consumed-cache two-token diagnostic added nothing. Freeze CATALIST: it supplies
executed action classes but no direction, metric pose, stationary
counterfactual, facade/sign ownership, or entrance truth.

Exa also surfaced the official 3D Street View same-physical-point benchmark.
Although the official archive was rate-limited, a frozen 181,018,624-byte prefix
provided 6,220 fully decodable real images. A deterministic endpoint-prefix
split produced 2,180 training pairs and 426 held-out pairs. A frozen optical-
center, scale-gated DINOv2-small lock improved held-out AUROC
`0.949859 -> 0.984860`, balanced accuracy `0.895773 -> 0.926341`, average
precision `0.954445 -> 0.982243`, and 32-anchor retrieval Top-1
`0.843750 -> 1.000000`; image/patch AUROC improved
`0.874644 -> 0.966274`. This is a strong real-image Development effect for
locking one provider-verified physical point, but the benchmark has no door,
entrance, venue-ownership, access, waypoint, arrival, or handoff truth.

The unchanged score then exposed its transfer boundary. On three consumed
SceneNN doors with edge-clipped truth proposals, Top-1 stayed `2/6` and upgraded
AUROC fell to `0.444444`. A geometry-first 3RScan cohort with six fully visible
proposals instead reached global `5/6` and center-scale `4/6`; two sibling doors
collided. A parameter-free closed-roster one-to-one assignment repaired that
consumed score matrix to an equivalent `6/6`. The successor was frozen on three
physically target-disjoint 3RScan doorframes before RGB: pair AUROC/AP and
independent Top-1 were `1.0/1.0/6-of-6`, assignment was `3/3`, and the best
complete assignment beat second-best by `0.419960`. Record
`L10_3RSCAN_ROSTER_ASSIGNMENT_TARGET_DISJOINT_DEVELOPMENT_GATE_MET`. This is
single-provider, truth-proposed, closed-roster Development—not detection,
open-set identity, named-entrance ownership, access, arrival, or handoff.

Open-roster transfer exposed the missing existence signal. On four new physical
targets across closed, extra, missing, and balanced-swap scenarios, multiview
appearance reciprocal matching reached only `TP=4, FP=5, FN=8`, F1 `0.380952`.
Registered surface ranking recovered `12/12` true matches but forced one adjacent
doorframe-to-door false match (F1 `0.96`, exact unmatched sets `3/4`). Two
predeclared registration witnesses supplied a `0.038756 m` rejector ceiling on a
third fresh cohort; the combined challenger reached `TP=12, FP=0, FN=0`, F1
`1.0`, and exact unmatched sets `4/4`, versus complete-assignment F1 `0.96`.
Rank-only also reached F1 `1.0` on that cohort, so the witness mechanism remains
a challenger whose incremental value needed fresh decision-changing evidence.
That evidence falsified the raw ceiling: on a new scan family rank-only reached
`TP=64, FP=2, FN=0`, F1 `0.984615`, while the witness rule reached
`TP=48, FP=0, FN=16`, F1 `0.857143`. A new parameter-free registered planar-
extent support rule then used six unconsumed doorframes and all thirty ordered
swaps. It improved rank-only `TP=136, FP=4, FN=0`, F1 `0.985507`, to
`TP=136, FP=0, FN=0`, F1 `1.0`, with exact unmatched sets `33/33`.
With the algorithm frozen, a newly materialized scan family supplied four
same-class doors and twelve ordered swaps. Rank-only `34/2/0`, F1 `0.971429`,
became `34/0/0`, F1 `1.0`, and exact unmatched sets improved `13/15 -> 15/15`.
This confirms the core across 3RScan families, not across providers.

The active information gaps are a source-disjoint provider/city check of the
  frozen SEVN mechanism, a new provider/city-disjoint sequence source with both
  text, visual-reference, directed pose/action evidence, and exact target-instance/facade
reachability for the PanoLab router, and provider-disjoint or independently
justified phone-side target-conditioned planar support before any raw-device or
portal-authority promotion.
See [the L10 current](../research/active/l10-r0/CURRENT.md).

### DTR-R2 obstacle and route risk

Status: `DTR_R2_DYNAMIC_RETAINED`

JRDB X21 is frozen as a same-source Development pass pending one genuinely
source-disjoint confirmation. In CARLA, X24 remains the same-source C2
Development reference; X26 and X30 missed their gates, and C8-C11 did not admit
an evaluable X31 occlusion source. The separate native-dynamics line materialized
N3 across three towns, while the sole N4 replay completed only Town01 and is a
consumed incomplete attempt.

The next decisions are source-disjoint X21 confirmation and source admission,
not threshold rescue or a broader model sweep. See
[the DTR current](../research/active/dtr-r0/CURRENT.md).

## Parallel-line rule

L10 goal completion and DTR obstacle risk are separate product/research lines.
Evidence, denominators, success, and authority do not transfer between them.
The untracked workspace may contain exploratory candidates; only the two
tracked route currents above own active committed decisions.

## Demonstration and engineering surface

- Semantic Anchor to Marker Pose remains the live-device showcase closure. It
  is separate from L10 and DTR research evidence and is not the current
  integration priority.
- Workstation entrypoint: `tools/ba.ps1`
- Android entrypoint: `scripts/run_android_gradle.ps1`
- CARLA integration: [CARLA_PLAYBOOK.md](CARLA_PLAYBOOK.md)
- Artifact routing: [LOCAL_ARTIFACTS.md](LOCAL_ARTIFACTS.md)
- Code ownership: [CODE_MAP.md](CODE_MAP.md)

## Claim boundary

- BlindAssist is not validated navigation or a certified safety product.
- Synthetic, replay, curated Development, registered-source, live-device, and
  natural-distribution evidence are reported separately.
- `UNKNOWN` and `NOT_EVALUABLE` are neither negative evidence nor known-safe.
- `referent != affordance != waypoint != arrival != handoff`.
- A build or JVM test is not a live-camera or user result.
- Protected blind/final claims follow
  [formal research governance](formal/RESEARCH_GOVERNANCE.md).
