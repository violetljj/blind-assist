# P3 lightweight temporal head closure

Terminal:

```text
P3_TEMPORAL_LIGHTWEIGHT_HEAD_R0_CLOSED_MIXED_NOT_PROMOTABLE
```

The four-frame A2-392 temporal head route is closed after the mixed,
implementation-diagnostic development result. Its consumed validation result
must not be used for further loss, threshold, seed, class-weight, clip-length,
checkpoint, or parent selection.

Retained engineering assets:

- junction-safe input/output path guards;
- activation, training-result, and evaluator schema checks;
- frozen clip manifest and parent-wise evaluator;
- sealed-holdout boundary checks;
- A2 disagreement materialization plumbing.

Explicitly not promoted:

- the pooled-depth MLP temporal head;
- the auxiliary clearance-delta head;
- the nine-class transition head;
- the attempt-05 through attempt-08 checkpoints and metrics.

No A5S, QNN/HTP, Android, cadence search, canonical replacement, or new P3
training is authorized by this closure.
