# P3 R0.2.1 ARKit validation download protocol

The user-confirmed license scope, eight-parent roster and 40-asset HEAD receipt are bound before body download. Each video is processed independently. The downloader CRC-checks the three raster archives, takes the earliest 300 source-native stems common to RGB, depth and confidence, binds the nearest intrinsics, retains trajectory and deletes temporary ZIP files after extraction.

The 300-frame choice is identity-only and fixed before media access. Every adjacent timestamp must remain within 500 ms. No failed video may be replaced.

This download does not authorize labels, transition inspection, model loading or training.
