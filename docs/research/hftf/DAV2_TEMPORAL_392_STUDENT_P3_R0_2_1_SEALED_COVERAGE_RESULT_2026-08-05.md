# P3 R0.2.1 sealed coverage result

The frozen 11-parent Bonn cohort is not statistically evaluable under the precommitted coverage gates.

All 132 clips and 528 locked frames were derived and sealed. Aggregate coverage confirms 11 independent video parents and 132 evaluable clips. Three key-transition gates pass: `CLEAR_TO_OCCUPIED=11`, `KNOWN_TO_UNKNOWN=8`, and `UNKNOWN_TO_KNOWN=12`. `OCCUPIED_TO_CLEAR=7` misses the immutable minimum of eight by one event.

This is a data-coverage stop, not a temporal-model result. The cohort cannot be supplemented, replaced or extended after aggregate coverage is known. The 1.5 m threshold, target operator and clip identities cannot be changed to rescue the route.

The private rows remain undisclosed; the ordinary record binds only their SHA and the authenticated sealed bundle SHA. No model, optimizer or training runtime was loaded or constructed. Activation is prohibited.

```text
P3_R0_2_SEALED_HOLDOUT_NOT_EVALUABLE_NO_COHORT_SUBSTITUTION
```
