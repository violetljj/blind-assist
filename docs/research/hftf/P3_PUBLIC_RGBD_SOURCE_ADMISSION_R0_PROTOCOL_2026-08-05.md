# P3 Public RGB-D Source Admission R0

This is a new public-data admission question. It does not reopen or modify the closed P3 R0.1 experiment or the local-universe R0.2 capacity result.

The finite source universe is Bonn RGB-D Dynamic, TUM RGB-D, JRDB, EgoBody, HOI4D and ARKitScenes. The search window is three Hong Kong business days ending 2026-08-07. Before any role freeze, the audit may inspect only source identities, timestamps and continuity, calibration and synchronization metadata, raw independent metric-sensor capability, access and licensing, file integrity and ancestry overlap. Labels, clearance, geometry, transitions, model outputs and performance are forbidden.

The successor evidence field is `independent_metric_sensor_valid`. It covers synchronized valid Kinect, structured-light, LiDAR, ToF or registered RGB-D evidence. It does not rewrite the historical R0.1 `tof_valid` contract.

At least eight parent- and higher-cluster-disjoint candidate holdout units are required; twelve are the target. Parent identities may never be manufactured by splitting one official recording sequence.
