# TARO O0R ARKitScenes source-adapter implementation lock

状态：`TARO_O0R_ARKITSCENES_SOURCE_ADAPTER_IMPLEMENTATION_LOCK_PASS / SYNTHETIC_ONLY / SCIENTIFIC_STATUS_NOT_RUN / EXECUTION_NOT_AUTHORIZED`

日期：2026-08-10

机器锁：[JSON](TARO_O0R_ARKITSCENES_SOURCE_ADAPTER_IMPLEMENTATION_LOCK_2026-08-10.json)

冻结合同对应的纯内存 runtime 已实现：exact Decimal timestamp 与 right-bracket watermark、冻结
`8 ADAPTER_FIT + 16 O0R_EVAL_CANDIDATE` role roster、asset SHA/CRC 与 decoded-payload receipt、内部
FARO/AppleDepth/confidence residual 与 private factory-bound uncertainty model、eval-only FARO
whole-geometry hash、由 FARO support plane 重算的
9 query receipts、immutable common-support base、固定 DepthART model/checkpoint/metric-zero output receipt、
TARO-specific reducer，以及具有逐组件 lineage 和 parent context 的 8 arms × 2 modes injection。

44/44 synthetic focused tests 通过。测试没有打开 24 个 selected source body，没有物化真实 truth 或
uncertainty，没有运行 DepthART 或 scientific O0R，也没有创建任何 future dataset/work/evidence root。
真实 candidate-relative SCALE correction 当前不存在；它只能在预先提交的 signed truth-only result 与
独立执行权限之后，由后续 runner 绑定 frozen common support 生成。

唯一 successor 是
`TARO_O0R_ARKITSCENES_TRUTH_ONLY_ONE_SHOT_PREFLIGHT_LOCK`（`execution_authority=false`）。该锁只允许绑定
exact source assets、argv、environment、budget 与 absent exclusive roots；它本身不得下载或打开 payload、
物化 truth、拟合 selected-source uncertainty、运行 DepthART 或执行 factorial arms。
