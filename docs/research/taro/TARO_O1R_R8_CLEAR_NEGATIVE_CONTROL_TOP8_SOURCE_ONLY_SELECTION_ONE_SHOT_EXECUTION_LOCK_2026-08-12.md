# TARO O1R R8 source-only top-eight selection execution lock

Status: `AUTHORIZED_UNCONSUMED`.

The admitted R1 Phase A contains 402 complete source records across the exact
24-parent pool, with zero FARO or truth read. This one-shot lock verifies all
808 pre-manifest files, reloads the complete source sequence, applies the
frozen truth-blind enrichment selector, and seals the final eight parents.

No FARO, truth, label, outcome, candidate rerun, threshold fit, training, or
network access is authorized. Phase B may bind only the resulting exact
selection seal.
