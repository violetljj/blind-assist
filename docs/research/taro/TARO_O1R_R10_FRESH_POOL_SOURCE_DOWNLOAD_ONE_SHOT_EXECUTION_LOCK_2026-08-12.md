# TARO O1R R10 fresh-pool source-download lock

The zero-body HEAD preflight admitted all 96 frozen assets and read zero response
body bytes. Its sealed Content-Length total is `1,945,902,515` bytes.

This one-shot lock authorizes only bounded download and byte-integrity validation
of those exact assets. It does not authorize archive/frame decode, model execution,
FARO access, truth scoring, training, deployment, product, or safety claims.

The evidence root is consumed when it is exclusively created. Failure does not
restore authority; overwrite, resume, repair in place, replacement, and rerun are
forbidden. A pass permits only a separately locked read-only source inventory.
