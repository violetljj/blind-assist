# TARO O1R R7 fresh confirmation Phase A execution lock

Status: `AUTHORIZED_UNCONSUMED`.

This one-shot authorizes the exact frozen eight-parent, 170-frame fresh cohort
to run 170 sealed DepthART inferences and source-only positive-occupancy
feature construction. Candidate inputs are RGB and effective K only. Phase-A
features may additionally use AppleDepth, confidence, intrinsics, trajectory,
the sealed candidate, and the frozen fit-only uncertainty model.

FARO reads, truth scoring, threshold fitting, training, network access, and
`CLEAR_OBSERVED` output are forbidden. Output-root creation consumes the lock.
A valid Phase-A completion admits only a separately locked FARO Phase B.
