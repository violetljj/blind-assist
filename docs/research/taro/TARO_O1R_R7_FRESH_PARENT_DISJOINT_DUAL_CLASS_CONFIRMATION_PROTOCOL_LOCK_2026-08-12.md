# TARO O1R R7 fresh parent-disjoint dual-class confirmation protocol

Machine-readable authority: [JSON](TARO_O1R_R7_FRESH_PARENT_DISJOINT_DUAL_CLASS_CONFIRMATION_PROTOCOL_LOCK_2026-08-12.json).

Only the fit canary's positive-occupancy hypothesis advances. The candidate is fixed to a connected confidence-2 AppleDepth component with at least 2 pixels, maximum height at least 0.08 m and minimum forward distance no greater than 2.0 m. It may emit `OCCUPIED_OBSERVED`; absence of that evidence remains `UNKNOWN`. The far-censored clear branch is disabled because the fit cohort contained no definite clear labels and produced no clear outputs.

Fresh confirmation requires an outcome-blind parent/visit-disjoint cohort and no replacement after labels are known. Before effectiveness gates can run, at least six parents must contain definite occupied labels, four must contain definite clear labels, and the cohort must contain at least 100 occupied and 50 clear query labels. Insufficient class coverage is `NOT_EVALUABLE`, not a pass or fail. `UNKNOWN` never becomes a negative.

This protocol does not authorize a cohort, download or execution. Its only successor is `TARO_O1R_R7_FRESH_CONFIRMATION_COHORT_AND_DATA_USE_LOCK`.
