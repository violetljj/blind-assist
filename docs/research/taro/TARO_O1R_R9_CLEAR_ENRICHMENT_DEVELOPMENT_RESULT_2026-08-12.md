# TARO O1R R9 clear-enrichment development result

The remaining 16 R8 parents were validly materialized as development truth:
269 frames and 2,421 query labels, with 14 `CLEAR`, 1,578 `OCCUPIED`, and 829
`UNKNOWN`. All 273 evidence files passed their manifest hashes.

The old 50-clear development target is infeasible on this pool and remains
failed. The frozen source-only rule nevertheless concentrated all 14 observed
clear queries, spanning five parents, into its ranked top eight. Among matched
definite labels the rule saw 11 clear and 7 occupied; it is a cohort selector,
not a path-state output or an effectiveness claim.

The fresh protocol will use a pre-outcome floor of 12 definite-clear queries
across at least four parents. If all 12 avoid a false occupied prediction, the
one-sided 95% Wilson lower bound for clear specificity is 0.8160, above the
0.8 guardrail. This is a feasibility-derived exploratory confirmation gate, not
a rewrite of consumed R7/R8 outcomes; those 50-query terminals remain fixed.

All 24 R8 parents are now consumed. The selector may only rank a new source-only
pool, and any result requires entirely fresh parents. `UNKNOWN` is never a
negative. No effectiveness, deployment, product, or safety promotion is made.
