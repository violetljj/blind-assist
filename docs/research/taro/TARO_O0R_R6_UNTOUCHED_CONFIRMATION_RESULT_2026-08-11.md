# TARO O0R R6 untouched confirmation result

Date: 2026-08-11

Mode: `WILD_LAB / UNTOUCHED_CONFIRMATION`

Terminal: `TARO_O0R_R6_FACTOR_SPLIT_UNTOUCHED_CONFIRMATION_PASS`

## Decision

The frozen R6 factor ownership is promoted as the TARO research baseline for
the next prospective runtime protocol:

- `SUPPORT` and `BOUNDARY` exact-copy the Phase-A selected component;
- `QUERY_CLEARANCE` always exact-copies the R1 baseline component;
- no truth-dependent branch reselection is permitted;
- unevaluable FARO support remains nine explicit `UNKNOWN` query slots.

This closes the R5 knownness regression without discarding its independently
confirmed SUPPORT/BOUNDARY improvement. It does not authorize deployment,
product efficacy, Android integration, or a safety claim.

## Frozen execution

The one-shot lock bound eight parent-disjoint ARKitScenes Training visits, 120
exact pose-bounded frames, 1,080 query slots, the sealed DepthART identity and
the unique argv. Phase A completed all 120 candidate inferences and all 120
source-only decisions before the first FARO read. Phase B then read exactly one
bound FARO member per frame. Training and network counts were both zero.

The execution used 140,934,144 CUDA peak allocated bytes and 1,248.156 seconds.
The evidence root is consumed and must not be overwritten or rerun.

## Scientific result

| Measure | R1 baseline | R6 composite | Result |
|---|---:|---:|---|
| Extraction-evaluable queries | 725 | 765 | `+40`, no regret |
| Boundary-evaluable queries | 45 | 50 | `+5`, no regret |
| Query-known slots | 6 | 6 | exact no regret |
| Height error reduction, median of parent medians | - | `0.258620600638 m` | positive |
| Normal error reduction, median of parent medians | - | `0.029162891117 rad` | positive |
| Parents jointly positive for height and normal | - | `8/8` | pass |

All nine frozen gates passed: exact cohort/lineage, Phase firewall, parent
denominators, positive height and normal parent-macro effects, all-parent joint
positivity, extraction no-regret, boundary-evaluability no-regret, and query
known-coverage no-regret. `promotion_allowed=true` applies only to adoption of
this factor policy inside the TARO research route.

FARO support was not observable on 34/120 frames. Those frames were retained as
`UNKNOWN`, not removed or counted as negatives: 29
`SUPPORT_PLAUSIBLE_INSUFFICIENT`, four `SUPPORT_SLOPE_EXCEEDED`, and one
`SUPPORT_GATE_FAILED`.

## Independent evidence verification

`verify_r6_confirmation_evidence.py` independently checked all 725 manifest
members (109,980,064 bytes), every size/SHA-256 receipt, the exact cohort and
execution-lock binding, all candidate arrays, Phase-A/Phase-B lineage, 120
nine-slot query packages, factor ownership, and the result terminal. Replaying
the 1,080 persisted query rows reproduced all counts, metrics within the frozen
12-decimal precision, all nine gates, and the PASS terminal.

The stored summary seal is valid. An exact seal replay from already-canonicalized
query JSON differs because medians are recomputed after 12-decimal serialization;
the maximum observed numeric delta is `5.000444502911705e-13`, below the
`1e-12` canonical precision. The verifier records
`summary_exact_seal_replay=false` rather than hiding this seam. A successor
runtime must canonicalize aggregate inputs before reduction if it requires
byte-identical summary replay.

Key evidence bindings:

- execution lock SHA-256: `4A1513E06FEC16061C645382C5FEB1EBB2C5805D554483364A402CDAE64B2022`;
- result file SHA-256: `F0D946EAF77D21E940E5B5E4AA678C3ED5703A34526095D3E0687FA2D07C24AB`;
- summary file SHA-256: `EDBE4FD26C59DA455D5C50876459F1E9EDD54D6372F47913534E6A4243F04E38`;
- manifest file SHA-256: `B41A6BE7AA992F2DE37C4498139CFE20E22CD2F82C7B3109395417960A902B4E`.

## Authority boundary and successor

The unique successor is
`TARO_O0R_R6_FACTOR_POLICY_ADOPTION_AND_PROSPECTIVE_RUNTIME_PROTOCOL_LOCK`.
It may freeze how the confirmed factor policy is consumed by a prospective
TARO research runtime. It may not reuse this untouched outcome to tune a
selector or threshold, rerun the consumed cohort, claim final clearance
quality, or modify deployment/product/safety state.
