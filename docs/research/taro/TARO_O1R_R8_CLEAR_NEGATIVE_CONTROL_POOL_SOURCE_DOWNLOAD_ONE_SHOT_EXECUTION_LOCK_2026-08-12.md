# TARO O1R R8 clear-negative-control pool source-download execution lock

Status: `AUTHORIZED_UNCONSUMED`.

The admitted zero-body HEAD found all exact 72 assets available within the
1,116,131,542-byte frozen source budget. This one-shot lock authorizes only
their bounded download and byte-integrity validation into the isolated R8
source root.

Archive/frame decoding, model execution, FARO, truth scoring, training,
deployment, product, and safety use remain outside this lock.
