# TARO O1R R8 clear-negative-control pool HEAD execution lock

Status: `AUTHORIZED_UNCONSUMED`.

The implementation and exact 72-request plan are complete and tested. The
user's `授权` reply on 2026-08-12 is bound to the exact frozen R8 24-parent pool;
the unique command may now run once.

HEAD response bodies are fixed at zero bytes. Download, source decoding, model
execution, FARO, truth scoring, and training remain forbidden by this lock.
