# TARO O0R direct Apple hybrid adapter-fit confirmation R5 amendment

## Outcome

Status: `PROTOCOL_AMENDMENT_FROZEN_EXECUTION_FALSE`.

R5 freezes a fresh task-metric test of the already fixed R4A policy on the
eight parent-disjoint former `ADAPTER_FIT` identities / 211 exact source frames
/ 1,899 query slots. It does not fit a selector, tune a threshold or change the
policy. The amendment authorizes implementation work only; it does not yet
authorize DepthART inference or FARO task scoring.

The exact machine authority is the adjacent
`TARO_O0R_ARKITSCENES_DIRECT_APPLE_HYBRID_ADAPTER_FIT_CONFIRMATION_R5_AMENDMENT_2026-08-11.json`.

## Why a new role is required

The predecessor contract deliberately limited `ADAPTER_FIT` to source
residual/uncertainty fitting and prohibited model outputs and task metrics. Its
data-use receipt covers the bound local source assets but does not activate
DepthART or task execution. R5 therefore creates a new, one-version-only role:
`R5_TASK_METRIC_CONFIRMATION`. It does not rewrite the predecessor role or
reuse a consumed execution lock.

These eight parents have not produced DepthART or task-metric outcomes for this
policy, but their AppleDepth/FARO residuals contributed to the frozen
uncertainty model. R5 can establish parent-disjoint task-metric confirmation;
it cannot be called untouched external-dataset validation.

## Exact cohort

| Visit | Video | Exact frames |
|---|---|---:|
| `470974` | `47332075` | 25 |
| `469216` | `47332946` | 16 |
| `423614` | `42898071` | 11 |
| `467370` | `47333776` | 24 |
| `469460` | `47333043` | 23 |
| `438794` | `44358241` | 26 |
| `467346` | `47333876` | 43 |
| `472473` | `47204786` | 43 |

The 211-frame sequence, source receipts, RGB bindings and the explicit
16-parent prior-eval denylist are hash-frozen. Missing, duplicate, reordered or
extra identities make execution invalid; no frame or `UNKNOWN` record may be
dropped.

## Frozen algorithm

Policy: `DIRECT_WHEN_SOURCE_SUPPORT_AVAILABLE_ELSE_R1_BASELINE_V1`.

- select direct SUPPORT iff the truth-blind Phase-A Apple plane is physically
  valid;
- otherwise select the existing R1 baseline;
- if a direct-selected branch later fails extraction, retain direct
  `UNKNOWN`; Phase B must not rescue it by switching to baseline;
- the only selection field is `source_support_available`;
- extraction evaluability, FARO, errors, boundary/query metrics and knownness
  are forbidden selection inputs;
- free parameters, thresholds and training steps are all zero.

## Two-phase firewall

Phase A first seals all 211 DepthART candidates, then all source-only Apple
scale/plane/branch decisions, and finally a completion receipt. FARO highres
depth, compact/query truth, task metrics, R4/R4A per-query outcomes and all
prior eval truth roots must have zero reads.

Only after completion and hash revalidation may Phase B open FARO for the same
211 frames. It must emit exactly nine ordered valid-or-`UNKNOWN` query slots per
frame and execute the branch already chosen in Phase A. Aggregation is query to
physical frame, frame to parent, then median across the eight parents.

## Pre-outcome confirmation gates

R5 PASS requires all of the following:

- exact cohort, lineage and phase-firewall validation;
- paired height and normal denominators on all 8 parents;
- positive median-of-parent-medians height reduction;
- positive median-of-parent-medians normal reduction;
- 8/8 parents jointly positive on height and normal;
- hybrid extraction-evaluable count no lower than baseline;
- hybrid known-query count no lower than baseline.

Required undefined denominators produce `NOT_EVALUABLE`; `UNKNOWN` is never a
negative. Gate or policy changes after Phase A are forbidden.

## Authority and next step

Current authority permits code, validators and synthetic tests only. It does
not permit creating the R5 evidence root, running the 211 DepthART inferences
or opening FARO for R5 task scoring.

The unique successor is
`TARO_O0R_ARKITSCENES_DIRECT_APPLE_HYBRID_ADAPTER_FIT_CONFIRMATION_R5_IMPLEMENTATION_LOCK`.
After a hash-bound implementation passes focused tests, a separate one-shot
execution lock and explicit model/task execution authority are still required.

Even a future R5 PASS would remain limited to this ARKitScenes Training
landscape cohort and fixed factor/extraction policy. It would not establish a
formal O0R PASS, RGB-only capability, cross-device generalization, deployment,
product efficacy or safety.
