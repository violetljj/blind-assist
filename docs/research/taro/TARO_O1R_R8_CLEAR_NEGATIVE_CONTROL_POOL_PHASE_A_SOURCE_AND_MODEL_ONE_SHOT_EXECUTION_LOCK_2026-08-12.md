# TARO O1R R8 clear-negative-control pool Phase-A execution lock

Status: `AUTHORIZED_UNCONSUMED`.

This one-shot lock runs the already frozen DepthART identity on all 402 exact
frames, seals every candidate before source-feature extraction, and then reads
only Apple depth/confidence plus bound intrinsics and trajectory. It produces
3,618 source-only query features across all 24 parents.

FARO, truth scoring, selector ranking, threshold fitting, training, network,
deployment, product, and safety use remain outside this lock. Parent scoring
and the top-eight seal are a separate source-only successor.
