# TARO O1R R7 fresh confirmation Phase B execution lock

Status: `AUTHORIZED_UNCONSUMED`.

This one-shot first re-verifies every Phase-A artifact and reloads the sealed
completion. It may then read exactly 170 FARO frames, construct 1,530 fixed
labels, and evaluate the pre-frozen dual-class and positive-occupancy gates.

Source or threshold reselection, training, network access, and clear output are
forbidden. FARO `UNKNOWN` labels are excluded from precision negatives. The
output root is consumed on creation even if execution later fails.
