# TARO O1R R8 selected top-eight FARO Phase-B execution lock

Status: `AUTHORIZED_UNCONSUMED`.

The final eight parents were sealed using only source-side evidence, with FARO,
truth, label, and outcome reads all equal to zero. This one-shot lock opens
only `highres_depth` for their exact 133 frames, constructs 1,197 fixed query
labels, and applies the unchanged R7 dual-class and positive-occupancy gates.

Unselected FARO, source or threshold reselection, training, network, device,
deployment, product, and safety use remain forbidden.
