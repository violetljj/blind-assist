# TARO O1R R7 fresh confirmation cohort and data-use lock

Machine-readable authority: [JSON](TARO_O1R_R7_FRESH_CONFIRMATION_COHORT_AND_DATA_USE_LOCK_2026-08-12.json).

The user's `授权` is constrained to the deterministic eight-parent Training roster in the JSON and the three official assets per parent. Selection used only bound split metadata and a repository exclusion snapshot; it read no media body, model output or truth. The roster is parent/visit-disjoint from the 24 formation parents, eight R6 untouched parents and other official identities present in the frozen repository snapshot.

This lock records data-use authority but sends no HEAD request and activates no download, decode, model or truth execution. Each later operation still requires its own hash-bound one-shot lock. No replacement is allowed after availability or outcome is observed.
