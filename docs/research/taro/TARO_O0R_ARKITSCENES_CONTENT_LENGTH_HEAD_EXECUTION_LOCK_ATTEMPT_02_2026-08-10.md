# TARO O0R ARKitScenes Content-Length HEAD Execution Lock — Attempt 02

状态：`WILD_LAB / AUTHORIZED_UNCONSUMED / HEAD_NOT_RUN / SOURCE_UNOPENED`

Attempt 02 保持 Attempt 01 的 exact 24 Training parent × 3 asset、72-URL request plan、zero-body、
20 GiB/8-worker/20-second/3-attempt budget 与 exclusive root，唯一实现变化是绑定提交 `2c0fdef8`：
受信任 `artifacts.local` junction 必须保留仓库 lexical receipt path，且解析后的所有更深 parent 仍须位于
同一 F-backed target 内。

Attempt 01 在 root/HEAD 前停止，request/body/source 为 0、one-shot 未消费；其锁与 incident 保留且不得重跑。
Attempt 02 必须先提交，随后才允许执行本锁冻结 argv。root 一旦创建即消费 Attempt 02；任何后续失败均不得
写成未启动或触发替换/重跑。

本锁不授权 GET、source body、truth、uncertainty fit、DepthART、factorial、训练、设备、产品或 safety。
