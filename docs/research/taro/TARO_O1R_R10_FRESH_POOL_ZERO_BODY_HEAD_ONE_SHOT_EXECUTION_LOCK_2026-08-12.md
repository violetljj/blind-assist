# TARO O1R R10 fresh-pool zero-body HEAD lock

The exact 32 fresh Training parents and 96 asset URLs are frozen. The executable
and fake-transport tests are complete, but this lock remains
`PENDING_USER_AUTHORITY` because the prior data authorization named a different
set of 24 videos.

Once explicitly authorized, this step may send only HEAD requests and read zero
response-body bytes. Download, decode, model execution, FARO, truth, and training
remain forbidden until separate one-shot locks.
