# TARO O1R R9 remaining-pool development truth lock

This one-shot uses the 16 R8 parents excluded from the sealed top-eight and the
269 source frames already materialized before any R8 FARO read. The user already
authorized source/truth-only WILD_LAB materialization for all 24 locked videos.

Only `highres_depth` FARO may be opened. The unchanged dense R7 labeler produces
2,421 `CLEAR/OCCUPIED/UNKNOWN` slots. A frozen 6,804-rule grid then fits a
source-only parent-enrichment selector from pre-existing Phase-A features. The
selector emits no path state and has no confirmation authority.

The run passes development only if its source-ranked top eight contain at least
50 definite-clear queries across four parents and at least four selected parents
have nonzero source scores. `UNKNOWN` is never a negative. All 24 R8 parents are
consumed after this step; a real confirmation must use entirely fresh parents.
