# USTRF observability program real-world authority terminal R0（2026-07-25）

状态：`FROZEN_BEFORE_EXECUTION`

## 唯一问题

现有可复算证据是否已经证明：继续 G1–G7 所需的 canonical source fact、stable metric geometry、intended-route truth 与 event lifecycle truth 仍不可获得，因而总目标应合法终止为 `EVIDENCE_PROGRAM_BLOCKED_BY_REAL_WORLD_AUTHORITY`，而不是算法失败或任务不可观测？

## 冻结证据族

1. 当前 41-sequence G0 authority/repairability validation；
2. JRDB 单帧 RGB/time/transform authority validation；
3. JRDB 32-frame global-affine availability validation；
4. SM-S9280 ARCore moving raw-depth freshness receipt；
5. 三源 RGB-D+pose R2 final report；
6. 三条动态 RGB-D R3 final report；
7. 当前 SANPO/USTRF authority 状态。

## 合法终态

1. `FAIL_CLOSED_PROGRAM_AUDIT_INCOMPLETE`
2. `NEXT_STAGE_REAL_WORLD_AUTHORITY_PRESENT`
3. `EVIDENCE_PROGRAM_BLOCKED_BY_REAL_WORLD_AUTHORITY`

blocked terminal 必须同时证明：

- 当前 canonical pack 的关键 source authority 缺失；
- 新来源 RGB/time transport 可以成功，避免把 blocker 错写成“所有 source 不可得”；
- 当前单目 global-affine quality 不足且未降门；
- 手机 metric depth/stable pose gate 关闭；
- RGB-D+pose transport 成功但 route/event authority 不存在；
- 动态来源 route/event admission 全部拒绝；
- 没有任何证据授权 G1–G7、人体或生产。

## 解释边界

该 terminal 不声称：

- USTRF 核心假设不成立；
- 任务在权威输入下不可观测；
- metric depth/VIO 永远不可行；
- JRDB 或现有工程资产没有研究价值。

它只说明在当前现实世界 authority、数据和权限下，正式实验不能完整执行。恢复必须带来新的 route/event authority、fresh frame-bound metric geometry 或明确的新采集/参与者授权。

