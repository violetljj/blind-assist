# Assistive Geometry R2 F1 FactorTensorAdapter synthetic canary result

Terminal: `R2_F1_FACTORTENSOR_ADAPTER_SYNTHETIC_CANARY_PASS`

The committed zero-parameter adapter passed all 8 frozen tiny-tensor cases and all `A01..A10` gates. All
8 cases were reproduced byte-identically by two independent worker processes; field coverage was `14/14`
for F1 predictions and `28/28` for required F0 fields. Receipt mismatch and invalid support reduced to
UNKNOWN, local missing depth never became occupied, portrait/landscape state maps matched, split/bridge
component behavior was canonical, and 7 sigma-only mutations never strengthened definite evidence.

Execution used CPU only, no network/GPU/device/real data/task outcome, zero learned parameters and zero
training steps. It took `1.583935 s` and wrote `17,158` bytes to the exclusive evidence root. The tracked
[machine result](BLINDASSIST_ASSISTIVE_GEOMETRY_R2_F1_FACTORTENSOR_ADAPTER_SYNTHETIC_CANARY_RESULT_2026-08-10.json)
binds every evidence file by byte count and SHA-256.

This closes only the tensor-to-frame interface/mechanics blocker. It does not establish real factor
learnability, causal headroom or task utility. The supervision frontdoor remains unsatisfied and F1
execution remains unauthorized.

Unique successor: `BLINDASSIST_ASSISTIVE_GEOMETRY_R2_F1_SUPERVISION_SOURCE_AND_LABEL_CONTRACT_LOCK`,
with execution authority `false`; it may only freeze a separate pre-outcome source/label contract.
