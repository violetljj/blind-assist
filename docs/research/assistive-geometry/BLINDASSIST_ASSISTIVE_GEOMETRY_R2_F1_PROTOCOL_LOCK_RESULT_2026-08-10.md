# Assistive Geometry R2 F1-P protocol-lock result

Terminal: `R2_F1_PROTOCOL_FROZEN_EXECUTION_NOT_AUTHORIZED_SUPERVISION_FRONTDOOR_UNSATISFIED`

F1-P is now frozen. The machine protocol and factor schema pass both the repository research-governance validator and the dedicated static validator with zero errors; nine mutation tests confirm that execution-authority drift, final-task shortcuts, UNKNOWN-as-negative, capability-count drift, aggregate-loss checkpoint selection, downstream reducer rescue, parent-role overlap and F0 predecessor drift fail closed.

The lock declares 14 factor prediction fields across depth/scale, support surface and obstacle-boundary evidence. Thirteen factor-specific losses remain separately visible; aggregate loss cannot select a checkpoint, and reducer/task output cannot train, select or rescue F1. A future roster requires at least 8 / 2 / 2 parent-disjoint identities for FIT / CHECKPOINT_SELECTION / TRAIN_CANARY and at least 12 jointly factor-complete parents.

Current F1 execution remains unauthorized. The bound AG-DCA atlas has metric-depth source support on 4,767 frames / 16 parents, but support reaches only 320 frames / 11 parents; crisp obstacle support is not continuous boundary truth. Continuous boundary truth and complete R2 factor-schema truth are both 0 frames / 0 parents. Direct sigma truth is absent; uncertainty may use only a future frozen heteroscedastic proper-score contract over valid parent-disjoint residuals, never invented constant sigma labels.

No label materializer, factor model, trainer, optimizer step, checkpoint or training artifact root was created.

The unique successor is `BLINDASSIST_ASSISTIVE_GEOMETRY_R2_F1_SUPERVISION_SOURCE_AND_LABEL_CONTRACT_LOCK`, with execution authority `false`. It may freeze supervision sources, continuous-boundary derivation, residual-scoring provenance and the parent-role roster; it may not create or run learning code.
