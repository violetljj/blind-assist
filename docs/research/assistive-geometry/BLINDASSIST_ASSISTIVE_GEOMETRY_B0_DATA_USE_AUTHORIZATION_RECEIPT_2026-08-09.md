# BlindAssist Assistive Geometry B0 data-use authorization receipt

日期：`2026-08-09`

用户原文：

> 我授权所有数据都能使用

本回执将该授权解释为：在数据源许可证、采集同意与本地访问权限允许的范围内，
Assistive Geometry B0 可以读取全部现有数据，也可以为新冻结的 ARKitScenes
`TRAIN 16 / DEVELOPMENT 8 / CONFIRMATION 8` roster 下载 RGB、depth、confidence、
intrinsics 与 trajectory，并按预冻结顺序用于训练、开发选择和一次性确认。

该授权不产生第三方数据再分发权，也不取消已冻结的科学防火墙：

- 已消费 120-frame cohort 不得进入本路线三个角色；
- DepthART task-preserving R2 的 8-session roster 继续专属于 R2；
- CONFIRMATION 在候选、阈值和 evaluator 冻结前保持 sealed；
- `UNKNOWN` 不得解释为负例；
- 数据可用不等于质量、部署、产品或 safety 已获授权。
