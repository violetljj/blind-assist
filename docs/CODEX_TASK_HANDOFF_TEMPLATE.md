# Codex 多任务长任务交接模板

> 每个独立任务使用一份交接文件：`artifacts.local/work/codex-handoffs/<TASK-ID>.md`，并在同目录的 `INDEX.md` 登记。`TASK-ID` 固定为全大写的 `DOMAIN-OUTCOME-ROUND`，例如 `SANPO-PUBLIC-SILVER-R7`。本文件是可提交的模板与字段说明；不要把实时任务状态写回本文件。

## 当前任务交接

- 任务 ID：`TASK-ID`
- 更新时间：`YYYY-MM-DD HH:mm`（`Asia/Hong_Kong`）
- 任务状态：`进行中` / `阻塞` / `等待用户决定` / `待核验`
- 目标：
- 非目标：

## 任务边界

- 允许修改的路径：
- 禁止修改的路径：
- 所属 worktree / 分支：
- 依赖的其他任务：`无` / 只读读取 `TASK-ID`
- 本任务的证据目录：

## 已完成

-

## 已修改

- `path/to/file`：修改原因与效果。

## 已验证

- 命令：`command`
  - 结果：`通过` / `失败` / `未运行`
  - 关键输出、失败原因或证据路径：

## 当前现场

- Git 状态：
- 本任务修改的文件：
- 与本任务无关、不得改动的现有工作区修改：
- 正在运行或仍需执行的操作：
- 已作决定与不可撤销约束：

## 风险与阻塞

-

## 下一步

1. 先执行：`command or concrete action`
2. 预期结果：
3. 若失败，先检查：

## 续作规则

续作前先阅读此文件，并核验声明的 worktree / 分支、执行 `git status --short` 与“已验证”中声明的必要检查。只允许操作“任务边界”内的路径；旧记录只提供线索，未经核验不得当作当前事实。若现场与交接不一致，停止实施，先记录差异。
