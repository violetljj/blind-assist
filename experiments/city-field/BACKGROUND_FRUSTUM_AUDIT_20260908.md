# BigCity background candidate audit

Status: **NOT_ADMITTED / visibility isolation UNVERIFIED**. Offline source metadata only; no model or formal collection.

`tools/city_background_isolation.py` audits exact actor-package identities against five infinite-frustum halfspaces. It uses the full descriptor AABB support, with no radius, region-box, origin-distance, label or spatial-loaded filtering. Invalid bounds are retained. Only exact native DirectionalLight and SkyLight classes are excluded as global illumination, with their package identities recorded. HLOD proxies stay in the sets, explicitly unmapped. This is a conservative geometric diagnostic, not a rendered-visibility certificate.

The existing 101,981-row BigCity export has **101,981 unique actor packages but only 20 distinct GUID strings**. All GUID strings are transient Python `<Struct ... (address)>` representations; they must not be used as identities. Actor packages are usable aggregate identities for this fixed map snapshot. Component and instance identities still require export. The original source-audit JSON was retained unchanged.

Evidence: `artifacts.local/nearfield/city-crossregion-v1-20260908/background-audit/`.

- `center-yaw-sweep.json`: 56 hypothetical views, seven descriptor centers × eight headings at 45° intervals. Heights derive from ground descriptor maxima plus 1.6 m, not floor probes. Candidate counts range from 134 to 94,927. Every cross-split region pair can achieve zero overlap in the **BLDG-label subset** with some heading pair. `summary.json` retains one joint seven-heading subset solution; it does not prove background isolation.
- `scout-views.json` and `scout-frusta.json`: the 35 provisional views from the source-only scout specs. Among 224 ground-view cross-split pairs, 102 have no shared BLDG-label candidates; the maximum is 34,709. The labels are diagnostic grouping only; all other descriptors remain in the actual candidate sets.
- Seven actor packages intersect every scout frustum: labels `CitySampleCityZoneShapes`, `GroundCollisionCube`, `SM_dome`, `water_plane`, `BP_MassCrowdSpawner`, `BP_CitySampleWorldInfo`, `ZoneGraphData`. Their large/global bounds cannot establish whether they draw shared physical background. They remain retained until actual primitive/type evidence justifies any contract exclusion. In particular, do not remove the water plane or dome merely from their labels.
- `asset-registry-root-packages.json`: 10,091 exact package roots comprising all 6,061 HLOD descriptors, all nonspatial actors, and every descriptor AABB intersecting any candidate center ±150 m in XY, with no Z cull. This is a provisioning seed list, not dependency closure or a capture visibility inventory. Dependency closure must resolve project/plugin mounts and map/editor dependencies separately.

The existing NumPy environment `E:/codex-tools/venvs/dual-loop-radial-geometry-lite-r0/Scripts/python.exe` ran four focused unit tests successfully: infinite-distance retention and off-origin bounds; invalid bounds retained; UE negative-pitch topdown convention; GUID alias isolation and HLOD accounting. Default Python 3.14 lacks NumPy, so that initial invocation failed before executing tests or the audit. Metadata execution was CPU.

## Next useful evidence

Outward-facing views are worth scouting, but they may lack useful street content. Keep the source-only RGB/floor/obstruction inspection as the route authority. Frustum overlaps overestimate visibility because occlusion is ignored; zero overlap still lacks complete source/primitive coverage, HLOD source mapping, reflected-scene dependencies and final camera perturbations.

`research/active/dtr-r0/nearfield/city_hlod_membership.py` supplies `export_membership(unreal, fresh_output_path)` for an already owned UE editor session. It enumerates loaded actors and attempts reflected `source_actors` → `actors` mapping for HLODs. It retains source Package, Path, WorldPackage, ContainerPackage, ActorInstanceGuid and ContainerTransform; failures remain explicit UNVERIFIED entries. Its v2 loaded-actor rows also inventory actual PrimitiveComponent classes/paths, visibility and rendering flags, world bounds, static mesh assets, instance counts and material paths/IsSky where readable. Failed property reads remain null with errors. Empty primitive lists mean only that this loaded actor has no primitive components; attached/spawned content is not ruled out. No exclusion is inferred from labels, and no instance-transform dump is performed. It starts no UE process, loads no actors and saves no source. Its reflection behavior is **not yet verified in UE**.

Local UE 5.8 headers substantiate the source data route: `Engine/Source/Runtime/Engine/Public/WorldPartition/HLOD/HLODActor.h` exposes `GetSourceActors()`; `HLODSourceActorsFromCell.h` exposes `GetActors()` backed by an editor-only UPROPERTY array; `WorldPartitionRuntimeCell.h` declares `FWorldPartitionRuntimeCellObjectMapping` with package/path/container transform/actor instance GUID. The getters are not UFUNCTIONs, so reflected editor properties may be required. If unavailable, a bounded native C++ exporter is the next option. Mappings can contain child HLODs or container instances: recursively resolve them before any physical-instance isolation claim.

## Native fallback and first live primitive findings

The first live Python export (`scouting/big_candidate_01-v4/evaluator/hlod-membership.json`) inspected 2,313 loaded actors, but all 925 HLODs failed reflected `source_actors` access. The native fallback is now implemented as `BlindAssistCaptureLibrary.export_world_hlod_source_membership(world, filename)` and compiled successfully with UE 5.8. The fresh package is `artifacts.local/ue-hlod-b2/package/BlindAssistCapture.uplugin`; `build-receipt.json` records PASS and DLL SHA-256 `0fe8fde21ba06f248434e2402eba7577235aa456b5c3d49efe0d25c3c7557939`. Build elapsed 255.47 s with a verified maximum two actions; all owned processes were released. The currently used plugin package was untouched. A later owned runtime probe successfully exported 925 HLOD mappings with zero unresolved nodes; its 3,342 source edges are all references to 3,342 distinct child HLOD packages, not original scene-actor leaves.

The method uses public native getters, writes only a fresh filename beneath `BA_CITY_OUT`, never loads or saves source actors, and exports exact source actor GUID/package/path/container identity/transforms plus nested-HLOD indicators. A true return means only that JSON was written. The report remains UNVERIFIED; unsupported source types and empty/incomplete mappings remain explicit blockers. Recursive source closure and component-instance identity are not fabricated.

`global-primitives-live-summary.json` and `global-surface-details.json` retain first-scout evidence. The water is a visible main-pass 30 km plane at Z −3.5 m using `MI_ocean`; the dome is a visible main-pass mesh roughly 51.6 km wide and Z 124–20,124 m using `M_sky`. Both material IsSky flags are false. Offline material package strings reference `/Game/Lighting/HDR/sunset.sunset`; this is a texture-inspection lead, not proof that it contains no baked skyline. GroundCollisionCube's actual component visibility is false. All four WorldInfo primitives are hidden. ZoneShape and crowd/disturbance annotation rendering requires the actual `ZoneGraph` show flag; ZoneGraphData rendering requires `Navigation`, as verified in the engine's scene proxy code. These debug primitives can be scoped out only when the capture flags are established; spawned content remains separate.

The root's later selected seven-heading assignment still has **333 distinct shared non-HLOD package candidates**, ranging from 7 to 142 per cross-split pair, despite zero BLDG-label overlap. Some pairs also share 4, 11 or 24 HLOD proxies. `selected-heading-all-actor-overlap.json` preserves all identities, including ground, traffic lights, cars, furniture and biom actors. No background isolation is admitted from the building-label result.

## Full HLOD graph attempt: explicitly incomplete

The verified native runtime evidence is `native-source-probe-v1/evaluator/hlod-membership-native.json` under the same dated artifact root. `tools/city_hlod_graph.py` merges batch exports, checks conflicting identities, cycles, missing child nodes and nested-type mismatches, and resolves original actor leaves only when the required nodes exist. Edge container and editor-parent transforms remain intact; nonidentity composition is explicitly unverified. Three focused tests pass for nested resolution, cycle/missing-node failure and nonidentity-transform handling.

`background-audit/partial-native-graph/graph.json` applies that tool to the verified runtime evidence and the complete 6,061-HLOD descriptor list. Result: **UNVERIFIED**, 925 exported nodes, 5,136 missing HLOD nodes, 3,342 unresolved referenced child packages, and zero established original-actor leaves. All exported edge transforms are identity, which does not close the missing graph. The zero leaf count is missing evidence, not absence of physical sources.

Three bounded NullRHI metadata sessions were preserved:

| Artifact root | Observed terminal and reason |
|---|---|
| `artifacts.local/hlod-map-a1` | Failed before exports: Slate callback reentered synchronous map loading, causing `Context.World() == GWorld` assertion. |
| `artifacts.local/hlod-map-a2` | Reentry guard fixed loading and enumerated all 6,061 descriptors; exited at the available-RAM floor before loading a batch. |
| `artifacts.local/hlod-map-a3` | Explicit post-map GC and bounded memory checks allowed the 64-proxy canary; Blueprint `load_actors` loaded zero requested HLODs, so it exited `INCOMPLETE_MISSING_REQUESTED`. |

All three source-integrity receipts confirm unchanged map/project hashes; all process-release receipts report released=true and zero survivors. No RGB, training or planned 336-frame experiment was run by these metadata sessions. The primary UE window was released.

The final failure has a concrete API explanation: `WorldPartitionBlueprintLibrary.cpp:193` routes LoadActors through `FLoaderAdapterActorList`, while `WorldPartitionActorLoaderInterface.cpp:125` filters on `IsEditorRelevant()`. `WorldPartitionActorDesc.cpp:1056` rejects runtime-only actors and `HLODActor.h:194` overrides `IsRuntimeOnly()` to return true. HLOD loading uses a separate adapter. The direct native getter is verified, but this generic Blueprint loading route did not expose the requested proxies. A future bounded native reference/package-loading implementation may close the graph; it was **not added or rebuilt this turn**, per the explicit stop instruction. Full recursive isolation remains incomplete and does not block the separate worker collection using the already verified plugin.
