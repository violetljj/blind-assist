# BlindAssist 下一次大更新准备计划

## 目标版本判断

- 实施目标版本：`v2.5.0`
- 更新级别：大更新，按项目规则从当前 `v2.0.0` 增加 `v0.5`
- 推荐主题：现场可测助行体验升级
- 当前阶段：执行实现，保留现有模型资产和构建方式

## 更新目标

下一次大更新不优先追求更复杂的模型，而是把现有单屏检测原型推进到更适合真机连续试用的状态：

- 降低 `MainActivity` 同时承担 UI、相机、推理、风险、反馈调度的压力。
- 让风险提醒从“当前帧有什么”升级为“当前行进状态下该怎么提醒”。
- 增加可复盘的现场测试能力，记录误报、漏报、性能和提醒触发原因。
- 强化助盲产品边界表述，继续避免把原型描述为可替代人工判断的安全设备。

## 建议范围

### 1. 助行会话层

- 新增轻量 `AssistSession` 或 `AssistEngine`，集中串联检测结果、风险分析、稳定策略和反馈决策。
- 将纯 Kotlin 逻辑尽量放在 `risk`、`alert`、`feedback` 或新的 `session` 包，保持可单元测试。
- `MainActivity` 保留相机生命周期、权限和 View 渲染职责，减少业务判断堆叠。

### 2. 现场测试模式

- 增加仅本地使用的测试开关或调试面板入口，用于记录最近一段时间的风险结果、反馈决策、FPS 和推理耗时。
- 记录内容优先写入内存环形缓冲，后续再决定是否导出文件，避免一开始引入存储权限和隐私风险。
- README 中明确测试模式只用于开发验证，不代表安全认证。

### 3. 引导式提醒策略

- 在现有 `Quiet`、`Standard`、`Sensitive` 档位基础上，增加更清晰的提醒状态，例如：
  - 观察中：仅展示视觉提示。
  - 注意：中近距离目标持续出现。
  - 避让：中心近处或迫近目标，需要立即提示。
- 保持语音短句克制，优先给方向和行动提示，例如“前方近处有人，放慢”。

### 4. 真机验证清单

- 连续运行 5 分钟观察发热、卡顿、FPS、推理耗时和提醒频率。
- 在室内走廊、门口、桌椅密集区域分别记录误报和漏报。
- 验证屏幕关闭、应用切后台、权限拒绝、TTS 初始化失败等边界行为。
- 验证 Care Mode 和三个提醒档位在同一场景下的差异是否可感知。

## 不建议纳入本轮

- 不更换 YOLO 模型资产，除非先完成模型来源、导出参数、形状检查和真机性能对比。
- 不直接引入大型架构框架或多模块拆分；当前项目规模更适合先做包内职责拆分。
- 不加入导航、地图、定位或联网能力；这些会显著扩大权限、安全和隐私边界。
- 不把提醒文案改成绝对安全承诺，应继续保持“原型辅助”表述。

## 第一批实施任务

1. 提取 `AssistEngine` 纯 Kotlin 编排层，并为风险到反馈决策链路补测试。
2. 新增 `SessionTrace` 内存记录结构，覆盖最近 30 次检测摘要、风险结果、反馈原因和耗时。
3. 在调试详情中展示最近一次会话摘要，Care Mode 下继续隐藏开发细节。
4. 调整提醒文案，使近处和迫近场景更像助行指令而不是检测报告。
5. 运行 Gradle 单元测试和 debug APK 构建，并按仓库规则记录验证结果。

## 本轮执行选择

- 按完整 `v2.5.0` 范围实施，不新增外部硬件、定位、联网、文件导出或 Android 权限。
- `SessionTrace` 只保留内存环形缓冲，容量固定为 30 帧，通过现有调试面板展示。
- 提醒文案采用轻量改进，保留现有风险等级、距离等级和提醒档位体系。

## 验收标准

- `MainActivity` 的新增业务判断明显减少，核心风险到反馈链路可在 JVM 单元测试中验证。
- 现有风险分析、稳定化、提醒档位和偏好持久化测试继续通过。
- README 更新当前行为和新调试能力，`DEVELOPMENT_LOG.md` 记录实施、验证和版本判断。
- APK 能正常构建，真机安装后能打开相机、显示覆盖层并按档位触发提醒。
