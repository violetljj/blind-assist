# BlindAssist 更新计划状态

## 当前说明

本文件原先记录的是 `v2.5.0` “现场可测助行体验升级”计划。该计划已经在后续版本中完成并继续演进，本文件现在作为历史计划状态说明，不再代表当前路线图：

- `v2.5.0`：完成 `AssistEngine`、`SessionTrace` 和现场测试摘要基础。
- `v3.4.0`：补强现场测试摘要和无障碍语义。
- `v4.1.0`：完成展示中心、真实版本路线和演示材料。
- `v4.2.0`：完成手动场景化提醒和风险解释层。
- `v4.3.0`：根据当前使用需要暂时移除 App 内项目展示中心，保留文档和 APK 归档作为展示材料。
- `v4.8.0`：完成单模块质量升级，拆出相机输入、检测结果、会话编排、FPS 统计和 UI 状态映射边界。
- `v5.3.0`：完成 TalkBack、大字体和中英文界面/提醒文案基础。
- `v5.8.0` / `v5.9.0`：完成日常使用向导、一键模式、真机完整测试和相机调试控件修复。
- `v6.4.0` / `v6.9.0`：完成 Hilt 运行时拆分和完整多模块迁移。
- `v7.1.0` / `v7.6.0`：完成反馈触达可靠性、运行时管线拆分、帧输入优化、发布卫生和当前真机回归验证。

为避免后续代理误把旧 `v2.5.0` 计划当成下一步目标，本文件改为“更新计划状态”说明。新的下一步方向应优先从 `idea.md` 中未完成或部分完成的条目选择，并结合当前代码、README、CHANGELOG 和开发日志重新制定计划。

## v4.2.0 已实施范围

- 新增手动 `AssistScenario`：通用、室内慢行、走廊通行、密集区域、户外慢行。
- 保持通用场景完全兼容 v4.1.0 行为，其余场景只调整规则层策略，不更换模型。
- 将场景偏好持久化到 `SharedPreferences`，检测开关仍保持 session-only。
- 在相机控制面板和现场测试摘要中展示当前场景与最近风险解释。
- 风险解释覆盖已触发、风险未稳定、距离较远、冷却中、提醒保持、反馈关闭和暂无可反馈风险。
- 本轮不引入自动场景识别、联网、定位、蓝牙、存储权限、Hilt、多模块、Room 或 DataStore。

## 后续候选

- 轻量展示入口回归：只有在答辩或课堂展示明确需要时再做，避免重新占用日常主流程。
- 眼镜设备模拟中心：在不申请蓝牙权限的前提下，把占位入口升级为模拟连接、电量和摄像头来源状态。
- 离线测试素材回放：使用少量授权图片或 debug-only 素材稳定复现检测框、风险规则和提醒解释。
- 设备回归增强：当前 `scripts/run_device_regression.ps1` 覆盖安装、清数据、冷启动、包信息、UI dump、截图、`gfxinfo` 和 `meminfo`；如需采集 CameraX/TFLite `BlindAssistPerf`，可扩展脚本自动进入手机摄像头路径。
- 当前路线图入口：优先查看 `idea.md`，并在真正实施后同步更新 README、CHANGELOG、DEVELOPMENT_LOG 和必要测试。
