# MZ3: frozen-output error attribution

2026-09-10 EXPLORE; consumed controlled Development diagnostic. This protocol is
written before computing the new joint error tables. Completed MZ0/MZ1/MZ2
aggregate results and their existing regional tables are already consumed.

Question: where does learned fusion gain or lose exact four-event predictions,
and are its wrong-far/cross-body errors accompanied by geometric observations?
Hypothesis: aggregate fusion gain combines recoveries and regressions that vary
by physical condition and modality disagreement; source geometry can distinguish
zone-center approximation support from native support but cannot explain model
causality or identify the actual return surface.

Freeze all 1500 EVAL_ONLY rows, in the admitted 5000-frame index order. Reuse
only saved predictions, clean packets, native event counts, source metadata and
receipts. Hash-check original artifacts, row truth, roles and original alerts.
Do not fit, infer models, select thresholds/checkpoints, alter runs, exclude rows,
or create a truth-selected routing/gating policy. Missing support is UNKNOWN /
NOT_EVALUABLE for claims requiring it, never clearance.

Comparisons: MZ1 fusion and MZ2 1x1/4x4 against retained MZ0 multi-surface, matched
MZ1 ToF-only and RGB-only; 8x8 is reused MZ1. MZ2 fixed noise/dropout/shift against
clean MZ1. Report gained/lost/both-correct/both-wrong exact rows and introduced /
removed / retained wrong-far and cross-body events. Keep all three fixed stresses;
do not rank/select interventions. Descriptive strata: region_id, condition,
declared_range, fixture family and original_fixture_group_id, plus condition by
range. List denominators and frame IDs. These are not independent natural trials.

Report RGB-only versus ToF-only disagreement by event and frame, their joint
correctness, and fusion exactness / agreement in each descriptive category.
No inference-time router is defined by these evaluator truth categories.

For false-positive event bits, report existing clean 8x8 zone-center support,
same-FoV native counts and full native counts (0, 1-2, >=3). Native >=3 defines
the event reference and is not independent evidence. Count all false positives
and the wrong-far/cross-body subsets separately; near-only wrong-far is per
body/range query, cross-body is per opposite-body activation. Multiple wrong
event bits may belong to one cross-body event. Clean support during a corrupted
input is explicitly counterfactual source support, not the corrupted packet.
Do not claim physical object association, causal explanations, hardware fidelity,
temporal reliability or safety from these arrays.

One CPU array/scalar analysis pass (`TASK_NOT_GPU_SUITABLE`), followed by a
separately expressed reconstruction of aggregate metrics and pair counts from
exported row records. No source capture, new training, accelerator/process lease,
or model inference. Store outputs only in
`artifacts.local/work/mz3-error-attribution-20260910/run-v1/`, preserving original
runs. A failure may receive a task-owned mechanical repair with its receipt kept;
the fixed denominator and analyses do not change.

Decision: locate concrete failure surfaces and retain the previously declared
MZ0/MZ1 dispositions. A mixed gain/loss result motivates a separately frozen
future observation/association test only; it does not authorize a replacement
gate or additional fitting. If metadata/support is absent, report that evidence
gap. Stop after the tables, verification and result report are delivered.
