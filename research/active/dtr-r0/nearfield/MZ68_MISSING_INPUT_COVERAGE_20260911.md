# MZ68 — missing-input training coverage

Status: candidate prepared for root review; no registration, fit, or model execution at preparation. All sources and prior results are consumed Development. Task: `artifacts.local/work/mz68-missing-input-coverage-20260911`.

MZ61 TRAIN packet census found 55/2,048 all-missing frames under existing DROP, versus 1,320/2,048 under a separately fixed DROP-plus-1.2 m range-budget stress proxy. This is an availability gap, not measured sensor performance or proof RGB cannot recognize those frames. MZ62 changed allocation among existing profiles; it did not introduce ALL_INVALID training. MZ66 numerical teacher-score preservation remains a separate proposed mechanism, not part of this test.

Fit exactly one `NULL_COVERAGE` head for 1,536 updates from the original MZ62 CONTROL checkpoint SHA `b7106449e4246b7bd7933659cbf0dd815691072c03a8b522633b7aee9a456d2e`. Compare with sealed MZ64 GEOMETRY at the same budget, initial weights, frame sequence, model, loss, original frozen encoder/normalization, Adam lr .001 and seed 151. Keep all MZ64 and MZ66 prediction arrays byte-exact; no baseline refit or full baseline recomputation. MZ66 remains a negative control and MZ64 remains a challenger, regardless of this candidate's outcome.

The sole change is zero-based `step % 4 == 3`: set all ranges to zero and all validity bits false for **both** native eight-frame inputs and selected OLD_NEG eight-frame replay inputs. Every other step retains original `step % 3` IDEAL/MERGE_CLOSE/DROP_CLOSE. The 1,536-step schedule therefore has 384 steps per profile including ALL_INVALID, replacing the baseline's 512 steps per original profile. All original RGB, labels, UNKNOWN, query selection and frame presentations stay unchanged. No projection, distillation, threshold search, geometry/pose change, new source, or additional epoch is added. In particular, MZ67 is not an input.

Each step retains 4 MZ48 native + 4 MZ61 native + 8 OLD_NEG frames. Total presentations: 6,144 MZ48, 6,144 MZ61 and 12,288 OLD_NEG. ALL_INVALID accounts for 1,536 MZ48 + 1,536 MZ61 + 3,072 OLD_NEG = 6,144 frame presentations. Fixed original sequencing means the MZ61 missing-input presentations cover 1,192/2,048 unique TRAIN IDs: 856 IDs never receive ALL_INVALID, 881 once, 278 twice and 33 three times. MZ48 covers 488/1,095 actual fit-unique IDs and OLD_NEG 1,114/3,533. This is 25% step coverage, not complete per-ID missing coverage. No outcome-driven reshuffling is permitted.

The model is the unchanged 11,020-parameter MZ56 `AnchorQuery(GLOBAL_ANCHOR)`, and loss remains MZ59/MZ64 balanced local plus original OPEN positive/frame query loss and `.25 * negative_loss` on selected old queries. Native labels are training/evaluator-only. All-missing ranges/validity must reach both callers canonically; every missing step records zero measured slots, zero anchor availability and exactly zero anchor vectors. Static sensor coverage means geometrical FoV, not actual valid measurements; it remains unchanged.

Calibration stays original DEV1,000 + MZ48cal256 under **DROP only**, with the original zero-added scalar rule and one new four-query cutoff vector. MZ61/MZ55 calibration and held rows never fit or calibrate. Evaluate MZ61 all 4,096 frames/all roles under four profiles, including ALL_INVALID. Evaluate all seven legacy cohorts under the original three profiles; retain attempted MZ36 400 rows/80 UNKNOWN bits and local UNKNOWN. No new cap-specific neural evaluation is included.

Primary definition is the exact importable `mz68_score.FROZEN_DEFINITION`; the proposed JSON is `primary-definition.proposed.json`, to be finalized and bound by root before fit. All **11** retaining clauses must pass, versus sealed MZ64 GEOMETRY's final OLD_NEG union:

1. MZ61 HELD1,024 ALL_INVALID total four-query TP strictly greater (baseline 43).
2. ALL_INVALID has no newly false query bit relative to baseline (4 FP); equal FP totals with different false bits fail.
3–8. On DROP, each of legacy_noncal, MZ48 nonfit and MZ55 held640 retains total TP and does not increase total FP.
9. DROP MZ61 HELD1,024 retains total TP (795).
10. DROP held has no newly false bit relative to baseline (38 FP).
11. DROP held native BODY_NEAR additions beyond OLD_NEG do not decrease (30).

Every failure retains full gains, losses, native/known-wrong/UNKNOWN winner attribution, all profile/group/query counts and paired support effects. Held awning's 64 BN positives and native additions are reported, without adding another gate. Compare also OLD_NEG, MZ57, MZ62 CONTROL, MZ64 CONTROL and MZ66 PROJECT. The observed prior scores motivated this proposal; it is not fresh confirmation. Dense argmax is saved from inference and not independently regenerated by the CPU scorer.

Reusable code: `mz68_missing_input_source.py`, `mz68_missing_input_coverage.py`, `mz68_score.py`. Source preparation admits all sealed inputs and all 2,664 MZ66 arrays; original MZ64 schedule/groups are byte-copied. The owned feature plan is 6,676 × 64 × 45 × 80 float32 (6,152,601,728 bytes including header); calibration/held frames are not cached as fit rows. Earlier caches were released, so this is a rebuild, not an existing-cache reuse claim. Evaluation shares each RGB encoding across profiles: 3,143 cache hits and 10,497 uncached evaluation frames; 17,173 planned total encodes. MZ64 GEOMETRY's actual fit took 49.508 s and MZ66's whole single-candidate run 292.266 s; roughly five minutes is planning context, not a measurement or stop timer for MZ68.

Preparation is CPU-only: source identity/owner checks, caller/profile and unchanged-sequence checks, original scalar cutoff/gate tests, and saved-output schema compatibility. One test-only MZ50 UNION alias lookup failure is retained and corrected in preparation evidence; no scientific output existed. Actual initialization parity is deferred to the authorized run: 16 MZ48 TRAIN and 16 MZ61 TRAIN unique frames over 3/4 profiles, against saved MZ62 CONTROL raw/support at atol2e-5/rtol1e-6. No complete old-model cohort is rerun. Execute only after root review, registration and GPU release; preserve failure evidence and do not silently repeat a scientific run.

CLI after authorization: `python mz68_missing_input_coverage.py --root E:/linnan/linnan --task E:/linnan/linnan/artifacts.local/work/mz68-missing-input-coverage-20260911`. Score only after sealed run PASS/root GO: `python mz68_score.py --root E:/linnan/linnan --task artifacts.local/work/mz68-missing-input-coverage-20260911`. Both use a new `run-v1`/`score-v1` owner path. Root supplies the frozen `inputs.json`, finalized `primary-definition.json` and ACTIVE `registration.json` before execution. Handles close in `finally`; scratch remains until scored and explicitly audited for cleanup. No hardware, sensor-noise fidelity, natural-scene, trained-ceiling or safety claim follows from this contrast.
