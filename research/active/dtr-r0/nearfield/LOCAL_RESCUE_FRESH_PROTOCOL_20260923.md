# Fresh source for fixed causal LOCAL rescue admission

2026-09-23 EXPLORE. This source supports a separately frozen rescue gate;
it does not authorize a gate fit, selection, threshold change or capture retry.
The owning rescue protocol supplies the candidate and decision criteria before
capture. This file governs source construction, capture and public inference.
The fixed gate is slot 939 <= 0.02239669393748045 in the winning centre query;
its gate.json and LOCAL_RESCUE_GATE_PROTOCOL_20260923.md are capture-sealed inputs.

One capture, 900 seconds, 576 frames in 48 complete clips. Keep four original
families, two composite sizes, three lateral relations, and approach-return and
approach-dwell-return schedules. The eight sizes, elevations, overlap distances,
and both front-distance schedules differ from the earlier stability source.
Each composite remains exactly two rendered cubes; union-of-cuboids labels do
not fill its holes. All instances remain in the denominator, including missing
visibility and invalid labels. Invalid labels are UNKNOWN, never negative.

Before capture, freeze the generated source, source checks and code hashes.
Check relative geometry signatures against query occupancy, data coverage,
LOCAL transfer and LOCAL stability sources. Planned geometry checks establish
source construction only; they are neither rendered labels nor model outcomes.
Retain the same map, materials, renderer and sensor law. This is new geometry
within the same controlled generator, not an independent natural distribution.

Capture uses the governed research-ue runtime. The launcher releases its owned
process tree in finally, and capture releases only newly spawned actors and
render targets. Preserve receipts, raw RGB, native evaluator arrays and hashes.
No task starts a paid worker or modifies shared actors, frozen sources or models.

Materialize RGB [576,3,180,320], ToF [576,64,6], and separately sealed rendered
evaluator labels. Frozen RAW/LOCAL feature extractors, models and thresholds
operate only on public RGB/ToF; metadata validates identity and does not enter
features. Seal all probabilities, baseline predictions and features before the
owning gate evaluator reads labels. Zero fits and zero threshold selections.

Artifacts: artifacts.local/evidence/ba-local-rescue-fresh-20260923 and siblings
-capture, -prepared, -predictions. Retain all outputs. Scalar materialization
and frozen HGB inference use CPU TASK_NOT_GPU_SUITABLE; capture uses the logged
renderer adapter. Sample times are posed 0.2-second indices, not measured
continuous physical motion or endpoint latency.
