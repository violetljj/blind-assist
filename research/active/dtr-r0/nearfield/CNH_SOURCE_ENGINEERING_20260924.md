# CNH 写实来源与背景共享：工程进度

2026-09-24。用户已选择背景资产共享口径，已同步v3第4节和当前决定。
目标/同类干扰物及参与通道侵入的建筑构件仍按资产族与可见依赖分区；
物理场地实例、布局和全部反事实片段不跨分区。背景资产交集必须披露。

## 早期探针记录（历史状态，最新进展见文末）

新增声明级分区审计和两来源共用的只读近场实体/LOD0导出探针。
实测证明两套来源都能导出所枚举静态实体的LOD0；不证明它等于实际渲染表面。
尚无逐实例可见ID光栅、全场景覆盖及七路写实结果，正式20布局试采仍被阻止。
本次各取一个历史已知视点验证导出能力，**不是各2个新布局的对比**。

| 来源 | 8m球与变换后网格包围盒相交的实体 | 唯一网格导出 | Nanite开启网格 | 其中ISM/HISM实体 |
| --- | ---: | ---: | ---: | ---: |
| Street200V7 | 97 | 13/13 | 10/13 | 73 |
| Small_City_LVL | 118 | 49/49 | 39/49 | 114 |

Street200也使用Nanite，不能仅凭场景规模推断其导出一致性更容易。
导出网格分别包含14,692和243,270个唯一三角形；实例化后分别62,046和425,312个三角形。
探针分别用时0.765s和4.078s；包含启动、单帧RGB-D、探针、退出的编辑器生命周期为88.000s和38.672s。
两次缓存状态/加载范围不同，以上不是七路吞吐排名，也不是持续采集速度。
City使用原生地图的既有局部加载方案，远景不完整，不声称完整城市已通过。

加载对象中还记录到36/33个未覆盖的非静态网格primitive，包括编辑器辅助组件；
Street200另有8个DynamicMeshComponent。它们是全局保守清单，未判定是否在近场或可见，
不能把这个计数直接叫漏掉36/33个障碍。实例材质遮罩、WPO、渲染LOD与Nanite等价性均未建立。
两张实际RGB均已查看；画面检查不替代几何准入。

## 稀疏原生深度诊断

每个视点固定中心偏移8像素、stride16的880条射线；以径向5m判断近场，误差用相机轴向米。
用导出的全部实例LOD0做CUDA两面射线求交，不把预测mesh归属当作渲染实例ID。

| 来源 | 原生/预测近场射线 | 共同近场 | 仅预测为近场 | 共同近场误差中位数/P95/最大值 |
| --- | ---: | ---: | ---: | --- |
| Street200V7 | 210/210 | 210 | 0 | 0.892 / 4.616 / 10.193 mm |
| Small_City_LVL | 193/195 | 193 | 2 | 6.054 / 22.258 / 82.862 mm |

两者仅原生为近场均为0；分母是每视点880条固定射线，不能把相邻像素当独立布局。
Street视点的稀疏深度吻合较好，City视点误差较大，但不能单独归因Nanite；
隐藏实体、材质形变/遮罩、未覆盖primitive及LOD差异均未分离。
没有为本检查事后设置准入阈值，两份结果均`NOT_VERIFIED`；不可据此选主力或宣称全场景通过。
实际CUDA设备为RTX5060 Laptop；诊断分别0.516/1.594s，Torch峰值分配均53,893,632字节，
这不是UE峰值显存。输出位于`artifacts.local/evidence/cnh-street-depth-audit-20260924-v2/`和
`artifacts.local/evidence/cnh-city-depth-audit-20260924-v2/`。

## 实现与证据

- `cnh_route_source_audit.py`：共享背景交集披露；受隔离资产跨角色/派生族/可见依赖泄漏和物理场地交叠检查。
  元数据PASS不是实际来源真实性或采集准入，输出始终`benchmark_eligible=false`。
- `cnh_route_scene_probe.py`：枚举StaticMesh/ISM/HISM，以变换后完整bounds筛选，导出LOD0、世界变换、材质和hash；
  保留不支持对象/导出异常，回执始终`NOT_ADMITTED`，不以碰撞代理代替。
- `cnh_route_scene_check.py`：通过既有collector的独立哈希快照注入探针，不修改collector或共享地图；
  保留原生readiness、RGB-D输送、源文件hash与进程树释放检查。采集器快照包含当时工作树实现，不把WIP当已冻结基线。
- `cnh_route_scene_depth_audit.py`：哈希核对、世界坐标变换及分块CUDA射线诊断，不作ID或材质一致性准入。

证据位于`artifacts.local/evidence/cnh-street-mesh-preflight-20260924-v4/`与
`artifacts.local/evidence/cnh-city-mesh-preflight-20260924-v2/`，含结果、mesh、原生RGB-D、源码快照及日志。
两者`source-integrity.json`均unchanged，`process-release.json`均released且无survivors。
初次管理路径、预建输出目录与Python依赖错误的失败回执保留；均发生在UE启动前，没有替换失败结果。
深度诊断最初输出位置与既有数据范围重叠，被管理入口拒绝；改用独立新输出，不覆盖原证据。
run spec和调度日志保留在`artifacts.local/work/cnh-route-comparison-20260924/`。
持久缓存属于原CitySample项目，继续保留；本任务没有需继续占用的UE进程。
验证：CNH既有及新增CPU检查33项通过，另2项CUDA解析平面/空几何检查通过；文档链接和差异空白检查通过。

## 下一步

本记录的诊断结果保持原身份，不按后定标准追认通过。用户反馈后，下一轮改用
[方案第4节的用途标准](CNH_ROUTE_COMPARISON_PLAN_20260924.md)：背景径向误差P95<=30mm、最大<=75mm，
另限每区回波缺失/伪新增角权重，并检验已知可见细物体近层不丢失。
只给受控插入物做可见ID，背景像素聚合为0；原生侵入物仍计入全几何标签和资产隔离。
插入物可以使用关闭Nanite/WPO的显式派生版本，所有模态共同使用、重新采集；不替原版本补PASS。
按固定2布局/来源完成后给出分项PASS/FAIL与来源选择，不再要求全场景每个面完全一致。
目前不选择主力、不扩充384布局，也不启动七组训练。用户随后已决定第一阶段仅四类室外，室内移至第二阶段。

## 室外范围与净空实现

第一阶段环境改为人行道、交叉口、广场、巷道；384布局、180/40/164分配和20布局试采各环境5个不变。
原生场景保留，正例使用受控插入资产；`cnh_route_native_clearance.py` 对四段完整分段线性轨迹的
六个查询体积检查15cm净空。旋转轨迹、非保守bounds或覆盖未知不自动通过；实际侵入仍记录并失败。

发现旧探针包含两个隐藏的编辑器摄像机代理（MatineeCam_SM），不属于实际可见场景。
UE5.8的SceneCaptureComponent::OnRegister明确将其设置为可视化组件并hidden-in-game。
新探针排除同时editor-only与hidden-in-game的组件并记录理由，旧探针与深度诊断结果保留、不改写。

基于旧视点的固定16候选诊断中，排除上述代理后，Street200为16个无bounds冲突、0个冲突；
City为2个无冲突、14个冲突。结果在`artifacts.local/evidence/cnh-native-clearance-screen-20260924-v2/`。
这些是同一消费视点附近候选，不是独立布局；bounds尚无形变与加载覆盖保证，正式准入数均为0。
错误地包含摄像机代理的v1结果也保留，用于解释实现修正，不用来估算城市布局接受率。

真实资产插入通路使用独立临时世界验证两件资产、各无遮挡/完全遮挡/部分遮挡，输出七路与导出网格。
可见ID候选实现为隔离目标原生深度与完整场景深度1mm内一致，背景0、插入ID1/254、无效深度65535；
这是原生深度可见性方法，不声称已实现stencil。派生副本不保存、不改变源资产族身份。

## 真实派生资产与可见ID：单元通路通过

`cnh-inserted-asset-canary-20260924-v8` 实际完成6帧、每帧七路：双目RGB、双目深度、法线、albedo、
uint16可见ID；另保留隔离目标原生深度和实际LOD0网格。两件资产为Metal_Bollard_00和No_Parking_Road_Sign_00，
原资产均启用Nanite，副本关闭Nanite并强制LOD0。材质保留原完整MaterialAttributes、实例参数和纹理，
仅清零WPO与PDO；源配置前后相同，不保存派生资产。UE插件通过原生公开ConnectInputAttribute实现，
避免Python不能读取的保护字段。结果为 `PASS_INSERTED_ASSET_ID_CANARY`。

| 资产ID | 无遮挡可见像素 | 完全遮挡 | 部分遮挡 |
| --- | ---: | ---: | ---: |
| 1（栏杆柱） | 4819 | 0 | 2442 |
| 254（标牌） | 5547 | 0 | 2695 |

ID按1mm原生深度一致性产生；完整场景反而比隔离目标更远的矛盾像素保留UNKNOWN，不能写背景。
原生执行含派生/采集13.047s（v7）；保留外观的v8全生命周期与转换32.92s。
这是空地图、实际素材的单元检查，不是写实两来源吞吐，不代表同帧多实例或254实例容量已验收。
v7已通过遮挡检查，但RGB实看过曝；v8仅降低补光、关闭bloom，复查后标牌纹理可见，全部原检查仍通过。

独立CUDA检查 `cnh-inserted-geometry-audit-20260924-v1` 每帧固定14,400条射线，
两件物体分别298/352条命中；六帧均无漏/多网格命中，采样可见ID与网格深度归属差异为0。
最大径向误差分别0.029mm、0.304mm。此为稀疏单元诊断，不给背景几何或正式布局补PASS。

v1–v6材质属性、Python反射、节点数组和配置字符串比较的失败回执全部保留；v4引擎异常退出也保留。
配置比较剔除UE结构字符串中的临时包装器地址，保留属性值比较。两个原生插件版本实际编译成功。
全部任务UE进程与临时目录已释放，插件包、源码快照、原始输出、失败日志及共享缓存保留供复现。
下一项是把已验证通路接入真实场景、核实原生净空，再运行各2布局比较；20布局试采和384布局采集均未启动。

## 两布局检查的固定数值网格

在读取成功采集的数值结果前固定 `cnh_route_source_geometry_audit.py`：640×360图像像素中心，
stride2、offset1，限定45°×45° ToF视场与径向5m，每帧12,321条射线；同一左目/ToF原点。
使用立体角Jacobian权重，分别检查每帧8×8和4×4全部分区；漏采和伪新增的分母是该区全部射线角权重，
不只取共同有效射线。每布局背景共同命中的径向P95<=30mm、最大<=75mm，两个布局分别判定。
插入物近层检查要求同一实例网格命中，不能用深度相近的背景替代；可见但网格未采到则不判通过。
此检查不代替加倍采样能量收敛、精确标签、来源隔离或完整8m观测验收。

工程采集每来源固定2个场地，每布局最多16个预先生成的候选；每布局四片段各取两个静态端点，共16帧。
它验证七路共用通路与端点，不是10Hz视频或20布局试采。插入细杆采用真实栏杆柱派生网格，
横截面最大30mm、长1.8m；第二布局横置作悬挂细杆。初版fixture误用等比例放大，
在产生任何来源帧之前修正为上述尺寸，原生净空查询和候选顺序不变。

## 两来源工程结论：当前均不准入，20布局不启动

本轮已结束固定检查。**插入通路通过；Street200前置净空未过，City七路输送通过但背景几何失败。**
不把来源状态继续笼统写为NOT_VERIFIED，也不以P95合格掩盖最大值或局部分区失败。
没有选出合格主力，没有启动20布局、384布局或七组训练；未运行项保留其真实状态。

| 检查 | Street200V7 | City Sample |
| --- | --- | --- |
| 两布局原生15cm净空前置条件 | FAIL：每布局固定16候选用尽，均有材质/运动边界未决 | PASS_LOADED_WORLD_ONLY：两布局均选第0候选，全部请求的局部WP描述符已加载 |
| 两布局七路端点采集 | NOT_RUN：前置拒绝，0帧 | PASS：16帧，每布局四片段×两个端点；移除片段ID1均为0 |
| 背景径向距离 | NOT_RUN | FAIL：布局0 P95=15.261mm/max=881.078mm；布局1 P95=8.066mm/max=428.617mm |
| 每区漏采/伪新增角权重 | NOT_RUN | FAIL：布局0最大1.093%/9.008%；布局1最大2.063%/2.105%，上限各2% |
| 固定网格上已知可见的插入细杆近层 | NOT_RUN | PASS：所有ToF视场内可见目标片段均保留同一实例近层；不可见端点不作正例 |
| 加倍采样能量收敛、精确查询标签、完整来源隔离/可用布局数量 | NOT_RUN | NOT_RUN：前述必要几何项已失败，不继续申请准入 |
| 主力来源与20布局试采 | 未选中/NOT_RUN | 未选中/NOT_RUN |

背景共同命中射线分母为布局0的19,314、布局1的19,012；每布局8帧，每帧12,321条固定射线。
这是两个已消费工程场地，不是独立统计样本的扩大。逐帧8×8与4×4共640个分区检查中，
布局0有24项、布局1有4项覆盖失败；重复片段/重叠分区不能当独立事件数。
City导出与渲染不一致的具体原因尚未分离，不能直接断言全部来自Nanite。

Street材质能力探针枚举514个材质槽来源，513个取得编译能力、495个无几何形变、18个树木材质
启用WPO且未设置位移上限，另1个为DynamicMesh空材质。当前保守实现无法为它们提供净空边界，
不等于已证明这些树实际侵入。City局部加载的399个材质全部无编译几何形变；
只排除由Navigation/ZoneGraph关闭标志控制的三个确切调试类型，不隐藏真实背景物体。
关闭actor/component tick只限静态端点采集，不能外推动态场景或完整世界加载保证。

City成功回执为 `artifacts.local/evidence/cnh-city-two-layout-20260924-v6/`，
独立审计为 `artifacts.local/evidence/cnh-city-source-geometry-audit-20260924-v1/`；
Street前置失败为 `artifacts.local/evidence/cnh-street-two-layout-20260924-v1/`。
City七路输出、源网格、源码快照与日志合计429,841,650字节（约410MiB）；
全生命周期69.344s，约0.231组端点/s；原生脚本含探针/派生/采集37.328s。
此实现每端点有稳定等待和多通道回读，不能用旧4.3 RGB-D帧/s估算它，也不能用16端点外推持续吞吐。
全GPU观测显存峰值5,581MiB（含其他进程，不是UE独占分配）；独立CUDA审计23.796s。
实际检查两布局RGB，当前City原生光照配固定曝光偏暗；没有据此认证视觉质量。

机械失败版本保留：v1预筛包含调试组件；v2 Python未暴露的show-flag调用；
v3材质编译重入Slate回调；v4因v3尚未释放而拒绝启动；v5完成16帧但把三个隐藏编辑器图标
从16cm到128cm的bounds变化误判为可见几何变化。逐项对照没有其他native差异，
v6仅修正指纹范围、保留可见性状态后复采；不覆盖v5或改动布局/数值门槛。
v3已按命令行与PID确认任务归属后终止，任务进程树、临时目录均释放；地图文件、项目文件和源素材配置未改写。
插件包、失败/成功证据和共享缓存保留供复现。相关76项测试、原生插件编译均通过。

下一项工程若继续，应针对明确失败机制建立树木形变边界或修复City导出/渲染一致性，
沿用当前失败回执作Development诊断；不得换候选、放宽75mm/2%门槛或把本轮重命名为全新确认。

## 用户反馈后的机制诊断（同日，已完成本次有界排查）

原始两来源失败回执不改写，不把 City 的失败归结为来源本身精度不合格。
本次范围是已消费两布局的工程诊断和机械修复；不启动20布局、384布局或新测试布局。
保留原始P95 30mm、最大75mm、分区各2%、5m径向窗和固定射线网格。

先对City原16端点回放，保存逐射线深度、带符号残差、逐区中位数/P95/max、视场角、
原生深度局部跳变及漏采/多采/大残差叠图。边缘定义只用于描述，不删射线：
1或3像素邻域中轴向深度跳变超过75mm或有效性改变；此定义也会标记斜面，不能把全部标记视为轮廓。
四个片段复用两个背景视角，重复射线和重叠分区不得充当独立证据。

在读取新对照结果前固定后续检验：City沿用原两布局、全部候选与16端点，
仅在任务编辑器内为原生StaticMesh组件设`disallow_nanite=true / forced_lod_model=1`，
让全部模态共同渲染所导出的LOD0 fallback；不改变地图、素材或旧回执。
检验原误差是否消失；未消失则保留失败并按材质/表面归属解释，不自动调参数。
同时补录有效材质blend mode、two-sided、masked以及LOD section→material slot，
不能按文件名含Transparent而删除几何。SceneDepth对应几何与物理净空几何必须分开。

Street沿用原两布局和16候选上限，通过未保存的材质派生清零WPO/PDO，保留原外观输入、
材质参数、网格与Nanite设置；编译完成后验证形变能力。不能把未知能力直接当零，
不能以隐藏树木或更换场地绕过当前对照。共享源码和源资产保持完整性回执。

5m缓冲带、点到表面距离或1像素容差均是后验规则变更建议；本轮不实施，不给旧FAIL追认PASS。
可另拟协议v2，在明确测量目标、透明障碍边界与新场地访问计划后由用户决定。

### 原City16帧误差分解

`cnh-city-error-decomposition-20260924-v1/diagnostic/`保留16份逐射线npz、预测命中实体清单、
逐区分布和RGB叠图。独立复算原布局/逐帧结果与旧审计逐字段完全一致；CUDA耗时27.203s。

| 原布局 | 背景共同命中 | 中位数/P95/max | >75mm记录 | 1像素边缘之外的max |
| --- | ---: | --- | ---: | ---: |
| City0 | 19,314 | 3.473/15.261/881.078mm | 4 | 66.763mm |
| City1 | 19,012 | 1.466/8.066/428.617mm | 48 | 428.617mm（8条超限） |

City0的4次超限是同一视角同一(223,277)射线在四片段复现，预测命中黄色路缘石；
原生轴向2.91m、导出求交约3.70m。City1超限的预测命中归属为vehicle06车身40次、
Transparent网格8次。归属是导出求交胜出者，不是原生可见ID，不能据此断言原生缺了哪个部件。
City0背景多采232条中204条归于vehicle02的Transparent网格；不是232个独立物体或事件。
四片段背景共享两个端点，帧/射线/重叠分区均不得当独立布局。

轴向/径向两种错误解释把两布局中位误差从约1–3mm提高到约0.29–0.31m，P95约0.49m。
UE本机源码也确认SceneDepth线性轴向单位与cm→m输送一致；未发现坐标/半像素约定错误。
逐像素平移诊断只支持局部配准检查，某些内部子集为空，不能声称已经证明七路同步。
带符号残差中位数分别+0.018/+0.392mm，未呈现全场大幅整体偏移；角度分组亦无单调发散证据，
但0–10°共同近场背景射线为0，因此不是全视场误差保证。

九个不支持primitive逐项为：WorldMapBoundingBox、ZoneGraphCrowdLaneAnnotations、
ZoneGraphFleeBehavior、BlockingVolume、PlayerStart胶囊、三个PostProcessVolume的Brush、
ZoneGraphRenderingComp。原净空回执已按隐藏状态或确切关闭的调试标志处理，未见车辆组件；
不能用“九个未支持对象”解释车辆缺面。

原探针的材质实例blend mode为UNKNOWN，修正为原生`MaterialInterface`生效属性并记录
section→material slot；不能把材质实例父链中的未覆盖值或section序号当最终材质槽。
引擎普通透明通道不写SceneDepth，但透明障碍仍可能参与物理净空；本轮不按名称剔除它们。

### Street关闭WPO后的固定门槛复测

`cnh-street-static-two-layout-20260924-v4`完成16端点七路，原候选序列和位姿不变，
两个布局均取候选0。18个唯一材质、46个组件槽经异步编译结束验证为无几何形变；
原生mesh/Nanite不变，所有模态共同使用未保存派生材质，不隐藏树木。
DynamicMesh空槽依据UE真实`UMaterial::GetDefaultMaterial(MD_Surface)`回退读取能力，
不是把空槽直接当零形变或改成自选材质；缺少该能力仍保持UNKNOWN。

| 固定布局 | 背景共同命中 | 中位数/P95/max | 每区漏/多采 | 可见细杆同实例近层 |
| --- | ---: | --- | --- | --- |
| Street0 | 15,469 | 1.690/6.422/18.471mm | PASS | PASS |
| Street1 | 16,544 | 1.614/6.036/50.812mm | PASS | PASS |

独立审计`cnh-street-static-source-geometry-audit-20260924-v1`按未修改的30/75mm及2%门槛全部PASS。
这推翻了“必须估计树木形变上限才能继续”的工程阻塞；原始未处理Street失败回执不翻案。
它尚未验证加倍采样能量收敛、精确标签、资产隔离或可用布局数量，不等于正式来源准入。
未启动20布局/384布局。Street是继续准入检查的优先候选，插入物+简单背景保留备选而非静默替换主基准。

Street0每区最大漏/多采0.560%/0.577%，Street1为0.546%/0.583%；每布局0/640分区失败。
原生脚本38.610s，独立CUDA审计4.938s；任务全生命周期约111.4s。

### City对照结果与本次停止点

City LOD0控制v1因编辑器属性通知触发蓝图重建、组件引用变成TRASH而机械失败，未采集。
改用不通知编辑器的临时属性写入，并用运行时LOD setter刷新渲染状态。
v2完成16帧但项目`r.Nanite.ProxyRenderMode=2`使fallback背景不显示，背景共同命中为0，
不能用于精度判断；失败采集和审计保留。v3在任务进程设ProxyRenderMode=0后完成16帧，
背景恢复但实际RGB仍缺失原车辆，未建立相同背景表面控制。

`cnh-city-lod0-control-20260924-v3`及其
`cnh-city-lod0-error-decomposition-20260924-v2`记录：背景共同命中18,354/16,328，
P95为0.095/126.534mm、max为435.828/910.344mm，覆盖仍FAIL。
这是**fallback控制未保持原生背景可见性**的失败，不能把这些数字当成原City精度退化或合格证据。
未在观察结果后换布局、调LOD参数或改数值门槛继续寻找PASS。

新探针确认vehicle02/06透明网格的有效blend为2或7；`is_masked=true`在此可来自
透明材质的masked阴影，并不使其写入SceneDepth。两个名为ForceMask的车身材质实际blend=0，
必须保留，不按名称处理。新增`DepthSurfaceFilter`仅供SceneDepth几何对应：按section真实材质槽
排除已核实blend=2/7，保留blend=0，未知模式拒绝；blend=1保留并披露尚未计算像素opacity。
物理净空几何完全不受此过滤影响。

`cnh-city-depth-surface-parity-20260924-v1`在v3对照上按上述属性排除143个实例section记录，
两个布局的数值门槛结果不变、仍FAIL。它证实“只删玻璃就能修好City”的方案不足，
但不能把此对照的无改善外推成原始Nanite渲染中透明几何无影响。单面剔除、fallback可见性
仍未分离；本次止于这一明确工程缺口，不把来源标为不合格、不继续扩大排查分支。

最终决定：优先让已过原数值门槛的Street完成剩余准入检查；City保留可恢复管线诊断，
旧FAIL不翻案。没有实施协议v2、5m缓冲带、轮廓豁免或新2布局，也未启20布局与训练。
备选受控插入物+简单背景保留，其写实度代价必须披露，本轮没有据此更改主基准。

验证：86项CNH针对性测试通过；原生插件v4/v5实际编译通过；文档索引15入口/131链接通过。
所有本轮UE/编译/审计进程完成，任务临时capture/temp已释放；源项目/地图完整性检查通过。
成功与失败采集、派生材质回执、源码快照、逐射线诊断、原生包和共享缓存均保留。

## 采集优先：首批 Development 数据与流水线提速

用户随后授权改变顺序：Street先采，剩余检查跟随数据，按布局隔离失败；City不挡采集。
本节更新执行顺序，不追认前述旧FAIL，也不改30/75mm、2%门槛。

**主机首批已经完成6个新布局、960帧**，人行道/交叉口/广场各2，单UE进程，
固定1m/s、离地1.6m、水平视轴，每段40个名义0.1s空间位姿。全部仍是一个Street200街区，
仅Development，不是独立训练/测试场地或384布局完成。场地清点有1190/458/6个保守净空位姿候选，
不是独立场地数；未确认巷道。广场候选均在原生广场铺装上，保守AABB拒绝不证明实际无路径。

| 布局 | 类别 | 抽样P95 / max（mm） | 原覆盖/近层规则 |
| --- | --- | --- | --- |
| street-dev-00 | 人行道 | 6.85 / 39.11 | PASS |
| street-dev-01 | 交叉口 | 0.06 / 0.06 | PASS |
| street-dev-02 | 广场 | 7.45 / 23.05 | PASS |
| street-dev-03 | 人行道 | 6.84 / 22.29 | PASS |
| street-dev-04 | 交叉口 | 0.06 / 0.06 | PASS |
| street-dev-05 | 广场 | 7.00 / 18.42 | PASS |

固定每布局centre/0、boundary/19、removed/0，共18帧；不是全序列通过。
首批起点8m网格导出不保证末端5m视锥完整；后续已按轨迹位移扩大导出半径。
新STATIC_SPATIAL_V1关闭时间累积，RGB预览存在空间锯齿/颗粒，不继承旧TAA画质等价性。
植被静态、SceneDepth不含不写深度玻璃；玻璃仍保留在物理净空几何中。

七路格式960帧PASS，渲染脚本481.89s，全流程750.47s（含格式转换220.51s）。
新版v6异步管线另用已消费首布局160帧验证：两对RGB/depth加一个四属性任务共享queue=4；
32项同目标同步参考逐字节相等，480/480任务完成、0失败。
实际camera写入起点跨度73.04s→23.33s，约3.13倍；统计包含队列反压、不含启动与最终格式转换。
ID保留已验证的隔离深度比较方法，尚未切换custom stencil。

副机3060已完成同布局160帧跨机对照，地图/引擎版本相同、插件及采集脚本哈希相同。
8个对应端点近场394,053像素深度最大差0mm、有效性/5m成员关系/目标与干扰物mask差均0。
但近场背景着色法线角差P95约0.115°、max22.236°，远景也有差异；全模态跨机混用未准入。
不据此停副机单机Development采集，机器身份和配方保留在每帧中，不固定绑定环境类别。

主机960帧规范格式逐值回读通过：EXR有效深度完全一致，normal float16最大分量量化误差
0.000244140625、albedo误差0；ID编码回放一致不等于独立标签精度通过。
保留所有规范文件、相机/几何/源码/回执及每段首尾isolated depth，移除5664个冗余文件、
8,759,611,392字节（8.158GiB），删除逐路径校验并留回执。历史transport哈希回执不改写。
真实EXR→CNH代理→固定单峰/双峰接口2帧烟测通过；反射率/入射角是假设，净空保持UNKNOWN，
不报告算法准确率。能量收敛、全帧标签精度和正式资产/场地隔离仍独立记录。

City近场派生副本真实预检止于混合近/远ISM：同一组件同时含范围内外实例，
在任何复制/赋值前拒绝，源场景未改、0帧、进程释放。精确拆分还需保留附加随机种子、
实例索引/排序、custom data与组件bounds；Python属性访问不足，不能只复制主seed假称远景不变。
这是实现能力缺口，不是City来源失败；本轮不继续扩大City支线。

核心回执均位于 `artifacts.local/evidence/`：
`cnh-street-development-batch-20260924-v1`、`cnh-street-development-sampled-geometry-20260924-v1`、
`cnh-street-development-postprocess-20260924-v1`、`cnh-street-seven-async-parity-20260924-v1`、
`cnh-street-crossmachine-comparison-20260924-v3/v4`、`cnh-city-nearfield-derived-20260924-v3`。
持久控制器队列在work根`development-queue-v1.json`；机械失败最多一次新路径重试，
质量失败只隔离受影响布局，已完成布局不重采。队列记录不代替几何/标签准入。

副机追加固定6个新布局也已完成960/960帧格式PASS，总计12个新布局、1920帧。
副机v6单进程原生脚本183.67s、全流程420.59s（格式整理192.06s），2880/2880异步任务完成、
32/32参考字节一致、源项目未改、进程释放。原始数据保留在副机
`G:/DevWorkspace/BlindAssist/artifacts/evidence/cnh-street-development-worker-batch-20260924-v1`，
主机thin回执位于`cnh-street-development-worker-batch-thin-20260924-v1/payload`，62文件hash核验通过。
副机这批几何抽样尚未运行；两批能量、完整标签与独立性仍PENDING。用户再次要求提高效率后，
停止新增优化和诊断，只完成已运行批次；没有继续开采或遗留活动UE任务。
本次121项针对性测试、插件v6-r2实编译、真实异步字节对照与文档索引检查通过。

## 两机数据接口与自建场地扩建

随后按用户要求补副机新批次抽样：固定18/960帧、6/6布局通过原30/75mm与2%规则，
最大P95为7.034mm，最大误差32.045mm；CUDA审计11.685s。没有重新采集或启动UE。
回执为`cnh-street-worker-sampled-audit-20260924-v1/result.json`，对应
`cnh-street-worker-quality-ledger-20260924-v1/result.json`仍仅允许临时Development使用。
两机同端点比较复用前节v3/v4结果：深度/插入物ID近场一致，背景法线不一致；
本轮CNH接口固定rho=0.5、incidence=1，不使用该法线，不能据此宣称全模态兼容。

两批清单合计人行道、交叉口、广场各4布局，共1920帧，巷道为0，仍同一Street200街区。
用户指出Street200为自建场景，明确可按生图规划扩建：优先补巷道和不同物理场地，
City继续暂停。生图仅为建筑布局概念，不是现有场地实测或独立性验证；保存于
`cnh-street-expansion-concept-20260924-v1/street200-expansion-concept.png`。
概念A为砖服务街区3m直巷与小广场，B为商业4m折巷，C为住宅3.5m巷道与庭院。
先落地A样板的独立新地图，复用现有普通背景资产但不改已消费Street200地图。
普通背景可共享不等于物理场地独立已获证；后续需按新地图真实几何划分场地，
四类环境均应在各分区覆盖，不能将A/B/C简单当作已确认train/dev/test。

端到端接口采用H3 `[8,8,16]`直方图和`[8,8]`ambient，1088维输入接一个六输出线性层；
固定Adam学习率0.001、30次全批迭代，不扫参、不按结果延长。每台机器每类环境按布局ID
排序，第一个布局训练、第二个布局作debug validation；全部clip/pose随布局，6+6布局各960帧。
共享一个物理街区和插入物资产，所以此划分仅检查接口，不满足正式场地/资产隔离。
原生/插入物三角形与六个闭查询盒的SAT接触作为独立监督文件，物理几何保留玻璃，
缺失覆盖以-1 UNKNOWN屏蔽；观测文件不含网格、实例ID或几何标签。
主机960帧转换耗时426.16s，六查询正例数168/84/168/84/333/165，UNKNOWN为0；
这仅是已加载世界导出表面的几何监督，不等于独立标签精度验收。
removed的240帧均为负：保留的干扰物横向在查询通道外，不能宣称已覆盖移除后通道内干扰物正例。
主机第一次接口运行因governed runtime预建空输出目录而在读数据前拒绝，保留v1失败；
修正为允许空目录、仍拒绝非空目录后，v2完成，没有改变标签或门槛。

扩建已在UE生成并实际查看两种视角。V1真实图暴露玻璃店面立面与外围悬空；
V2换opaque墙并补地基，但地基纹理被巨大缩放、墙面仍偏浅石材；两版地图和图像保留。
最终V3为`/Game/BAResearchExpansion/BrickServiceA_V3`：按真实Wall09边界拼接，
3m净宽、27m巷长、12×12m广场、6m入口。墙组件引用现有棕砖材质，运行时确认opaque且无材质形变；
外围使用新建无纹理粗糙灰材质，明确属于简化背景，尚非完整城市美术场景。
原Street200地图、源网格/材质文件及uproject哈希未变，三次作者会话均释放进程与临时目录。
V3实际巷道与完整俯视图已目视核对，无前述玻璃/黑void/巨幅纹理问题；这是可继续布置的几何样板，
不是新数据验收PASS，尚未计入1920帧，不冒充四类环境齐全或新独立测试集。
配置入口为`cnh_street_expansion_a_v3.json`，构建/预览/启动脚本为`cnh_street_expansion_*.py`；
构建及实际图位于`cnh-street-expansion-a-20260924-v3`。AI概念图与实际UE截图分别保留，不混称实测。

两机1920帧现已全链路完成，回执`cnh-street-e2e-train-20260924-v1/result.json`状态
`PASS_DEVELOPMENT_E2E_INTERFACE`。副机960帧转换704.09s，四产物回传hash逐项相符；
两机紧凑输入各约2.43MB，没有复制整套原始帧。主机CUDA线性训练与保存/重载校验2.922s，
模型重载预测逐位一致；输入1920×1088，训练与debug validation各960×6=5760个查询。
训练TP/FP/FN/TN为1061/505/51/4143，调试验证725/1315/167/3553，UNKNOWN均0。
这些较多误报说明极简基线远非可用算法，当前结论仅为格式、监督与训练接口跑通；
没有调参救分，没有正式测试，也没有补造传感器或独立性证据。
11项新增针对性单测通过，三次UE作者运行和一次全量CUDA基线实跑已完成；
副机与主机的本轮进程已释放。后续先用新增巷道样板扩建场地，City仍暂停。

### 参数化巷道与净空外原生杂物

按用户要求扩展为[参数化生成器](CNH_ALLEY_GENERATOR_20260924.md)，不继续扩广场或City。
R2配置生成9个不同几何场地，train/dev/test预分区各有straight/L/T，宽1.8–3.9m、
主段24–36m、支段14–22m，墙和地面各3种材质循环分配。每场8件杂物，共72件，
种类为垃圾桶、通风设备、管道、配电箱、托盘；资产来自现有CitySample工程，部分屋顶资产改用，
并非全部已在原Street200地图实例化。背景资产跨分区共享，不使用受控障碍资产族。

先按源网格真实边界计划，再以UE实际组件完整AABB复核墙和杂物到指定保护体积≥15cm。
九场全通过，最小间隔约19cm；保护体积和支撑地面例外明确写在生成器说明中，
不把本检查扩充成任意真实采集姿态或30/75mm深度验收。每场8件完整生成，无隐藏/删减过关。
几何签名和持久登记阻止同几何仅换名、换种子、换材料后重新跨区分配。

R1九图几何完成，但作者看图发现15m屋顶管道和偏大配电箱，以及预览曝光偏亮。
R2均匀缩放管道至约3m、配电箱至约0.71m；预览降低1.5档曝光并关辉光，保留原材质。
R1/R2属于同九场地修订，不计成18个场地，旧证据保留。
R2单UE会话101.422s完成9图与27张1280×720预览；6项针对性测试通过。
逐场地入口或内部图已目视检查，另查L形俯视图。曝光/巨型管道问题改善，
仍有简化的外围空场、部分立面纹理尺度和屋顶通风设备安装支架不足，不称完整写实城市。
源网格/材质、原Street200及A区V1–V3地图、uproject哈希未变，进程与临时资源已释放。
证据为`cnh-street-alley-generator-20260924-v1/v2`，最终使用R2；
`build-run/compact-audit.json`给出每场杂物数与最小间隔，持久登记含9场地。
本轮交付作者工具和场地，没有新增训练帧、没有正式划分准入或模型分数。
