# MZ12 existing-data inventory and frozen replay

User requests reuse of the existing20000 frames before further collection.
Audit authoritative manifests for old5000, relation10000 and distance5000,
including partition/configuration coverage, prior usage and RGB identity overlap.
Do not count canaries, retries or partial captures as additional accepted data.

One EXPLORE diagnostic, no fitting or threshold selection: select ALL relation
DEV_ONLY2000 plus ALL distance dev1000 from the admitted final indices, in their
original order. Selection is by source partition only, before model scores.
This is consumed shared-asset Development, not independent confirmation. The
visual encoder and decoder trained on relation TRAIN5000 and historically used
DEV for selection; the distance source already had frozen historical inference.
Neither fact makes it useless for this different-layout regression question.
No EVAL pixels or new capture enter this replay. Metadata inventory may count
all partitions. Do not relabel already consumed partitions as fresh tests.

Compare original MZ5, fixed MZ9 adapted MZ5, MZ9 SOURCE and MZ11 positive-preserving
addition. Keep all checkpoint hashes, original MZ9 thresholds and MZ11 cutoffs.
The baseline encoders and decoder remain frozen. The shared underlying training
examples do not imply equal training effort or identical supervision.

Use original640x360 RGB with BOX256x144 preprocessing and established frozen
ContextEvidence feature extraction. Sensor preprocessing constructs generic
8x8 two-return packets from native depth, with the same40-bin first/last>=3pixel
rule. Reuse the previously verified vectorized packet implementation. Native
query membership, identity, camera pose and labels cannot enter model inference.
Full-image native positive-support events (>=3 valid pixels, radial<=4m) are
evaluator truth, matching the MZ series. False support is not a clearance claim;
record unknown native pixels and absent sensor packets separately.

Before replay, check16 previously consumed old TRAIN frames against cached
MZ1 visual features, packets and flags. No validation-set refit or tolerance
adjustment. Fresh-feature tolerance1e-4 and unchanged prediction signs are the
existing extraction checks; range tolerance1e-6m. Batch16 for extraction, frozen
weights only. A mechanical failure must be recorded separately, without changing
the scientific recipe or hiding partial results.

Report per-dataset four-bit exactness and per-query TP/FP; near-only wrong-far
and far-only wrong-near; family, site and complete group/pair correctness.
Distance1000 contains HEAD-positive near/far endpoints, no HEAD_ANY-negative or
BODY-positive denominator. Relation has mixed positive and negative controls.
Neither dataset is a new independent vertical-thin-pole confirmation.

Decision: assess whether MZ11's old-DEV gain survives these existing different
placements at unchanged thresholds. Require no extra FP per query in either
dataset, no lost original positives and some far TP recovery for a favorable
transfer diagnostic. If errors increase or there is no gain, report it and stop;
no training, threshold rescue, enlarged subset, collection or automatic successor.
Even a favorable result remains Development and does not override MZ11's failed
thin-retention gate. Preserve all source and result receipts and release temporary
delivery resources. GPU extraction/inference, CPU metadata and scalar metrics.
