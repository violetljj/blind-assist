# Track A 放量 v3 协议与 v2 失败记录（正式道，生成前冻结）— 2026-09-26

实验登记：`cnh-track-a-scale-v3-20260926`。执行者 Claude（用户授权）。

## v2 失败（原样保留，不用于正式结论）

`cnh-track-a-scale-v2-20260926`：192 单位生成完毕，G0 通过、划分层面 G2 通过（合格组合 36/35/36）、INCOMPLETE 1（audit unit 143，限额内），**G1 失败：63 对跨单位精确 1 cm 指纹重复**，按协议停止，未合成传感、未读出。原因是执行者的失误：为提速把单位拆成独立进程并行生成，生成器原有的“接受时全局去重”只剩单位内去重。重复集中在边界计划配置 30/31（43 对），即宽横杆在近乎固定位置按 1 cm 量化碰撞，与 v1.1 查明的根因相同。候选拒绝率 63.4%（只报告）。

## v3 与 v2 的唯一差异

新种子族 `cnh-track-a-scale-v3-20260926`。并行生成后，按单位 0..191、配置 0..31 的顺序运行去重修复（`cnh_track_a_v13_repair.py`）：与已访问配置精确重复的配置，用候选编号 1000 起的新候选（同一轨迹重抽 0..3、同一接受检查）重新生成，并对全部已访问配置去重；修复后重算该单位的检查与 G0。这恢复了单进程生成的去重语义。在 v2 失败数据的副本上验证：63 个重复配置全部修复，合并后 G0/G1/G2 通过、精确重复 0。修复无解则停止。

其余全部继承 [v2 协议](CNH_TRACK_A_SCALE_V2_PROTOCOL_20260926.md)（门槛、主检验、次要指标、S1cell 臂、快速余量、5 Hz）。读出使用与冻结实现逐位一致的 `cnh_track_a_scale_fast.py`（提交 d158ff12），四个条件共用一个进程池。
