# MZ42: real-mesh diversity and paired native-surface labels

2026-09-10 EXPLORE. Build one small, real-rendered diversity block before larger
acquisition. Use the first source-admitted MZ36 dense_candidate_05 site in saved
rank order (site001; rejected site003 is ineligible), its unchanged camera and
background, the established worker CitySample project/runtime/plugin, and four
existing mesh families: pipe, ladder, irregular pouch, and birch. The prior MZ38
scout verified files only; this experiment must load and render them.

Budget: one read-only NullRHI metadata probe, then one fixed20-frame capture.
Four new families each have HEAD_ONLY, BODY_ONLY, BOTH and visible nonintruding
variants (16frames), plus four retained same-camera oblique_rod controls. Keep
old sources untouched. No model inference, training, new assets, downloads, or
outcome-driven extra capture. Metadata must load all four StaticMesh assets;
failure is recorded rather than silently changing the asset list.

Use actual mesh bounds only for declared transform placement. Preserve original
material slots; no RGB-only image editing or opaque replacement to hide alpha
holes. Freeze the exact objects, transforms, camera, source hashes and spec before
capture. Keep mesh/scale/material fixed within each new family; BODY/HEAD move
the same form; all new variants contain two instances, BOTH places one at each
height, and the nonintruding forms remain
visible outside the body corridor. These are controlled arrangements, not a
claim that every placement is a natural scenario.

Label actual visible native surfaces using the existing camera/body query
geometry, never the object name, bounding box or intended relation. Retain all
20attempted frames, source/floor checks, observed relation, mismatched intent,
and UNKNOWN. Validate native depth/RGB correspondence and inspect all frames,
especially material holes, thin structures, support and visible negatives.
Report successful pipeline coverage separately from valid paired relation groups.
No failed family or subset disappears from the denominator.

Keep one canonical recoverable RGB/depth source on the worker, compact labels,
spec, hashes, receipts and diagnostic previews on the primary host. A lossless
compact transfer may retain original relative paths; verify decoded arrays and
file identity and avoid a new permanent dense-feature cache. Do not delete old
datasets or task evidence. Verify owned UE/Zen process release when finished.

This20-frame block is engineering/controlled Development evidence. Shared
CitySample backgrounds/assets, synthetic placements, native labels and any
ideal depth-derived ToF do not establish hardware or natural-scene performance.
Stop after source integrity, admission/limitations and the bounded delivery.

Metadata loaded all four meshes successfully. Their inspected base materials
are opaque and one-sided: the birch is a woody-form source, not demonstrated
leaf/alpha-mask coverage. Uniform scale/rigid rotation preserve mesh shape but
make these controlled forms rather than natural-size everyday placements.
The final pre-capture spec is `spec-v2/spec.json`, SHA256
`cc76970fe3f358c4cffca24d423cb1d81a73720d609a4e7dc21b8f3067cf7600`,
under `artifacts.local/work/mz42-rich-objects-20260910/`. The unrendered v1 draft
was replaced before capture to face the ladder plane toward the camera.

## Result

`MZ42_RICH_OBJECTS_PIPELINE_PASS`. The sole capture rendered20/20frames; native
surface, floor and source checks pass for all20, with all5complete paired groups
matching intended relations. No frame or event bit was excluded/UNKNOWN in this
block. This is source-pipeline coverage, not learned-model accuracy.

| Query | Positive frames | Negative frames | Native support pixels |
|---|---:|---:|---:|
| BODY_NEAR |10|10|24393|
| BODY_FAR |0|20|0|
| HEAD_NEAR |10|10|12524|
| HEAD_FAR |0|20|0|

Positive evidence is near-only; the20frames establish no far-positive coverage.
Independent NumPy geometry reproduces all80event labels/counts directly from
the compressed native arrays. Every attempted frame remains in the manifest.
Both the producing agent and root inspected all20RGB/native preview cells:
ladder openings and twig gaps align, and lateral negatives remain visible.
The obvious scaled/floating arrangements limit realism. `CLEAR` names the
visible nonintruding fixture variant and is not a device-clearance result.
Opaque woody branches do not validate leaf alpha masks or foliage sensing.

The92-file canonical capture compresses42,326,459bytes to20,489,306bytes,
51.59% smaller. Worker verification compares every decompressed byte to source;
primary verification rechecks all92hashes and reads all20RGB/native pairs
without extracting raw files. No original data was deleted and no dense visual
feature cache was created. Raw evidence remains at worker
`G:/DevWorkspace/BlindAssist/artifacts/work/mz42-rich-objects-20260910/capture-v1`;
its owner is MZ42. The primary recoverable source is
`artifacts.local/work/mz42-rich-objects-20260910/returned-v1/capture.source.zip`.

A missing worker label-helper import was repaired using a task-local snapshot
after capture/world verification; the failure log remains and no scene was
recaptured. Metadata and capture lifecycle receipts pass; final inspection finds
no task-owned Unreal process or listener on the used Zen port25229.

Canonical final evidence is the task root's `result.json`, `receipt.json`,
`visual-review.json`, `return-verification.json` and `resource-release.json`.
The returned dataset retains labels, per-frame counts and the contact sheet.
All fits/inference counts are zero. This bounded source delivery is complete;
existing fixtures, model baselines and prior sensor findings remain unchanged.
