# MZ48: kilotier paired object and context source

2026-09-11 EXPLORE source acquisition. The user explicitly authorizes
secondary-worker collection in thousands. MZ45/MZ46 show that multiplying
one fixed support arrangement would preserve a known shortcut. Collect
2,560 intended frames with target/context pairs, multiple sites, ranges and
sizes before the next larger learning comparison. No model fit or inference
is part of this source-only experiment or the separate fixed MZ47 fit.

The fixed Cartesian design is8 sites x5 families x4 relations x2 support
contexts x2 ranges x4 size/position settings. Reuse actual pipe, ladder,
pouch and leafless woody birch meshes plus the retained oblique rod. This
extends geometric/context coverage; it is not evidence of natural foliage
or calibrated hardware. Preserve native materials and all intended frames.

Use existing fully admitted MZ36 camera/floor sites: region05 sites001,002,
004,007 and region06 sites001,002,003,004. These are the first fully admitted
site for each heading in the two regions; exclude failed05-site003. Transform
local target/support geometry into each camera heading with loaded mesh bounds;
do not reuse a world-X-only anchor at a rotated camera. Freeze all specs and
asset/runtime/source hashes before rendering.

New-mesh scale factors are0.75,0.90,1.05,1.20. Near front positions are0.85,
0.90,0.95,1.00m; far fronts are1.90,2.10,2.30,2.50m. Use source-bound local
dimensions for relation placement and small predetermined lateral offsets.
The rod retains its shape and varies position. A fixed12-part lateral support
rig is shared across families and relations, with constant clamp height and
supports outside the body/head queries. It must not encode target relation.
Each pair has exactly equal target object dictionaries, one with and one
without the rig. Expected relations remain intent, never labels.

Predeclare region05-site002 and region06-site004 as future Development
held-out sites:640 frames. The other six sites supply1,920 training-candidate
frames. Pair/group/site identities remain attached and pairs never cross this
split. These are existing consumed source sites held out from a future fit,
not fresh blind or foundation-model-independent evidence.

First capture64 stratified frames, eight per site, covering all families,
relations, ranges and settings across the canary. They count toward2,560.
Require source/render/floor/native health for all64, exact target-pair
identity and paired native event-mask equality, and at least56/64 intended
relation matches before expanding. Inspect all canary previews. A failure
stops expansion and preserves actual labels, UNKNOWN and failure evidence;
do not silently relabel, discard, recapture or change geometry from model
outcomes. Mechanical source failures may be diagnosed explicitly. If the
canary passes, capture the remaining2,496 in bounded per-site shards.

Audit all frames with the existing native geometry/source checks, report
intent matches, true label balance, UNKNOWN and pair invariance separately.
Keep full original native/raw evidence on the worker. Return a lossless
training package with original RGB bytes, range/validity packets, native event
and local-contributor supervision, metadata, source/entry hashes and the64
predeclared native canary samples for independent primary checks. Local
contributor labels describe IDEAL selected bins; DROP_CLOSE can mask deleted
slots, while MERGE midpoints cannot inherit those labels. No native truth or
instance identity enters predictor input.

Use compact shards and no persistent dense-feature duplicates. Record total
raw/training-package bytes and actual transfer, not hypothetical disk savings.
Verify package bytes/hashes and release owned UE/Zen/worker jobs after source
delivery or failure, retaining evidence. Stop after this2.56k source, its
checks, concise record and scoped delivery; no automatic larger batch or
training on held-out outcomes is included.
