# Track A 放量协议（正式道，生成前冻结）— 2026-09-26

实验登记：`cnh-track-a-scale-20260926`。执行者 Claude（Codex 额度用尽，用户授权接手）。用户决定（2026-09-26）：放量；主比较扩为三项并做 Holm 校正；提醒层面指标作次要；学习器 L2 不在本次。本文件与所用代码在任何放量数据生成前提交；之后不改门槛、τ 候选、指标或检验。

## 数据

- 种子族 `cnh-track-a-scale-20260926`；192 个独立单位：0–95 train、96–127 calib、128–191 audit。每单位 32 配置 × 12 帧（5 Hz），calib/audit 另有 10 Hz 标签与渲染。
- 生成器与传感合成沿用 v1.3 试采代码（`cnh_track_a_v13_generate.py`、`cnh_track_a_v13_sensor.py`、`cnh_track_a_v13_merge.py`，仅增加种子族/划分参数）：75% 现实尺寸 + 25% 小块层 + 宽横杆，按划分分族；配置不可行时轨迹重抽 ≤3 次；SNR 3/6/12 由 G3 参考面设档；安装 M0=0°、M1=−10°；可见性真值只为 audit 保存。
- 物理标签、5 cm 边界带、主子集定义（第 4–12 帧且非边界）继承 v1.1/v1.2。

## 门槛（全部硬停止，失败即不进入读出，不换种子、不替补单位）

1. 192 单位全部完成（无 INCOMPLETE）；G0 全部通过。
2. G1：跨单位精确 1 cm 体素重复为 0；近重复（MinHash 候选 + 精确 Jaccard ≥ .98，某单位 ≥90% 配置有伙伴）为 0。
3. G2：每个单位通过（计数按 ⌈旧值×N/160⌉），每个划分合格组合 ≥20。
4. G3 参考面 200 次校验三档均在 ±10%；G4 两安装 × 三 SNR 与标签逐帧对齐。

候选拒绝率只报告、不作门槛：v1.3 按用户决定改了物体尺寸，试采实测 53.9%，拒绝只依据几何与去重，不读传感或读出。G5 不运行（本次不含学习器）。

## 读出（只用 H3、公开 ambient、公开 T_Q_tof、GT 或带噪自运动）

B0；B1-R（K4，r² 补偿，v1.2 冻结实现）；S1；S2（K4，r² 补偿）；S3（S2 与 8 帧近场记忆取大）；次要臂 S2r4（K4，历史帧按 (r_now/r_past)⁴ 降权）。偏置 = calib 单位逐 cell 中位数；τ ∈ {.25, .5, .75} 每臂在 calib 上按 HEAD/BODY 宏平均 AP 的均值选，平局取小；报警阈值每组在 calib 主子集上取 FPR ≤ 5% 下召回最高者，平局取高阈值。audit 不参与任何选择。

## 主条件与主检验

主条件：M1、SNR6、5 Hz、带噪自运动，audit 主子集。主指标：各组布局宏平均 AP（各 audit 单位 AP 的均值；缺正例或负例的单位在该组排除并报告）。

六个主检验（单侧，H1：前者 > 后者）：S2−B1-R、S1−B0、S3−S2（带噪自运动）× HEAD/BODY。按 audit 单位配对 bootstrap，B = 10000，种子 20260926；p = (1 + #{Δ* ≤ 0})/(B + 1)；Holm 逐步校正，家族 α = 0.05；报告点估计、95% 百分位区间、为正单位数。另按 v1.2 原规则报告：S2−B1-R 两组 95% 下界均 > 0。

## 次要（仅描述，不作结论）

M1 下 SNR3、SNR12；M0 下 SNR6（均 5 Hz、主检验同样计算但不作判定）。S2r4 臂。正例分层（小块/现实/宽；ToF 射线可见/不可见）。报警工作点：calib 阈值下 audit 实际召回与 FPR（超过 5% 不回调）。提醒层面：audit 序列 × 查询，只用第 ≥3 帧；有正例帧即为一个事件；事件内任一正例帧得分 ≥ 阈值即“已提醒”；报告事件召回、漏提醒数、首次提醒帧的盒内最近表面前向距离中位数、无事件对中出现报警的比例。

不运行：10 Hz 读出、学习器 L2、三态输出、BODY 行走坐标。

## 预算与边界

墙钟 ≤ 8 小时、存储 ≤ 60 GB；代码 `cnh_track_a_scale_evaluate.py`。受控程序化仿真，模拟器未标定，结论只针对本仿真条件下读出方法的相对效果，不代表硬件或真实使用表现。
