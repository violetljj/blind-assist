# MZ69: frozen weak-input transfer to controlled new shapes

EXPLORE, zero-fit transfer. Does MZ68's missing-input training carry useful
obstacle sensitivity from the consumed MZ61 source to the complete MZ67
topology source, and what near/head and false-alert costs accompany it?
The hypothesis is that its additional weak-input sensitivity transfers to
different controlled shapes rather than only shifting scores on MZ61.
This checks transfer before deciding whether further learning needs more
data or a different confidence/readout mechanism.

Evaluate sealed MZ64 GEOMETRY and MZ68 NULL_COVERAGE against each other and
OLD_NEG/UNION. Retain MZ37, OLD_NEG OPEN/GATED, both candidate branches and
both final unions, with all original weights, normalization and four cutoff
vectors. No fit, threshold sweep, per-query selection or availability router.
MZ66 remains the recorded projection negative control; projection is not an
intervention in this missing-input transfer comparison.

Bind MZ67 source-index SHA256
`23aa6f552e38a0534052d2723b9a447ee91574bacacd503ecc64957c81b6560b`.
Use all 4,096 frames from its ten original compact archives: the 2,048 TRAIN,
1,024 CALIBRATION and 1,024 HELDOUT_GEOMETRY roles are descriptive here.
None are used for fitting or calibration. Keep all four target families,
relations, range/support/site conditions, incidental positive bits and UNKNOWN.
The source was inspected and curated during collection: this is controlled
Development transfer, not protected fresh confirmation or unseen natural
category evidence. Its complete count/presence collision audit remains part
of interpretation; distinct geometry IDs do not prove distinct supervision.

Apply unchanged IDEAL, MERGE_CLOSE and DROP_CLOSE transformations plus
ALL_INVALID (all-zero ranges and all-false validity). These are controlled
stress inputs, not a calibrated VL53L8CX simulator or failure probability.
Prediction reads only original RGB, observed packets and fixed calibration.
Native geometry/depth, event labels, local known masks, roles, families and
pair identities remain evaluator-only. Missing readings imply no clearance.

The primary paired description is MZ68/NULL_COVERAGE/UNION versus
MZ64/GEOMETRY/UNION on HELDOUT_GEOMETRY, separately for DROP_CLOSE and
ALL_INVALID. Report per-query TP/FP/FN/TN and every TP gained/lost and false
bit added/removed. Attribute newly true branch-winning cells to native,
known non-native or locally UNKNOWN. Also report native additions beyond
OLD_NEG, all roles/profiles/families/ranges/support conditions and paired
support effects. Do not replace event truth with winner attribution.

For each primary profile, a descriptive transfer flag requires at least one
new true native-winning event over GEOMETRY and zero newly false event bits.
This is not a replacement gate: report retention losses and all numerical
tradeoffs even if that flag passes. A gain with costs is retained as an
explicit tradeoff; no gain leaves useful transfer unproved. Neither outcome
establishes a trained ceiling, insufficient sensing information or natural
performance. Invalid input/model identity is NOT_EVALUABLE.

Budget: one streamed 4,096-frame GPU inference pass, batch 16, four packet
profiles; zero training steps and one independent saved-output CPU score.
Decode each RGB once; share its unchanged global256x144 BOX, crop224 and
full640x360 encodings across profiles and compatible heads. Differently
trained views stay distinct. No permanent dense feature cache or old-source
replay. Record actual backend/device, view counts, decoding/encoding/readout
time and file bytes. Run only after primary UE/model resource release.

The independent score reconstructs decisions from raw scores, support,
original cutoffs and MZ37, then reconstructs unions and scalar-recounts
events; compact native attribution is read only after inference. Preserve
all source/checkpoint bytes. Check fixed state/buffer identity and the
all-invalid unavailable-anchor contract. Mechanical repairs preserve failed
logs and the same scientific inputs; they do not authorize a new model or
outcome-selected threshold. Release images, archive handles and task-owned
processes on all paths. End this comparison after its score, scoped delivery
and cleanup; continue the broader authorized goal from the actual evidence.
