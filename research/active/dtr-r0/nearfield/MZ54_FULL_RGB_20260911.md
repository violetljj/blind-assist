# MZ54: matched native-scale crop versus full-RGB spatial learning

2026-09-11 EXPLORE. MZ51 finds352 BODY_NEAR events without a45-degree local
witness; MZ52 supplies full-frame labels for all2,560 existing frames. Test
whether a wider RGB/local-supervision field improves near-body recall without
increasing old or new false-alert totals. Keep ToF observability unchanged.
Global RGB already sees a resized full image; this changes the local branch.

Fit CROP_RASTER and FULL_RASTER for600 steps each, with identical trainable
architecture and initial MZ50 weights, no optimizer state. Both use a41->32->4
head, local Conv64->16 and frozen original encoder/channel normalization.
The extra sensor-coverage input starts with zero weight; first40 head columns
and existing local/output weights copy MZ50. Native-scale inputs are the exact
224x224 crop or full640x360 RGB; no aspect-ratio distortion or backbone fitting.

Use MZ52's45x80 raster of8x8 blocks and exact camera pixel-center geometry.
CROP reads/supervises only756 fully contained blocks, rows9..35/cols26..53;
the crop begins at y68, so partial boundary blocks must not receive hidden
native-pixel labels. FULL uses3600 cells. This is a matched raster control,
not bitwise legacy3136-angular-sample replay. Keep fixed MZ50, MZ51 individual
readouts, MZ53 unions and MZ37/global comparators visible as external references.

Per cell, use RGB token16, masked RGB angular-bin mean16, absolute coordinate2,
relative angular-bin coordinate2, observed range slots2, validity2 and sensor
coverage1. Only cells inside the exact45-degree square may gather the original
8x8 ToF packet. Outside it, ranges are inert zero placeholders with false
validity and coverage. RGB grouping regions outside this field are not sensor
zones. No native depth, cell truth, object identity or source profile label is
a predictor input. A negative RGB score is not measured clearance.

Reuse exactly MZ51 schedule.npz shared/OLD_NEG/query: each step8 shared new
frames plus8 original-TRAIN query-negative presentations, Adam0.001, seed151.
Cycle IDEAL/MERGE_CLOSE/DROP_CLOSE identically. Use the existing balanced
native local loss plus0.25 witnessed frame BCE, and0.25 selected-negative
OPEN softplus. Mask local UNKNOWN/candidates and exclude full-positive frame
loss when that arm has no witness. Other queries in a negative presentation
are not assigned a negative target. Only1,280 MZ48 fit and original old TRAIN
IDs enter optimization; actual shared schedule visits1,250 of these new rows.

Preserve256 rich calibration,640 held-out-site,384 nonfit-family and all
source pairs. For each arm derive exactly one cutoff vector on old1,000 DEV
plus256 rich calibration, DROP_CLOSE, with the unchanged zero-added-FP rule
and MZ37 additive baseline. Two vectors total; no search, fit extension,
checkpoint selection or threshold rescue. Evaluate all original six cohorts
and three profiles, including MZ36's400 attempts/80UNKNOWN. Reuse all saved
baseline arrays byte-for-byte; do not rerun global comparators.

Primary comparison under DROP_CLOSE: FULL should gain BODY_NEAR true events
over matched CROP on1,024 new nonfit frames, with no higher total nonfit FP
or old noncalibration FP. Report every query's TP/FP/FN, exact frames and
paired gains/losses, including ideal/merged costs and external comparators.
Also restrict FULL's same logits to CROP's mask using the existing FULL
cutoff, without refitting or a third cutoff. This diagnoses pooling support,
not an identical-input crop ablation, since full features/context remain.
Save winning cells, native-winning flags and sensor/crop coverage. Stratify
recovered events outside45 and below the crop; neither correct decisions
without native attribution nor old positive retention alone proves local skill.

Use GPU for encoder/head computation. Verify exact model initialization and
synthetic raster/sensor/mask gradients before actual fitting. Hash RGB during
reading, load no old native depth, and reuse existing old crop mmap. Prepare
only4,783 unique training full features in a task-owned temporary float32
mmap (~4.41GB); stream evaluation and discard dense batches. New crop training
features need~251MB. Keep batch16 padded encoder calls and fixed arithmetic;
a batch-memory failure allows mechanical recovery only with unchanged source,
explicit numerical checks and retained failure evidence. No permanent dense
cache. Record backend/device, input/transfer/cache sizes and stage timing.

Budget: two600-step fits, two cutoffs, one saved-output score and its focused
verification. Keep one source/initialization identity, retained mechanical
failures and all frozen comparators. Stop after result/disposition, scoped
delivery and removal of exact task-owned scratch with handles released.
An accepted gain is a controlled consumed-Development challenger, with costs
reported; a failure retains the matched control and identifies the limitation.
No protected blind, physical VL53L8CX calibration or default-App promotion.
