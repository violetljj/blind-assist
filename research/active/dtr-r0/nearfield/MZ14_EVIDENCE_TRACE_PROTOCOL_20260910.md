# MZ14: trace local evidence before changing representation

EXPLORE, consumed Development, 2026-09-10. User authorized trying the diagnostic
order in MZ_RESEARCH_REASSESSMENT_20260910.md. No capture, fitting, cutoff change,
temporal restart, protected access or baseline replacement in this bounded run.

Question: do current failures originate in selected-return coverage, angular
candidate geometry, local source discrimination, or query aggregation? Hypothesis:
the four maxima conceal distinct attribution and geometry failures that a final
score gate cannot diagnose. MZ5 remains the baseline; frozen MZ9 SOURCE supplies
local evidence, and frozen MZ13 supplies the previously observed added alerts.

Inspect all old DEV1000, all MZ6 clean200 (including the paired pole25+25), and
all MZ12 relationDEV2000 + distanceDEV1000. These are reused diagnostic samples,
not independent confirmation. Report output bits, configurations/groups, and the
single pole configuration separately. No outcome-selected sample extrapolation.

For each query record native full-image truth, true query contributors to selected
returns, contributor support surviving the existing mean-range/midpoint eligibility,
the best correctly supported local score, the winning zone/return/subcell, and
its actual source/query contributor counts. Distinguish no contributor at that
location from a real contributor outside the claimed query. A subcell may contain
multiple surfaces; contributor presence is not exact pixel identity.

Two evaluator-only interventions hold weights/cutoffs fixed: restrict maxima to
cells containing actual selected-return contributors; then restrict to actual
query contributors. The first isolates return-location inconsistency; the second
also uses true query membership and is intentionally an oracle. Neither is an
inference method or measured achievable upgrade. False-positive removal by an
oracle is not new model accuracy. Max removal cannot increase a negative score;
if no correct candidate clears the frozen threshold, pooling alone cannot restore
that query under these weights.

Paired local RGB diagnostics use cached normalized frozen features and native
contributor labels only for evaluation. Compare foreground and same-zone
background responses to adding the pole. Measurable response does not establish
generalization; weak response does not prove information-theoretic absence.

Acceptance: reproduce prior SOURCE signs (report numerical discrepancies rather
than hide them), verify packet/truth identity, decompose all selected MZ13 added
false bits, and produce reproducible case traces. Choose the next intervention
from the observed failing stage; stop this diagnostic without training a successor.
Use CUDA for tensor inference/reconstruction, CPU for summaries and figures.
Preserve UNKNOWN/no support, input hashes, timing, outputs and failed receipts.
