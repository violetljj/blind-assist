# v1.2 前置核对：G1碰撞与距离补偿

只读核对旧v1.1元数据，不重生成，不改其FAIL。证据在`artifacts.local/evidence/cnh-track-a-v12-rootcause-20260925-v1/g1-pair-rootcause.json`；精确1cm重复判定仍以旧`readonly-audit.json`为准。

|单位/配置对|原始seed|锚点连续顶点最大差|
|---|---|---:|
|0/18–1/18|39260982 / 39261991|3.217mm|
|1/17–3/17|38261988 / 38264006|7.468mm|
|8/19–9/19|40269057 / 40270066|4.136mm|

配置种子实际已经包含单位：`20260925 + 1009*unit + 1000003*(config+1)`。尾部固定的边界Z=.26/.32、固定类别Y和毫米级中心/尺寸变化导致1cm指纹碰撞，不是单位seed遗漏。更换统一hash派生便于隔离用途，但**仍需精确指纹去重**，不能把换seed本身当修复证明。

距离平方假设只支持将每个历史贡献乘g=(r_past/r_now)^2。原27724.557已混合过去与当前，整体再乘(2/1.8)^2会同时错误放大当前帧；不能由这个算术近似认定剩余L1只差1%。正确方差为g²v，空间分摊时同一个源cell对同一窗口的系数先相加再平方。距离衰减补偿不恢复角分辨率、入射角、遮挡或视场截断，也不自动提高单次测量SNR。

固定窗口Z在声明的独立近似零假设下可近似标准正态；取多个窗口最大值后不再是标准正态。所有最终scan报警仍由独立calib（旧Part0为alley train）选阈值；不把max Z解释成单次高斯检验p值。
