# MZ45: fixed-model transfer to richer near/far objects

2026-09-11 EXPLORE. MZ43 improved restricted-input decisions on earlier fixtures
with a small old-EVAL false-alert cost. MZ42/MZ44 now supply four actual new
mesh forms and a retained rod control at near/far distances. Hypothesis: the
fixed restricted-trained candidate transfers its benefit to these forms rather
than only the previous primitive fixtures. Same site/background and controlled
scaled/floating geometry limit this to object-replacement Development evidence.

Use all 40 admitted attempts (20 near, 20 far), preserving frame IDs, exact
native labels, UNKNOWN and ten four-state groups. The primary new-form cohort
has 32 frames; all eight retained-rod controls are reported separately. No
frame, mesh, distance or outcome selects a model or threshold. Original image,
native source and label bytes remain in their verified compact source packages.

Prepare IDEAL, MERGE_CLOSE and DROP_CLOSE packets with the unchanged MZ40
operator. Native depth belongs only to packet generation and evaluation.
Predictors receive RGB, packet ranges/validity and fixed calibration; no family,
intended relation, native mask, event truth or source-quality label is a feature.
Read package members directly; create no extracted raw tree or permanent dense
feature cache. Reuse a single full/crop visual pass for all packet conditions.

Compare frozen RGB, original ToF/MZ5, MZ28, MZ35/MZ37, and both MZ43 mixed
ensemble and joint FUSION. Include its ideal-trained joint FUSION comparator.
All checkpoints, normalization, bank, and cutoffs are fixed. A 16-frame original
MZ36 replay must reproduce retained visual/packet/decision arrays exactly before
interpreting the new source. Compact/dense mixed readout decisions must match.

Report per-condition FP/FN, exact four-event frames and paired changes, by
family, near/far block and new forms versus control. Preserve source coverage.
Transfer support requires reduced restricted pooled FN versus MZ5 without
increased FP on the new forms; report each condition and control even if pooled
counts improve. Compare the joint head separately with its matched ideal head.
No superiority over MZ28/MZ37 follows unless their actual errors support it.
Inspect any remaining misses using observable support and branch disagreement;
these are diagnostic associations, not proof of a physical sensing cause.

Budget: one 16-frame adapter replay, one fixed 40-frame visual extraction and
three cached packet-condition readouts, one independent scoring/isolation check.
No fitting, calibration, recapture, threshold rescue or new seed. Mechanical
failures retain logs and cannot become model negatives. Stop this comparison
after its result and scoped delivery. This is not a VL53L8CX hardware test,
natural-scene generalization, deployment or safety claim.
