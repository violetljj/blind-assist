# MZ65 bounded execution and preservation

[Protocol](MZ65_ACTOR_POOL_20260911.md) and
[results](MZ65_ACTOR_POOL_RESULTS_20260911.md) describe one worker A/B/R sequence,
16 cases per arm. The selected MZ61 source IDs, world coordinates, geometry,
32/64 settling policy, readiness, asynchronous native writer and evaluation
arithmetic were fixed before launch. No source asset or map was saved or changed.

The versioned runtime changes one MZ48 collector file. Twelve other launcher,
lifecycle and evaluator files and fourteen label dependencies keep their old
bytes. The input manifest is SHA
`ba469b680c9fdc57bf3d5c5699699f771932457e917812d4de4d9e640a322362`;
the316033-byte candidate archive is SHA
`72c897b8b4966bfc26529fa01808ce8770b9e62e326f431e104487a0fad79c30`.
Exact reviewable copies are [collector](mz65_actor_pool_capture.py) and
[scorer](mz65_actor_pool_score.py). They execute under their original names
`city_pcg_capture.py` and `score.py` inside the frozen task bundle, with its
bound dependencies/configuration; the copies do not replace the shared collector.

The first driver stopped before creating runs or starting UE. PowerShell treats
`$Registration` and `$registration` as one variable; the typed string parameter
converted the parsed lock record back to a string. A versioned mechanical repair
renamed only the parsed variable at three references, with actual-lock smoke
and diff evidence. Original driver, manifest, archive and failure logs remain.
The v2 driver SHA is
`eee92d955277b6e77b9ce87ffc00cac2587016c524389871a358b9d94e3201cf`.
No case, source, quality criterion, model or48-frame budget changed; no capture
was retried. The persistent Exec sequence subsequently exited0.

The full worker driver took302.423 seconds. A/B/R capture commands took97.005,
87.032 and89.715 seconds including launcher/startup/exit. The three world checks
took9.464,3.633 and3.449 seconds; the scoring command took9.316 seconds. Native
geometry uses the existing CUDA kernels on the worker, not a CPU substitute or
model. The result separately records the actual device and each measured phase.
No simultaneous model execution is claimed for this worker comparison.

The returned28,747,001-byte evidence archive has SHA
`6e70024ab4ce68c440da66cd59894bf43a7dc3c15a4a1e0fd85d9f7a45904743`.
The controller verifies the complete package and member hashes. Sixteen image
panels were actually reviewed by root, covering48 captured views; each panel and
the original result is bound in `root-visual-review.json`. Saved scalar/float32
diagnostics explain the tiny packet differences without changing the sealed
scorer or original gates. No diagnostic reran the model or captured new data.

Evidence root: `artifacts.local/work/mz65-actor-pool-20260911/`.
`release-confirmation.json` and `returned-v1/final-release.json` confirm controller,
UE, Python and Zen release, no listener on the task port, and upload-script
cleanup. No scheduled task was created. Owner-local raw data, source inputs,
captured evidence, the returned package, versioned runtime and repair receipts
remain; no raw source was deleted. The runtime artifact junction is a retained
routing link in the evidence bundle, not an active process or reserved compute.

Successful execution and gross visual consistency do not override RGB residual
failure or the exact packet baseline limitation. The default collector is retained;
no natural-scene, input robustness, Android, deployment or safety claim follows.
