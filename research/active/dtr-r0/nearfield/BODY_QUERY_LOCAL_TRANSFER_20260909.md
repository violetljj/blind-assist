# Frozen LOCAL transfer contrast

2026-09-09 EXPLORE. One zero-fit comparison to decide whether the existing simpler
local-query decoder avoids JOINT's cross-query background sensitivity. LOCAL uses
one query's64 visual features plus fixed XYZ; JOINT mixes all12queries. Both
completed identical2000-step native-count training previously; no run is extended.

Freeze LOCAL SHA256
`15d0fc5acd0a26de1a6b0fe32a908a307e7be77ce9e0c37e7c73610693995ca8`,
original B, TRAIN normalization and original alert thresholds. No fitting or
threshold choice. Model inputs are RGB and fixed calibration only.

Use the existing53pair original-fixture background diagnostic and existing106pair
ordinary plus106pair matched-size simplified-fixture diagnostic. Preserve both
original failed primary source gates. These are consumed admitted-subset
diagnostics, not fresh confirmation. Do not alter any admission or denominator.
Infer LOCAL once on all600captured frames, compare to SAVED JOINT and B outputs,
and report near query hit, wrong-far, strict pairs and original alert parity.

LOCAL exceeding JOINT strict pairs by>=10percentage points in the original-fixture
background cohort AND both simplified-fixture arms supports preferring local
readout for further robustness work. Otherwise that proposed remedy is not
supported; retain the scoped existing components. No result grants deployment or
ordinal promotion. Stop after one inference pass, saved-probability check and
scoped report/delivery. No capture, paid resource, persistent worker or training.
