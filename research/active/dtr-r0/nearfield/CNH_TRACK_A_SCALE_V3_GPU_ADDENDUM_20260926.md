# Track A 放量 v3 协议附录：正式读出改用 GPU 实现 — 2026-09-26

用户决定（03:19）：停止 CPU 读出，正式读出改用 GPU。本附录在查看任何 v3 读出统计量之前提交；此时 CPU 读出尚未完成任何单位，GPU 已写出部分单位得分文件，但未运行任何分析或汇总。

1. 正式读出实现：`cnh_track_a_scale_gpu_run.py` + `cnh_track_a_gpu_readout2.py`（提交 becde8c9 起）。几何与查询权重为 float64，扫描为 float32。四个条件（主条件与三个次要条件）全部用该实现；统计分析仍为冻结的 `cnh_track_a_scale_evaluate.analyze`。方法定义、τ 与阈值选择、主检验与次要指标全部不变。
2. 已知误差：试采 unit 9 上与冻结 CPU 实现相比，B0/B1-R 相对误差 ≤1e-13，扫描类 ≤5e-7；12 单位试采端到端宏 AP 最大差 2.6e-6，τ 选择、六个主检验判定与提醒指标完全一致。
3. 预先指定的 CPU 对账：audit 单位 128、133、…、188（每 5 个取 1，共 13 个），主条件，用冻结 CPU 实现（`cnh_track_a_scale_fast.score_unit_fast`，逐位等于原 `score_unit`）重算，与 GPU 得分逐项比较，报告最大相对误差。若任一扫描类得分相对误差 >1e-5 或 B0/B1-R >1e-10，则停止采用 GPU 结果，改回完整 CPU 读出。
4. 被中止的 CPU 读出输出目录保留为未完成（不使用）。
