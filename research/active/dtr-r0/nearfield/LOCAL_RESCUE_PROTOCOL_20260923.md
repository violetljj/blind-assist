# Causal LOCAL-only rescue admission diagnostic

2026-09-23 EXPLORE, authorized by the user's request to try selective rescue.
Keep A, LOCAL, their thresholds and UNKNOWN unchanged. Prior transfer and
composite stability are consumed Development, including already known errors.
This is a new readout diagnostic, not a retry of their frozen experiments.

Question: can current/past public evidence reject LOCAL-only false alerts while
retaining its rescued true frames and first event alerts? Angular ownership
alone already failed the unchanged-interval opportunity; do not repeat it.

Use all 576 transfer and 576 stability frames. Read sealed public probabilities,
features, baseline and ToF; only clip boundaries/order control causal state.
No shape, placement, trajectory, time index, evaluator XYZ or labels enter the
gate. Labels join only after candidate predictions are saved. Histories reset
at clips and never use future frames. No fitting or cutoff search in this stage.

Compare A, unchanged A OR LOCAL, and four fixed admission rules for LOCAL:
two consecutive standalone LOCAL flags (any centre); two consecutive flags in
the current winning centre; any winning-query nominal regional footprint
overlap at range 0.3--3 m; and the same with original range upper bound <=3 m.
Also report the fixed nominal-range plus same-query confirmation conjunction.
These are hypothesis controls, not measured pixel ownership or free space.

Export LOCAL-only TP/FP distributions for scores, A evidence, range/support,
RGB contrast and past evidence; AUC is descriptive, not threshold selection.
Report full TP/FP/FN, events, false segments, first-alert changes, coverage,
UNKNOWN and every incremental row. Keep both cohorts and all families.

Advance a fixed gate only if BOTH cohorts retain >=80% incremental true frames,
remove >=50% incremental false frames, lose no LOCAL-detected event and delay
no LOCAL event onset. All A alerts remain structurally preserved. Passing is
only consumed-data feasibility; freeze and test new layouts/trajectories before
any reliability claim. If no gate passes, record the tradeoff and decide from
feature evidence whether a separately bounded learned gate is justified; no
automatic threshold sweep, neural fit, source pruning or App promotion.

Small scalar statistics use CPU TASK_NOT_GPU_SUITABLE. Governed research-ue
runtime records declared inputs, source identities and receipts. Preserve all
outputs; release no shared processes. Synthetic tests cover history reset,
causality, A retention and range boundary semantics. Audit saved metrics before
delivery. This diagnostic does not establish physical motion or endpoint delay.
