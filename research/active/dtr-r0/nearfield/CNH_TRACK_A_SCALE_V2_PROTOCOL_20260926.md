# Track A 放量协议 v2（正式道，生成前冻结）— 2026-09-26

实验登记：`cnh-track-a-scale-v2-20260926`。执行者 Claude（用户授权）。v1（`cnh-track-a-scale-20260926`）因每单位 G2 与 INCOMPLETE 被列为硬门槛而失败，失败原样保留、数据不用于正式结论；v1 的门槛设计是执行者的失误：12 单位试采全部通过不代表 192 单位中每单位都能通过。用户 2026-09-26 同意：换种子族按 v2 重跑；加入单格 z 检验对照臂；“确认畅通距离”作为后续方向（不在本协议内）。本文件与所用代码在 v2 数据生成前提交，之后不改。

## 与 v1 的差异（其余全部继承 [v1 协议](CNH_TRACK_A_SCALE_PROTOCOL_20260926.md)）

1. 种子族 `cnh-track-a-scale-v2-20260926`；单位 0–95 train、96–127 calib、128–191 audit。
2. 门槛：
   - 硬门槛：G0（所有已完成单位）；G1 跨单位精确重复与近重复为 0；G3；G4。
   - 标签多样性改为划分层面硬门槛：每个划分的 `check_unit` 全部通过（比例、按 N 缩放的计数、组合 ≥20）。单个单位的 G2 差距只报告。
   - INCOMPLETE 单位排除、不补；任一划分被排除数超过 5%（train >4、calib >1、audit >3）即停止。
   - 候选拒绝率只报告。
3. 速度：几何余量先用物体包围盒的解析上界认证，上界 < −0.05 m 时不解线性规划（标签、边界、贡献者与精确计算逐位一致；在试采 unit 8 上 32 配置全部一致、93% 余量由认证界解决）；存储的远处负余量可能是上界，只影响描述性分位数。不生成 10 Hz 标签，所有划分按 5 Hz 渲染。
4. 新增次要臂 S1cell：S1 只保留 1×1×1 窗口（查询支撑内逐格 z 检验，对应“期望背景 + 逐格残差”类做法），τ 同样在 calib 上选。
5. 读出条件：主条件与三个次要条件并行运行，结果不因并行方式改变。

主条件、六个主检验（S2−B1-R、S1−B0、S3−S2 × HEAD/BODY，按 audit 单位配对 bootstrap B=10000、Holm 家族 α=0.05）、τ 与阈值的 calib 选择规则、次要指标定义均与 v1 相同。audit 单位若因 INCOMPLETE 被排除，按实际单位数计算并报告。

## 预算与边界

墙钟 ≤ 6 小时、存储 ≤ 60 GB。受控程序化仿真、模拟器未标定；结论只针对本仿真条件下读出方法的相对效果。
