# Separate visible-HEAD diagnostic after full-extent source failure

2026-09-09 EXPLORE; frozen BEFORE model access. The original background-only
protocol terminated NOT_EVALUABLE: 0/60 complete pairs passed the inherited
full-target raster/identity gate. Preserve that terminal and its unmodified rows.

The source check incorrectly demanded complete bar extent for a task about
visible HEAD-range evidence. Original fixtures contain clamps/side supports;
unmatched or occluded edge rays cannot establish full-extent support. No such
pixel becomes CLEAR or verified target evidence in this diagnostic.

This separate consumed-source diagnostic uses the SAME 120 captured frames;
zero new capture or fitting. Replace only the inappropriate full-extent rule by
at least THREE distinct visible native pixels INSIDE the HEAD corridor and
intended range, each with a native collision MATCH to the exact bound target
component and scene-versus-isolated depth agreement <=0.03m. Use original BODY
and HEAD geometry. Preserve all other admission failures, counts and source hashes.
Keep the failed original reasons alongside the new witness records.

Require >=24/30 complete pairs per region, otherwise no inference. No replacement
samples, threshold search or later admission rescue. If admitted, execute the
same one frozen B/JOINT inference pass and four-metric identity-matched comparison
already specified in the original protocol. The original primary remains failed;
this can explain background sensitivity only in the admitted visible-evidence
subset. It does not establish size-confound robustness or fresh confirmation.
