# 实机接入：当前状态

2026-09-23 App v10.14.0：新增前台最近最多 20 秒留证、私有本地保存与静音回放、
可选 2 Hz 小图、手动导出/删除；安装检查只报告背景/中/左/右粗位置一致性。
Wi-Fi 相机和 ToF 分路失效，相机不可用时可明确保留仅测距模式，ToF 失效则 UNKNOWN。
原固件、10 Hz、750 ms 新鲜度及几何参数不变，未完成物理标定或锁屏运行。
用法见[展示指南](../../../docs/HARDWARE_OBSTACLE_DEMO.md)。

2026-09-23 App v10.13.0：按用户要求将实物前视展示设为默认启动入口，原首页从
“更多功能”进入。增加事件式语音、独立震动开关及中断/恢复提示；基础几何、10 Hz
采样、固件与过期门槛不变。行为和验证见[展示指南](../../../docs/HARDWARE_OBSTACLE_DEMO.md)。
这是入口与反馈工程更新，不将模拟研究成绩晋级为实机效果。

2026-09-23 相机延迟有界试验：[合并发送/提前释放缓冲 A/B/A](CAMERA_LATENCY_TRIAL_20260923.md)
完成。P95 为原版 200 → 候选 161 → 恢复原版 175 ms，所有快照可用；候选首次改善
未被证实为稳定显著收益，因此未晋级，实物已恢复 `atom-camera-wifi-v1`。
App v10.12.1、ToF 10 Hz 不变。没有完成物理现场到屏幕发光的光学延迟测量。

2026-09-23 延迟更新：ToF 最终固定 **8×8 / 10 Hz**（用户要求），固件为
`xiao-tof-8x8-wifi-v3`，UDP3335 主动推送；App v10.12.1。15 Hz HTTP 版本仅为
已结束的中间试验。45 秒最终测试 1300/1300 快照有效，ToF 时龄上界中位数/P95
71/132 ms；读出至手机接收上界中位数 14 ms。相机 VGA 保持不变。
新旧时龄估计方式不同，不能把百分比差异等同于物理事件到提示的改善。

2026-09-23：已完成相机与 8×8 ToF → 手机热点 → Android v10.12.0 直连展示。
两板当前分别为 `atom-camera-wifi-v1`、`xiao-tof-8x8-wifi-v1`，不再运行下文 USB 固件。
两份原始完整 Flash 已备份，凭据仅保存在本地忽略文件与硬件 NVS。
45 秒手机测试 1295/1295 有效快照、919 相机帧与 230 ToF 帧，未使用电脑中转。
界面改为相机主视图，原始数据可展开；算法仍是基础完整 ToF 区域走廊相交。
使用、时龄边界和验证见 [展示指南](../../../docs/HARDWARE_OBSTACLE_DEMO.md)；
协议见 [WIRELESS_PROTOCOL.md](WIRELESS_PROTOCOL.md)。证据在
`artifacts.local/hardware-bringup/wireless-demo-20260922/`。历史研究结论不变。

2026-09-22：用户授权硬件展示并暂不使用 CNH。XIAO 已切回现有
`xiao-tof-8x8-v1`，完整旧 Flash/配置已备份，Atom 未改。
80 帧/约 5.130 Hz 的 64 区读出连续且无解析错误；已有异常/UNKNOWN 仍保留。
手机 App 接入与用法见 [8×8 展示](../../../docs/HARDWARE_OBSTACLE_DEMO.md)，
一键中转入口为 `app-demo.ps1`；这取代下文“暂不推进手机”的当前操作状态，
历史 CNH 结果和原始证据不变。证据位于
`artifacts.local/hardware-bringup/demo-8x8-20260922/`。

更新：2026-09-21。性质：工程接入，独立于避障模拟研究。

最新 [CNH 增量价值审计](CNH_INCREMENTAL_20260921.md) 完成：全部 242 个可评估
半遮挡区域观测中，107 个单值偏近物但有后峰、47 个偏背景但有近端、1 个 UNKNOWN
保留双证据；另 87 个只有近端。原七区双证据贯穿 22 帧/3.953 秒，其中四区单值
在近远参考间切换。保留场景参考条件下的多成分观测线索，未证明距离恢复或告警收益。
240/242 幅值外推及已消费单场景限制不变；只审计保存判断，无新评分、采集或阈值变化。

用户暂不方便做固定前景/改变背景的实物对照，先暂停该项。已完成
[CNH 对照回放](CNH_REPLAY.md)：83 帧相机/CNH、全部 16 区、572 条已有判断，
观测台 `/cnh-review` 可直接查看。不新增采集或研究结论，不自动恢复实物对照。

最新授权的 [CNH 弱后峰检验](CNH_LATE_ECHO_20260921.md) 已完成：复用已消费四步记录，
采用背景定位的局部峰对比与缩放/±1-bin 近物尾部包络。11 区可评估，7 区半遮挡
154/154 区域帧通过；纯近物检查 0/55 后峰误报，恢复 220/220 有后峰且 0/220 双证据。
保留 `EXPLORATORY_TAIL_EXCESS` 候选机制，未证明物理双目标、距离恢复或避障收益。
240/242 混合观测涉及近端幅值外推，已消费数据与小控制样本不能提供独立误报率。
旧两轮负结果不改；本轮没有采集或改硬件，停止新评分，不自动进入下一次实测。

最新实机：[CNH 四步成分对照](CNH_COMPONENTS_20260921.md) 已完成，1975 JPEG、444 CNH，
传输无错误或缺帧。固定纯背景/近景参考后，12 个区域可评价，0 区通过完整波形 NNLS
判据：264/264 半遮挡区域帧通过双系数和残差门槛，但 0/264 达到整体误差改善 0.10；
恢复背景 240/240 相容且无双成分误报。保留 `NOT_SUPPORTED`，不调门槛。部分后峰
仍可见，但强近峰主导全波形范数，此结果不证明 CNH 缺少双成分信息。手抖、姿态
及近乎全覆盖偏差已记录，不把拟合系数当作真实目标或测距。采集结束，设备参数未改。

最新：用户要求暂不推进手机，转向电脑端信息价值研究。已完成一次
[CNH 固定指纹检验](CNH_FINGERPRINT_20260921.md)：消费已录 RGB 核验窗口，用后时段
背景作回顾参考，主区域标量失效背景 21/21 UNKNOWN。完整/去 bin 0 的 cosine 均把
书本阶段排得更像参考，描述性 AUC=0，结论 `NOT_SUPPORTED`，关闭这个固定配方。
后段 bin 7 在两背景阶段幅值接近、近段变化很大，仅保留成分分离的待检验线索，
不宣称距离恢复、CNH 无信息或融合收益。3 项测试及独立重算通过；中央登记受旧账本
第 303 行及 unknown terminal 阻塞，回执已保留。未新采集、改固件或改模拟路线。

模块审视见 [硬件审视](REVIEW_NEXT_20260921.md)。其中诊断固件与一次对照已完成：
XIAO 现运行独立 `xiao-cnh-diag-v2`；旧 Flash 已备份。实际为 Strongest、4×4、5 Hz、
20 ms；102 帧的 1632 个区域换算一致，近零/零值仍在，未宣称修复。详见
[诊断实测](LOCAL_DIAGNOSTIC_20260921.md)。拆分/翻转装配对照未执行，已取消。
本机 [硬件观测台](DASHBOARD.md) 已完成：相机、ToF/CNH、质量提示、录制和回放。
52 项测试通过；短时实机获得 388 张 JPEG、77 帧 CNH，ToF 有一条损坏记录和一帧
序号缺口，界面已显式报告，不能称为无损传输通过。近零值根因仍未确定。
当前停止采集，保留观测台供查看；尚未完成空间/曝光标定或手机接入。模拟路线保持独立。

随后完成一次 [120 秒双路传输验收](TRANSPORT_20260921.md)：2373 张 JPEG、613 帧
CNH，无损坏或序号缺帧，文件哈希/分块回放一致，串口已释放。相机实际接收跨度
117 秒、20.274 fps，ToF 119.734 秒、5.111 Hz。旧损坏帧已证实存在于原始串口
字节中，不是 UI 或解析分块引入；本次未复现，不能声称已修复。该轮验收结束。

[方向检查](ORIENTATION_CHECK.md) 随后完成一段 50 秒屏幕引导采集：1189 JPEG、
256 CNH，无传输错误或缺帧。按实际 RGB 而非提示标签复核后，本段支持原始行号
增大对应画面向左；上下轴仍未确定，未应用任何旋转/镜像或像素配准。该轮结束，
无需继续移动设备；不自动重录。

用户随后授权的 [手动上下补查](MANUAL_VERTICAL_CHECK.md) 已完成：1504 JPEG、290
CNH，无传输错误或缺帧，三次摆好标记后各录 6 秒，末段自动停止。按固定内窗口
与实际 RGB 复核，上下轴支持原始列号增大对应画面向上；结合上一段左右证据，
已保存 [当前装配粗朝向](coarse-orientation.json)。仅为轴向/翻转关系，未做像素
投影或距离标定；观测台保留原始网格，不自动重排。此轮完成，不追加录制。

## 已有证据

手机 USB 中转显示已完成，见 [手机观测台](PHONE_VIEW.md)：三星 S24 Ultra 浏览器实测
相机帧号更新、4×4 ToF 与 CNH 显示、断开转发后错误及过期提示，并恢复连接。一次
40 秒检查在电脑端保存 821 JPEG、204 CNH，无采集错误或序号缺失。已补一键连接/
断开脚本；电脑继续采集，手机查看，本阶段不含独立手机采集、无线传输或推理。

- XIAO 识别为 ESP32-S3 rev 0.2，8 MB Flash；原有相机程序已做全量备份。
- I²C 0x29 连续应答，ID 0xF0 / revision 0x0C，驱动识别为 VL53L8 家族。
  此身份读数不单独证明 CX/CH 产品后缀。
- 8×8 测距：首次 81 帧约 5.128 Hz；用户调整朝向后 103 帧约 5.106 Hz。
  后一段帧序号连续、API 无读取错误，中心状态 5 距离中位数 594 mm。
- 零距离及非状态 5 值保留为原始数据，显示时为 UNKNOWN；未做尺量精度标定。
- CNH 16 区域 × 24 bin、5 Hz 固件编译和刷写成功，但第一次采集在
  `is_alive` 阶段返回 1，CNH 尚未启动，取得有效 CNH 帧数为 0。
- 2026-09-21 移至本机后，同一已刷写程序实际输出 CNH：首次 12 秒 61 帧；
  用户确认朝向墙面/纸板后 20 秒 102 帧，约 5.108 Hz，中心有效距离中位数 710 mm。
  两段序号均连续、无设备错误，16×24 数组非全零且缩放后有限；墙面段首尾各有
  一条不完整 JSON，已保留。只确认真实数据读出，未做尺量或直方图幅值标定。
- 前景物体对比 20 秒收到 103 帧，中心 status=5 距离中位数 182 mm；中央四区
  CNH 主峰均在 bin 1。前景段 status=9 占 801/1648 个区域读数，仍按现行保守
  规则记作 UNKNOWN，不更改有效性门槛。详见 [本机读出记录](LOCAL_CNH_20260921.md)。

## 当前阻塞与下一步

Atom 独立 USB 相机接入也已完成：最终使用 USB-OTG CDC 的 `atom-usb-jpeg-v3-otg`，
17.046 秒收到 472 张 640×480 JPEG，27.706 fps，全部解码成功、请求序号连续。
已保留旧 Atom 8 MB Flash 和三段传输失败证据；见 [Atom 实机记录](LOCAL_ATOM_20260921.md)。
本轮 Atom 已刷写并验证新固件；下段“只读取串口”仅指此前的 XIAO/CNH 采集。

离线准备已完成：隔离 Python 环境、三个固件的独立编译、18 项采集协议测试，
以及既有两段成功测距日志和一段失败 CNH 日志的回放。
绘图工具用明确标注的合成 CNH 数据测试，不算 CNH 实机成功。

此前 CNH 本机复测只读取串口，没有重新刷写或主动重启；后续 v2 诊断升级则已完成
备份、刷写和复位，见顶部最新状态。两阶段均未连接远程主机。
此前初始化阻塞未在本次连续读出中出现，但这不能单独确定此前故障原因。
墙面和前景对比已完成，三段共 266 帧 CNH。固件文件与实机内容尚未做读回比对。
两路现已在用户固定装配后并行读出：15/45/40 秒三段共 2482 张 JPEG、511 帧 CNH，
均无采集错误或序号缺失。补录中纸板靠近占据画面，同时中央三个 ToF 区域从约
240 mm 降至约 120 mm；桌面占据较大画面，另一中央区此前已读到约 130 mm，
尚不能确定区域与物体对应。两次完整移入/移开循环未验证，不宣称标定或同步。
见 [固定装配并行记录](LOCAL_PAIR_20260921.md)。用户没有尺子，后续改为手持整套设备
靠近/退远，已完成 40 秒检查：920 张 JPEG、205 帧 CNH，无传输错误/缺帧；中央三个
区域约 500 → 373 → 1040 mm，画面也有相应变化。另有 22 帧中的 42 个区域读数为
1–3 mm 且 status=5，已在原始串口数据核实，不能当作可信物距；原因待查，未改规则。
离线追查发现装配前也有同类近零值；7 段中共 111 个 1–3 mm 区域读数的 CNH 最大值
均在 bin 0，部分异常区仍保留后段峰。不能直接归因于手持、装配或确定光学原因。
随后 20 秒静止检查取得 473 张 JPEG、102 帧 CNH；10 帧中在 zone 0/8 复现 10 个
1–3 mm 读数，中央四区约 50–58 cm，抽帧场景基本不变。首条 JSON 不完整已保留，
完整帧无序号缺失。静止条件下仍可复现，故移动不是必要条件；尚未确定根因。
详见 [手持响应记录](LOCAL_HANDHELD_20260921.md)。无需尺子即可继续工程诊断；
绝对精度标定留待有参考量具时进行。手机接入和融合判决尚未完成。

## 与其他路线的关系

- 不读取避障模拟器真值，不修改该路线的数据、模型、阈值、评测或冻结结果。
- 不将实机日志自动导入模拟训练集，也不把模拟成绩当作硬件性能。
- Android 现有 Atom MJPEG 代码只作为未来适配参考，本轮不修改 App。
- 实机阶段保留测距可用版本和未验证 CNH 候选，两者不互相覆盖源码。
- 本记录是接入状态，不是研究模型晋级或算法实验结论。

复现入口与下一次验收见 [README.md](README.md)。
